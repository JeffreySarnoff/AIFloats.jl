var documenterSearchIndex = {"docs":
[{"category":"section","location":"91-developer.html#dev_docs","page":"Developing","text":"note: Contributing guidelines\nIf you haven't, please read the Contributing guidelines first.\n\nIf you want to make contributions to this package that involves code, then this guide is for you.","title":"Developer documentation"},{"category":"section","location":"91-developer.html#First-time-clone","page":"Developing","text":"tip: If you have writing rights\nIf you have writing rights, you don't have to fork. Instead, simply clone and skip ahead. Whenever upstream is mentioned, use origin instead.\n\nIf this is the first time you work with this repository, follow the instructions below to clone the repository.\n\nFork this repo\nClone your repo (this will create a git remote called origin)\nAdd this repo as a remote:\ngit remote add upstream https://github.com/JeffreySarnoff/AIFloats.jl\n\nThis will ensure that you have two remotes in your git: origin and upstream. You will create branches and push to origin, and you will fetch and update your local main branch from upstream.","title":"First time clone"},{"category":"section","location":"91-developer.html#Linting-and-formatting","page":"Developing","text":"Install a plugin on your editor to use EditorConfig. This will ensure that your editor is configured with important formatting settings.\n\nWe use https://pre-commit.com to run the linters and formatters. In particular, the Julia code is formatted using JuliaFormatter.jl, so please install it globally first:\n\njulia> # Press ]\npkg> activate\npkg> add JuliaFormatter\n\nTo install pre-commit, we recommend using pipx as follows:\n\n# Install pipx following the link\npipx install pre-commit\n\nWith pre-commit installed, activate it as a pre-commit hook:\n\npre-commit install\n\nTo run the linting and formatting manually, enter the command below:\n\npre-commit run -a\n\nNow, you can only commit if all the pre-commit tests pass.","title":"Linting and formatting"},{"category":"section","location":"91-developer.html#Link-checking-locally","page":"Developing","text":"We use lychee for link checking in CI. You can run it locally to avoid waiting for CI. First, install lychee, then run against the repository root using the project config:\n\nlychee --no-progress --config .lychee.toml .\n\nThe config is .lychee.toml at the repository root — the same file CI passes to lychee-action. It holds the exclusions: the stable documentation URL does not resolve before the first tagged release, and the P3109 Public PDF is served through a redirect chain that rate-limits automated requests (what actually pins that document is the SHA-256 in draft_identity()).","title":"Link checking locally"},{"category":"section","location":"91-developer.html#Testing","page":"Developing","text":"The suite requires Julia 1.12 (the package's declared floor). The full run is:\n\njulia --project=. -e 'using Pkg; Pkg.test()'\n\ntest/runtests.jl discovers every test/test-*.jl file and wraps it in a @testset titled from the filename — do not add tests to runtests.jl itself, add a test-your-topic.jl file and it is picked up automatically.\n\nThe full run is long, because the engine is verified against an independent BigInt reference over millions of decisions. While iterating, run only the files your change touches:\n\njulia --project=. -e 'using Test, AIFloats; include(\"test/test-projection.jl\")'\n\nChange touches Run at least\nformats, traits, validity test-binary-format.jl, test-traits.jl\ndatums, encode/decode test-binaryvalue.jl, test-codec.jl\nrounding, saturation, project test-projection.jl, test-rounding-paths.jl\nthe operation register test-ops.jl, test-fastpaths.jl\narray kernels, tables test-kernels.jl, test-tables.jl\nblocks, packed storage test-blocks.jl\nBase surface, promotion test-compat.jl, test-singletons.jl\nconformance, κ test-governance.jl\nthe vendored carrier test-dyadic.jl\nexports, docstrings, Aqua/JET test-quality.jl\nanything the documentation asserts test-doc-contracts.jl\n\nA documentation-only change does not need Pkg.test(). Run the doctests and the docs build instead — see Documentation below.","title":"Testing"},{"category":"section","location":"91-developer.html#Benchmarking","page":"Developing","text":"Chairmarks lives in benchmark/Project.toml and is deliberately not a dependency of the package or of the docs. Run the suite in its own environment:\n\njulia --project=benchmark -t 4 benchmark/runbenchmarks.jl            # every suite\njulia --project=benchmark -t 4 benchmark/runbenchmarks.jl scalar     # one suite\n\nThe suites are scalar, arrays, and latency. Four threads unless JULIA_NUM_THREADS says otherwise, whatever the machine's core count: the threading rows exist to be compared against each other and against THREAD_MIN_ELEMS, which was fitted at four threads, and a machine-dependent count makes two builds incomparable.\n\nRun it from a clean commit. The generated page records the commit it measured and marks a dirty tree; a row from a dirty or older tree is historical evidence, not a description of the code you are reading. Never hand-copy a number from it into prose — link to the row instead.","title":"Benchmarking"},{"category":"section","location":"91-developer.html#Continuous-integration","page":"Developing","text":"Workflow What it runs\nTest.yml / TestOnPRs.yml the package test suite via ReusableTest.yml, on Julia 1.12 and 1\nDocs.yml the documentation build, including doctests and a full benchmark run (AIFLOATS_DOCS_BENCHMARKS=1)\nLint.yml pre-commit hooks and the lychee link check\nCompatHelper.yml, TagBot.yml dependency bounds and release tagging\n\nCodecov runs on the Julia 1 / ubuntu-latest cell only.","title":"Continuous integration"},{"category":"section","location":"91-developer.html#Working-on-a-new-issue","page":"Developing","text":"We try to keep a linear history in this repo, so it is important to keep your branches up-to-date.\n\nFetch from the remote and fast-forward your local main\ngit fetch upstream\ngit switch main\ngit merge --ff-only upstream/main\nBranch from main to address the issue (see below for naming)\ngit switch -c 42-add-answer-universe\nPush the new local branch to your personal remote repository\ngit push -u origin 42-add-answer-universe\nCreate a pull request to merge your remote branch into the org main.","title":"Working on a new issue"},{"category":"section","location":"91-developer.html#Branch-naming","page":"Developing","text":"If there is an associated issue, add the issue number.\nIf there is no associated issue, and the changes are small, add a prefix such as \"typo\", \"hotfix\", \"small-refactor\", according to the type of update.\nIf the changes are not small and there is no associated issue, then create the issue first, so we can properly discuss the changes.\nUse dash separated imperative wording related to the issue (e.g., 14-add-tests, 15-fix-model, 16-remove-obsolete-files).","title":"Branch naming"},{"category":"section","location":"91-developer.html#Commit-message","page":"Developing","text":"Use imperative or present tense, for instance: Add feature or Fix bug.\nHave informative titles.\nWhen necessary, add a body with details.\nIf there are breaking changes, add the information to the commit message.","title":"Commit message"},{"category":"section","location":"91-developer.html#Before-creating-a-pull-request","page":"Developing","text":"tip: Atomic git commits\nTry to create \"atomic git commits\" (recommended reading: The Utopic Git History).\n\nMake sure the tests pass.\nMake sure the pre-commit tests pass.\nFetch any main updates from upstream and rebase your branch, if necessary:\ngit fetch upstream\ngit rebase upstream/main BRANCH_NAME\nThen you can open a pull request and work with the reviewer to address any issues.","title":"Before creating a pull request"},{"category":"section","location":"91-developer.html#Documentation","page":"Developing","text":"Set up the docs environment once:\n\njulia --project=docs -e 'using Pkg; Pkg.develop(path = \".\"); Pkg.instantiate()'\n\nBuild the site:\n\nAIFLOATS_DOCS_BENCHMARKS=0 julia --project=docs docs/make.jl\n\nAIFLOATS_DOCS_BENCHMARKS=0 skips the benchmark run, which is what you want for ordinary prose work — it takes minutes, and the previously generated 60-benchmarks.md is left untouched rather than overwritten with a placeholder. Leave the variable unset when you actually intend to regenerate measurements.\n\nTwo pages are generated by docs/make.jl and must not be hand-edited:\n\ndocs/src/60-benchmarks.md — the benchmark run;\ndocs/src/95-reference/*.md and the 95-reference.md landing page — the public API listing, built from Base.isexported and Base.ispublic. The build fails if a public binding is neither documented nor listed in REFERENCE_EXEMPT.\n\nThe page tree is explicit in docs/make.jl (PAGES). Adding a page means adding a line there; the build fails if docs/src holds a Markdown file PAGES does not list, which is what keeps planning documents out of the published site.\n\nRun the source doctests on their own — much faster than a full build:\n\njulia --project=docs -e 'using Documenter: DocMeta, doctest; using AIFloats; DocMeta.setdocmeta!(AIFloats, :DocTestSetup, :(using AIFloats); recursive=true); doctest(AIFloats)'\n\nFor live preview, julia --project=docs then using LiveServer; servedocs().","title":"Documentation"},{"category":"section","location":"91-developer.html#Making-a-new-release","page":"Developing","text":"AIFloats.jl is not yet in the General registry. Until it is, a release is the version bump, the changelog entry, and the tag; the registrator steps below apply from the first registration onward.\n\nTo create a new release:\n\nCreate a branch release-x.y.z\nUpdate version in Project.toml\nUpdate the CHANGELOG.md:\nRename the section \"Unreleased\" to \"[x.y.z] - yyyy-mm-dd\" (i.e., version under brackets, dash, and date in ISO format)\nAdd a new section on top of it named \"Unreleased\"\nAdd a new link in the bottom for version \"x.y.z\"\nChange the \"[unreleased]\" link to use the latest version - end of line, vx.y.z ... HEAD.\nCreate a commit \"Release vx.y.z\", push, create a PR, wait for it to pass, merge the PR.\nGo back to main screen and click on the latest commit (link: https://github.com/JeffreySarnoff/AIFloats.jl/commit/main)\nAt the bottom, write @JuliaRegistrator register\n\nAfter that, you only need to wait and verify:\n\nWait for the bot to comment (should take < 1m) with a link to a PR to the registry\nFollow the link and wait for a comment on the auto-merge\nThe comment should say that all is well, and auto-merge should follow shortly\nAfter the merge happens, TagBot will trigger and create a new GitHub tag. Check on https://github.com/JeffreySarnoff/AIFloats.jl/releases\nAfter the release is created, a \"docs\" GitHub action starts for the tag.\nAfter it passes, a deploy action will run.\nAfter that runs, the stable docs should be updated. Check them and look for the version number.","title":"Making a new release"},{"category":"section","location":"60-benchmarks.html#benchmarks","page":"Benchmark results","text":"Generated by running benchmark/runbenchmarks.jl during a documentation build. Nothing here is hand-entered.\n\nThese numbers describe the machine that built this page, recorded in the header below along with the commit and the thread count. A shared CI runner is noisy and its absolute times mean little; the ratios between rows on one run are the informative part. Reproduce locally with:\n\njulia --project=benchmark -t 4 benchmark/runbenchmarks.jl          # all suites\njulia --project=benchmark -t 4 benchmark/runbenchmarks.jl scalar   # one suite\n\nThe suite is run with four threads unless JULIA_NUM_THREADS says otherwise, whatever the machine's core count, so that the threading rows stay comparable between builds.\n\nWhat the columns mean: time per call, then allocations and bytes when a call allocates at all (their absence is itself the claim — most scalar rows are allocation-free), and elements per second for array rows. The latency suite measures package load and first-call compilation in fresh processes, because those happen exactly once and cannot be sampled.\n\nCheck the commit in the header below. These rows describe the tree that was measured, which is not necessarily the tree you are reading about. A row marked (dirty), or naming a commit older than the current one, is historical evidence — see Performance characteristics for the design decisions these measurements justify, and re-run the suite from a clean commit before quoting a number anywhere else.\n\nAIFloats benchmarks\n  julia    1.12.7   threads=4\n  cpu      AMD EPYC 7763 64-Core Processor\n  commit   c64eb29 (dirty)\n  defaults ρ=ρ(RoundToEven, SatNone)  TABLE_EAGER_BITS=16  THREAD_MIN_ELEMS=1024\n\n\n── scalar — codec and projection ───────────────────────────────\ndecode K=8 (generated table)                          3.1 ns\ndecode K=12 (computed)                                5.0 ns\ndecode K=16 (rung 2)                                 27.5 ns\ncodepoint                                             3.1 ns\nproject Float64 → K=8                                15.1 ns\nproject Float64 → K=12                               15.1 ns\nproject Float64 → K=8 RTZ_SF                         14.5 ns\nproject Float64 → K=8 RSA_SN                         14.5 ns\nproject Float128 → K=16                              25.3 ns\nbinary128 → exact Dyadic                              5.2 ns\nbinary128 exact Float64 accept                       13.3 ns\nbinary128 exact Float64 refuse                       12.2 ns\norder_key                                             3.1 ns\nNextGreaterThan                                       3.1 ns\nClass                                                 3.4 ns\n\n── scalar — construction ───────────────────────────────────────\nT8(v)        (Float64 value)                         17.6 ns\nT8(v)        (Float32 value)                         17.6 ns\nT8(v)        (Integer value)                         19.7 ns\nconvert(T8, v)                                       17.9 ns\nfromcode(T8, c)   (code point)                        4.0 ns\nConvert(F8, RTE_SN, v)                               15.4 ns\n\n── scalar — operations by registry group, K=8 ──────────────────\nAdd       explicit ρ                                 21.6 ns\nAdd       task default ρ                             23.1 ns\nAdd       scoped non-RTE ρ                           65.4 ns\nDefaultProjection() bound                            47.1 ns\nDefaultProjection() unbound                           4.3 ns\nSubtract  explicit ρ                                 21.3 ns\nMultiply  explicit ρ                                 21.0 ns\nDivide    explicit ρ                                 20.7 ns\nFMA       explicit ρ                                 29.3 ns\nFAA       explicit ρ                                 23.6 ns\nSqrt      explicit ρ                                 43.2 ns\nExp       explicit ρ                                 50.2 ns\nExp2      explicit ρ                                 51.8 ns\nLog       explicit ρ                                 65.4 ns\nLog2      explicit ρ                                 64.9 ns\nSin       explicit ρ                                 63.0 ns\nTanh      explicit ρ                                 58.8 ns\nArcTan2Pi explicit ρ                                 74.2 ns\nMaximum   explicit ρ                                 17.6 ns\nConvert   K=8 → K=12                                 15.7 ns\nAdd       RSA_SN (stochastic)                        21.1 ns\nAdd       FAST_ARITH=false                           21.6 ns\nExp       FAST_ENCLOSURE=false                     2291.8 ns  allocs=44 bytes=1328\n\n── scalar — operations, K=12 and K=16 ──────────────────────────\nAdd       K=12                                       30.1 ns\nMultiply  K=12                                       29.3 ns\nFMA       K=12                                       38.6 ns\nExp       K=12                                       58.4 ns\nLog       K=12                                       68.8 ns\nAdd       K=16 (rung 2)                              96.3 ns\nMultiply  K=16 (rung 2)                              94.4 ns\nExp       K=16 (rung 2)                             233.9 ns  allocs=2 bytes=48\n\n── scalar — Base veneers ───────────────────────────────────────\nx + y                                                23.8 ns\nx * y                                                24.4 ns\nfma(x, y, z)                                         32.7 ns\nexp(x)                                               52.9 ns\nlog(x)                                               67.6 ns\nx < y                                                 3.1 ns\nFloat64(x)                                            3.1 ns\nBigFloat(x)                                          23.0 ns  allocs=1 bytes=96\nround(x)                                             46.0 ns\nfloor(x)                                             46.0 ns\nceil(x)                                              46.1 ns\ntrunc(x)                                             44.8 ns\nround(x, RoundUp)                                    46.1 ns\neps(x)                                               27.3 ns\nhash(x)                                              20.1 ns\nrand(T8)                                             15.9 ns\n\n── arrays — vmap! and broadcasting ─────────────────────────────\nvmap! Add K=8  warm table N=4096                   2189.1 ns  1.871 Gelem/s\nvmap! Add K=12 compute   N=4096                   96141.0 ns  allocs=22 bytes=1568  0.043 Gelem/s\nvmap! Add K=12 compute 1thr N=4096               117942.0 ns  0.035 Gelem/s\nvmap! Exp K=8  warm table N=4096                   1584.6 ns  2.585 Gelem/s\nvmap! Exp K=12 warm table N=4096                   1578.5 ns  2.595 Gelem/s\nA .+ B      K=8  N=4096                            2354.0 ns  allocs=3 bytes=4168  1.74 Gelem/s\nA .+ B      K=12 N=4096                           96561.0 ns  allocs=25 bytes=9832  0.042 Gelem/s\nexp.(A)     K=8  N=4096                            1743.0 ns  allocs=3 bytes=4168  2.35 Gelem/s\nfma.(A,B,A) K=8  N=4096                          108534.0 ns  allocs=25 bytes=5800  0.038 Gelem/s\nFloat64.(A) K=8  N=4096                            1595.5 ns  allocs=3 bytes=32840  2.567 Gelem/s\nsort(A)     K=8  N=4096                            4219.7 ns  allocs=5 bytes=6320  0.971 Gelem/s\nsort(A)     K=12 N=4096                           12042.0 ns  allocs=8 bytes=49464  0.34 Gelem/s\n\nvmap! Add K=8  warm table N=65536                 32551.0 ns  2.013 Gelem/s\nvmap! Add K=12 compute   N=65536                 971093.0 ns  allocs=22 bytes=1568  0.067 Gelem/s\nvmap! Add K=12 compute 1thr N=65536            1.903416e6 ns  0.034 Gelem/s\nvmap! Exp K=8  warm table N=65536                 22803.0 ns  2.874 Gelem/s\nvmap! Exp K=12 warm table N=65536                 23714.0 ns  2.764 Gelem/s\nA .+ B      K=8  N=65536                          32781.0 ns  allocs=3 bytes=65608  1.999 Gelem/s\nA .+ B      K=12 N=65536                       1.033168e6 ns  allocs=25 bytes=132712  0.063 Gelem/s\nexp.(A)     K=8  N=65536                          23945.0 ns  allocs=3 bytes=65608  2.737 Gelem/s\nfma.(A,B,A) K=8  N=65536                       1.098018e6 ns  allocs=25 bytes=67240  0.06 Gelem/s\nFloat64.(A) K=8  N=65536                          23835.0 ns  allocs=3 bytes=524360  2.75 Gelem/s\nsort(A)     K=8  N=65536                          49082.0 ns  allocs=5 bytes=67760  1.335 Gelem/s\nsort(A)     K=12 N=65536                          85000.0 ns  allocs=8 bytes=172344  0.771 Gelem/s\n\n\n── arrays — cold table build (evals=1) ─────────────────────────\nempty_tables! + vmap! Add K=8 N=4096             765623.0 ns  allocs=24 bytes=75656  0.005 Gelem/s\nempty_tables! + vmap! Exp K=8 N=4096              14807.0 ns  allocs=1 bytes=288  0.277 Gelem/s\n\n── arrays — packed storage ─────────────────────────────────────\nPackedVector(A)   K=8  N=65536                    67106.0 ns  allocs=2 bytes=65576  0.977 Gelem/s\nPackedVector(A)   K=5  N=65536                    85461.0 ns  allocs=2 bytes=41000  0.767 Gelem/s\ncollect(P)        K=5  N=65536                    11682.0 ns  allocs=3 bytes=65608  5.61 Gelem/s\nP[i]              K=5                                 3.7 ns\nvmap Negate packed K=5 N=65536                    26630.0 ns  allocs=4 bytes=65640  2.461 Gelem/s\nvmap Negate vector K=5 N=65536                    23554.0 ns  allocs=3 bytes=65608  2.782 Gelem/s\nvmap Exp    packed K=5 N=65536                    26599.0 ns  allocs=4 bytes=65640  2.464 Gelem/s\nvmap Exp    vector K=5 N=65536                    22903.0 ns  allocs=3 bytes=65608  2.861 Gelem/s\n\n── arrays — blocks ─────────────────────────────────────────────\nblockdecode        B=16                              33.5 ns  allocs=1 bytes=144\nBlockReduceAdd     B=16                             539.9 ns\nBlockReduceMultiply B=16                            160.8 ns\nBlockDotProduct    B=16                             817.4 ns\nBlockAdd           B=16                             358.5 ns\n\nblockdecode        B=32                              66.6 ns  allocs=1 bytes=272\nBlockReduceAdd     B=32                            1032.3 ns\nBlockReduceMultiply B=32                            297.1 ns\nBlockDotProduct    B=32                            1217.3 ns\nBlockAdd           B=32                             710.3 ns\n\nScaledAdd (scalar lanes)                             23.6 ns\nBlockReduceAdd rung 2 B=16                          959.5 ns\nBlockReduceMultiply rung 2 B=16                     741.6 ns\n\n── latency — load and first call (fresh process each) ──────────\nusing AIFloats                                   132.61 ms\nfirst T(1.3)                                        0.0 ms\nfirst Add(x,y)                                     0.03 ms\nfirst Exp(x)                                       0.03 ms\nfirst Log(x)                                       0.03 ms\nfirst sort                                         0.05 ms\nfirst vmap! Add                                    1.44 ms\nfirst A .+ B                                       0.02 ms\nfirst exp.(A)                                      0.07 ms\nfirst PackedVector                                 0.04 ms\nfirst BlockReduceAdd                               3.86 ms","title":"Benchmark results"},{"category":"section","location":"95-reference/30-blocks.html#Blocks-and-scaled-operations","page":"Blocks and scaled operations","text":"Part of the Reference; see that page for how this listing is generated and what it guarantees.\n\nPages = [\"30-blocks.md\"]\nDepth = 2","title":"Blocks and scaled operations"},{"category":"section","location":"95-reference/30-blocks.html#Block-containers","page":"Blocks and scaled operations","text":"","title":"Block containers"},{"category":"section","location":"95-reference/30-blocks.html#Block-conversions","page":"Blocks and scaled operations","text":"","title":"Block conversions"},{"category":"section","location":"95-reference/30-blocks.html#Block-operations","page":"Blocks and scaled operations","text":"","title":"Block operations"},{"category":"section","location":"95-reference/30-blocks.html#Scaled-operations","page":"Blocks and scaled operations","text":"","title":"Scaled operations"},{"category":"section","location":"95-reference/30-blocks.html#Index","page":"Blocks and scaled operations","text":"Pages = [\"30-blocks.md\"]","title":"Index"},{"category":"type","location":"95-reference/30-blocks.html#AIFloats.Block","page":"Blocks and scaled operations","text":"Block{B, S<:BinaryValue, E<:BinaryValue}\n\nA P3109 block (s, (x₁ … x_B)): a scale datum of type S and B ≥ 1 element datums of type E (draft §5). isbits.\n\nBlock(Binary8p1uf(4.0), (Binary5p2se(3.0), Binary5p2se(0.5)))\nBlock(s, x1, x2, x3)          # varargs form\n\n\n\n\n\n","title":"AIFloats.Block"},{"category":"type","location":"95-reference/30-blocks.html#AIFloats.BlockVector","page":"Blocks and scaled operations","text":"BlockVector{B,S,E} <: AbstractVector{Block{B,S,E}}\n\nStructure-of-arrays storage for blocks: scales::Vector{S} and elems::Matrix{E} (B × n, column-major, so each block's elements are one contiguous column). Build from a vector of blocks: BlockVector(blocks).\n\n\n\n\n\n","title":"AIFloats.BlockVector"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.blockdecode","page":"Blocks and scaled operations","text":"blockdecode(b::Block) -> NTuple{B}\n\nThe values the block denotes: per lane ωMultiply(decode(s), decode(xᵢ)), exact on a carrier (Float64 when the product is exactly representable there, BigFloat otherwise). The draft's fold algebra applies: 0 · ∞ and NaN lanes are NaN. Not exported.\n\n\n\n\n\n","title":"AIFloats.blockdecode"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.blockproject","page":"Blocks and scaled operations","text":"blockproject(FR, ρ, sr, Z; rng) -> Block\n\nωBlockProject (draft §5.1.2): each lane result in Z (a carrier value or an enclosure) divided by the result scale sr and projected into FR under ρ; the block returned carries sr. Not exported.\n\n\n\n\n\n","title":"AIFloats.blockproject"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.blocksize","page":"Blocks and scaled operations","text":"The number of elements B of a block or block type.\n\n\n\n\n\n","title":"AIFloats.blocksize"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.elemformat","page":"Blocks and scaled operations","text":"The elements' Binary format.\n\n\n\n\n\n","title":"AIFloats.elemformat"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.scaleformat","page":"Blocks and scaled operations","text":"The scale's Binary format.\n\n\n\n\n\n","title":"AIFloats.scaleformat"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ConvertFromBlock","page":"Blocks and scaled operations","text":"ConvertFromBlock(FR, ρ, b::Block; rng) -> NTuple{B, BinaryValue}\n\nDraft §5.2.1: decode the block and project each lane into FR — no scale division; the scale is folded into the values.\n\n\n\n\n\n","title":"AIFloats.ConvertFromBlock"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ConvertToBlock","page":"Blocks and scaled operations","text":"ConvertToBlock(FR, ρ, xs::NTuple{B, BinaryValue}, s::BinaryValue; rng) -> Block\n\nDraft §5.2.2: the elements' values, block-projected against the supplied scale s into element format FR.\n\n\n\n\n\n","title":"AIFloats.ConvertToBlock"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ConvertToBlockMaxAbsFinite","page":"Blocks and scaled operations","text":"ConvertToBlockMaxAbsFinite(FS, FR, ρs, ρ, xs::NTuple{B, BinaryValue}; rng) -> Block\n\nDraft §5.2.3: the scale is reduce(ωMaximumFinite, [NaN, |x₁| …]) projected into FS under ρs; the elements are then block-projected against it into FR under ρ. All-NaN input gives a NaN scale (and NaN elements); infinities never set the scale but survive as ±Inf elements.\n\n\n\n\n\n","title":"AIFloats.ConvertToBlockMaxAbsFinite"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockAbs","page":"Blocks and scaled operations","text":"BlockAbs(fr, ρ, b1, sr; rng) -> Block\n\nAbs applied lanewise to 1 shared-scale block, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, Abs is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent Abs calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockAbs"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockAdd","page":"Blocks and scaled operations","text":"BlockAdd(fr, ρ, b1, b2, sr; rng) -> Block\n\nAdd applied lanewise to 2 shared-scale blocks, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, Add is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent Add calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockAdd"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockArcCos","page":"Blocks and scaled operations","text":"BlockArcCos(fr, ρ, b1, sr; rng) -> Block\n\nArcCos applied lanewise to 1 shared-scale block, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, ArcCos is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent ArcCos calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockArcCos"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockArcCosPi","page":"Blocks and scaled operations","text":"BlockArcCosPi(fr, ρ, b1, sr; rng) -> Block\n\nArcCosPi applied lanewise to 1 shared-scale block, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, ArcCosPi is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent ArcCosPi calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockArcCosPi"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockArcCosh","page":"Blocks and scaled operations","text":"BlockArcCosh(fr, ρ, b1, sr; rng) -> Block\n\nArcCosh applied lanewise to 1 shared-scale block, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, ArcCosh is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent ArcCosh calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockArcCosh"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockArcSin","page":"Blocks and scaled operations","text":"BlockArcSin(fr, ρ, b1, sr; rng) -> Block\n\nArcSin applied lanewise to 1 shared-scale block, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, ArcSin is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent ArcSin calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockArcSin"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockArcSinPi","page":"Blocks and scaled operations","text":"BlockArcSinPi(fr, ρ, b1, sr; rng) -> Block\n\nArcSinPi applied lanewise to 1 shared-scale block, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, ArcSinPi is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent ArcSinPi calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockArcSinPi"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockArcSinh","page":"Blocks and scaled operations","text":"BlockArcSinh(fr, ρ, b1, sr; rng) -> Block\n\nArcSinh applied lanewise to 1 shared-scale block, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, ArcSinh is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent ArcSinh calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockArcSinh"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockArcTan","page":"Blocks and scaled operations","text":"BlockArcTan(fr, ρ, b1, sr; rng) -> Block\n\nArcTan applied lanewise to 1 shared-scale block, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, ArcTan is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent ArcTan calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockArcTan"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockArcTan2","page":"Blocks and scaled operations","text":"BlockArcTan2(fr, ρ, b1, b2, sr; rng) -> Block\n\nArcTan2 applied lanewise to 2 shared-scale blocks, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, ArcTan2 is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent ArcTan2 calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockArcTan2"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockArcTan2Pi","page":"Blocks and scaled operations","text":"BlockArcTan2Pi(fr, ρ, b1, b2, sr; rng) -> Block\n\nArcTan2Pi applied lanewise to 2 shared-scale blocks, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, ArcTan2Pi is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent ArcTan2Pi calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockArcTan2Pi"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockArcTanPi","page":"Blocks and scaled operations","text":"BlockArcTanPi(fr, ρ, b1, sr; rng) -> Block\n\nArcTanPi applied lanewise to 1 shared-scale block, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, ArcTanPi is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent ArcTanPi calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockArcTanPi"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockArcTanh","page":"Blocks and scaled operations","text":"BlockArcTanh(fr, ρ, b1, sr; rng) -> Block\n\nArcTanh applied lanewise to 1 shared-scale block, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, ArcTanh is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent ArcTanh calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockArcTanh"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockClamp","page":"Blocks and scaled operations","text":"BlockClamp(fr, ρ, b1, b2, b3, sr; rng) -> Block\n\nClamp applied lanewise to 3 shared-scale blocks, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, Clamp is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent Clamp calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockClamp"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockCopySign","page":"Blocks and scaled operations","text":"BlockCopySign(fr, ρ, b1, b2, sr; rng) -> Block\n\nCopySign applied lanewise to 2 shared-scale blocks, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, CopySign is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent CopySign calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockCopySign"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockCos","page":"Blocks and scaled operations","text":"BlockCos(fr, ρ, b1, sr; rng) -> Block\n\nCos applied lanewise to 1 shared-scale block, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, Cos is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent Cos calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockCos"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockCosPi","page":"Blocks and scaled operations","text":"BlockCosPi(fr, ρ, b1, sr; rng) -> Block\n\nCosPi applied lanewise to 1 shared-scale block, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, CosPi is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent CosPi calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockCosPi"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockCosh","page":"Blocks and scaled operations","text":"BlockCosh(fr, ρ, b1, sr; rng) -> Block\n\nCosh applied lanewise to 1 shared-scale block, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, Cosh is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent Cosh calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockCosh"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockDivide","page":"Blocks and scaled operations","text":"BlockDivide(fr, ρ, b1, b2, sr; rng) -> Block\n\nDivide applied lanewise to 2 shared-scale blocks, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, Divide is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent Divide calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockDivide"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockDotProduct","page":"Blocks and scaled operations","text":"BlockDotProduct(FR, ρ, bx::Block{B}, by::Block{B}; rng, R) -> BinaryValue\n\nDraft §5.3.2: the lane products and their sum formed exactly, the ∞/NaN fold algebra resolved on the lane classifications first, then one projection.\n\n\n\n\n\n","title":"AIFloats.BlockDotProduct"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockExp","page":"Blocks and scaled operations","text":"BlockExp(fr, ρ, b1, sr; rng) -> Block\n\nExp applied lanewise to 1 shared-scale block, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, Exp is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent Exp calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockExp"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockExp2","page":"Blocks and scaled operations","text":"BlockExp2(fr, ρ, b1, sr; rng) -> Block\n\nExp2 applied lanewise to 1 shared-scale block, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, Exp2 is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent Exp2 calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockExp2"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockExpMinusOne","page":"Blocks and scaled operations","text":"BlockExpMinusOne(fr, ρ, b1, sr; rng) -> Block\n\nExpMinusOne applied lanewise to 1 shared-scale block, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, ExpMinusOne is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent ExpMinusOne calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockExpMinusOne"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockFAA","page":"Blocks and scaled operations","text":"BlockFAA(fr, ρ, b1, b2, b3, sr; rng) -> Block\n\nFAA applied lanewise to 3 shared-scale blocks, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, FAA is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent FAA calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockFAA"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockFMA","page":"Blocks and scaled operations","text":"BlockFMA(fr, ρ, b1, b2, b3, sr; rng) -> Block\n\nFMA applied lanewise to 3 shared-scale blocks, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, FMA is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent FMA calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockFMA"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockHypot","page":"Blocks and scaled operations","text":"BlockHypot(fr, ρ, b1, b2, sr; rng) -> Block\n\nHypot applied lanewise to 2 shared-scale blocks, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, Hypot is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent Hypot calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockHypot"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockLog","page":"Blocks and scaled operations","text":"BlockLog(fr, ρ, b1, sr; rng) -> Block\n\nLog applied lanewise to 1 shared-scale block, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, Log is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent Log calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockLog"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockLog2","page":"Blocks and scaled operations","text":"BlockLog2(fr, ρ, b1, sr; rng) -> Block\n\nLog2 applied lanewise to 1 shared-scale block, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, Log2 is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent Log2 calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockLog2"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockLogOnePlus","page":"Blocks and scaled operations","text":"BlockLogOnePlus(fr, ρ, b1, sr; rng) -> Block\n\nLogOnePlus applied lanewise to 1 shared-scale block, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, LogOnePlus is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent LogOnePlus calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockLogOnePlus"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockMaximum","page":"Blocks and scaled operations","text":"BlockMaximum(fr, ρ, b1, b2, sr; rng) -> Block\n\nMaximum applied lanewise to 2 shared-scale blocks, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, Maximum is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent Maximum calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockMaximum"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockMaximumFinite","page":"Blocks and scaled operations","text":"BlockMaximumFinite(fr, ρ, b1, b2, sr; rng) -> Block\n\nMaximumFinite applied lanewise to 2 shared-scale blocks, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, MaximumFinite is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent MaximumFinite calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockMaximumFinite"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockMaximumMagnitude","page":"Blocks and scaled operations","text":"BlockMaximumMagnitude(fr, ρ, b1, b2, sr; rng) -> Block\n\nMaximumMagnitude applied lanewise to 2 shared-scale blocks, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, MaximumMagnitude is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent MaximumMagnitude calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockMaximumMagnitude"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockMaximumMagnitudeNumber","page":"Blocks and scaled operations","text":"BlockMaximumMagnitudeNumber(fr, ρ, b1, b2, sr; rng) -> Block\n\nMaximumMagnitudeNumber applied lanewise to 2 shared-scale blocks, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, MaximumMagnitudeNumber is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent MaximumMagnitudeNumber calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockMaximumMagnitudeNumber"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockMaximumNumber","page":"Blocks and scaled operations","text":"BlockMaximumNumber(fr, ρ, b1, b2, sr; rng) -> Block\n\nMaximumNumber applied lanewise to 2 shared-scale blocks, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, MaximumNumber is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent MaximumNumber calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockMaximumNumber"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockMinimum","page":"Blocks and scaled operations","text":"BlockMinimum(fr, ρ, b1, b2, sr; rng) -> Block\n\nMinimum applied lanewise to 2 shared-scale blocks, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, Minimum is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent Minimum calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockMinimum"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockMinimumFinite","page":"Blocks and scaled operations","text":"BlockMinimumFinite(fr, ρ, b1, b2, sr; rng) -> Block\n\nMinimumFinite applied lanewise to 2 shared-scale blocks, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, MinimumFinite is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent MinimumFinite calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockMinimumFinite"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockMinimumMagnitude","page":"Blocks and scaled operations","text":"BlockMinimumMagnitude(fr, ρ, b1, b2, sr; rng) -> Block\n\nMinimumMagnitude applied lanewise to 2 shared-scale blocks, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, MinimumMagnitude is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent MinimumMagnitude calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockMinimumMagnitude"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockMinimumMagnitudeNumber","page":"Blocks and scaled operations","text":"BlockMinimumMagnitudeNumber(fr, ρ, b1, b2, sr; rng) -> Block\n\nMinimumMagnitudeNumber applied lanewise to 2 shared-scale blocks, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, MinimumMagnitudeNumber is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent MinimumMagnitudeNumber calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockMinimumMagnitudeNumber"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockMinimumNumber","page":"Blocks and scaled operations","text":"BlockMinimumNumber(fr, ρ, b1, b2, sr; rng) -> Block\n\nMinimumNumber applied lanewise to 2 shared-scale blocks, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, MinimumNumber is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent MinimumNumber calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockMinimumNumber"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockMultiply","page":"Blocks and scaled operations","text":"BlockMultiply(fr, ρ, b1, b2, sr; rng) -> Block\n\nMultiply applied lanewise to 2 shared-scale blocks, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, Multiply is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent Multiply calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockMultiply"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockNegate","page":"Blocks and scaled operations","text":"BlockNegate(fr, ρ, b1, sr; rng) -> Block\n\nNegate applied lanewise to 1 shared-scale block, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, Negate is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent Negate calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockNegate"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockRSqrt","page":"Blocks and scaled operations","text":"BlockRSqrt(fr, ρ, b1, sr; rng) -> Block\n\nRSqrt applied lanewise to 1 shared-scale block, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, RSqrt is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent RSqrt calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockRSqrt"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockRecip","page":"Blocks and scaled operations","text":"BlockRecip(fr, ρ, b1, sr; rng) -> Block\n\nRecip applied lanewise to 1 shared-scale block, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, Recip is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent Recip calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockRecip"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockReduceAdd","page":"Blocks and scaled operations","text":"BlockReduceAdd(FR, ρ, b::Block; rng, R) -> BinaryValue\n\nDraft §5.3.1: project(reduce(ωAdd, [0, X…])) over the block's decoded lanes — the sum formed exactly, then projected once.\n\n\n\n\n\n","title":"AIFloats.BlockReduceAdd"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockReduceMultiply","page":"Blocks and scaled operations","text":"BlockReduceMultiply(FR, ρ, b::Block; rng, R) -> BinaryValue\n\nDraft §5.3.1: project(reduce(ωMultiply, [1, X…])) — the product formed exactly, then projected once. 0 · ∞ anywhere in the fold is NaN.\n\n\n\n\n\n","title":"AIFloats.BlockReduceMultiply"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockSin","page":"Blocks and scaled operations","text":"BlockSin(fr, ρ, b1, sr; rng) -> Block\n\nSin applied lanewise to 1 shared-scale block, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, Sin is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent Sin calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockSin"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockSinPi","page":"Blocks and scaled operations","text":"BlockSinPi(fr, ρ, b1, sr; rng) -> Block\n\nSinPi applied lanewise to 1 shared-scale block, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, SinPi is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent SinPi calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockSinPi"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockSinh","page":"Blocks and scaled operations","text":"BlockSinh(fr, ρ, b1, sr; rng) -> Block\n\nSinh applied lanewise to 1 shared-scale block, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, Sinh is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent Sinh calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockSinh"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockSoftplus","page":"Blocks and scaled operations","text":"BlockSoftplus(fr, ρ, b1, sr; rng) -> Block\n\nSoftplus applied lanewise to 1 shared-scale block, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, Softplus is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent Softplus calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockSoftplus"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockSqrt","page":"Blocks and scaled operations","text":"BlockSqrt(fr, ρ, b1, sr; rng) -> Block\n\nSqrt applied lanewise to 1 shared-scale block, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, Sqrt is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent Sqrt calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockSqrt"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockSubtract","page":"Blocks and scaled operations","text":"BlockSubtract(fr, ρ, b1, b2, sr; rng) -> Block\n\nSubtract applied lanewise to 2 shared-scale blocks, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, Subtract is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent Subtract calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockSubtract"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockTan","page":"Blocks and scaled operations","text":"BlockTan(fr, ρ, b1, sr; rng) -> Block\n\nTan applied lanewise to 1 shared-scale block, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, Tan is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent Tan calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockTan"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockTanPi","page":"Blocks and scaled operations","text":"BlockTanPi(fr, ρ, b1, sr; rng) -> Block\n\nTanPi applied lanewise to 1 shared-scale block, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, TanPi is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent TanPi calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockTanPi"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.BlockTanh","page":"Blocks and scaled operations","text":"BlockTanh(fr, ρ, b1, sr; rng) -> Block\n\nTanh applied lanewise to 1 shared-scale block, with sr as the result scale.\n\nEach operand block is (scale, elements); the lanes are decoded at scale * element, Tanh is formed on the common carrier, and every lane is projected once into fr against sr (see blockproject). All operand blocks must have the same block size B; the result is a Block of that size.\n\nWhen every operand scale and sr are one, the call reduces to B independent Tanh calls — same answer, no scale arithmetic.\n\nrng applies only under a stochastic ρ, and the lanes draw in index order from one stream.\n\n\n\n\n\n","title":"AIFloats.BlockTanh"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledAbs","page":"Blocks and scaled operations","text":"ScaledAbs(fr, ρ, s1, x1; rng) -> BinaryValue\n\nAbs over 1 scale/value pair (s1, x1) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before Abs is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to Abs on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledAbs"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledAdd","page":"Blocks and scaled operations","text":"ScaledAdd(fr, ρ, s1, x1, s2, x2; rng) -> BinaryValue\n\nAdd over 2 scale/value pairs (s1, x1), (s2, x2) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before Add is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to Add on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledAdd"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledArcCos","page":"Blocks and scaled operations","text":"ScaledArcCos(fr, ρ, s1, x1; rng) -> BinaryValue\n\nArcCos over 1 scale/value pair (s1, x1) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before ArcCos is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to ArcCos on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledArcCos"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledArcCosPi","page":"Blocks and scaled operations","text":"ScaledArcCosPi(fr, ρ, s1, x1; rng) -> BinaryValue\n\nArcCosPi over 1 scale/value pair (s1, x1) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before ArcCosPi is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to ArcCosPi on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledArcCosPi"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledArcCosh","page":"Blocks and scaled operations","text":"ScaledArcCosh(fr, ρ, s1, x1; rng) -> BinaryValue\n\nArcCosh over 1 scale/value pair (s1, x1) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before ArcCosh is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to ArcCosh on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledArcCosh"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledArcSin","page":"Blocks and scaled operations","text":"ScaledArcSin(fr, ρ, s1, x1; rng) -> BinaryValue\n\nArcSin over 1 scale/value pair (s1, x1) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before ArcSin is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to ArcSin on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledArcSin"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledArcSinPi","page":"Blocks and scaled operations","text":"ScaledArcSinPi(fr, ρ, s1, x1; rng) -> BinaryValue\n\nArcSinPi over 1 scale/value pair (s1, x1) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before ArcSinPi is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to ArcSinPi on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledArcSinPi"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledArcSinh","page":"Blocks and scaled operations","text":"ScaledArcSinh(fr, ρ, s1, x1; rng) -> BinaryValue\n\nArcSinh over 1 scale/value pair (s1, x1) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before ArcSinh is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to ArcSinh on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledArcSinh"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledArcTan","page":"Blocks and scaled operations","text":"ScaledArcTan(fr, ρ, s1, x1; rng) -> BinaryValue\n\nArcTan over 1 scale/value pair (s1, x1) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before ArcTan is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to ArcTan on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledArcTan"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledArcTan2","page":"Blocks and scaled operations","text":"ScaledArcTan2(fr, ρ, s1, x1, s2, x2; rng) -> BinaryValue\n\nArcTan2 over 2 scale/value pairs (s1, x1), (s2, x2) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before ArcTan2 is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to ArcTan2 on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledArcTan2"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledArcTan2Pi","page":"Blocks and scaled operations","text":"ScaledArcTan2Pi(fr, ρ, s1, x1, s2, x2; rng) -> BinaryValue\n\nArcTan2Pi over 2 scale/value pairs (s1, x1), (s2, x2) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before ArcTan2Pi is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to ArcTan2Pi on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledArcTan2Pi"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledArcTanPi","page":"Blocks and scaled operations","text":"ScaledArcTanPi(fr, ρ, s1, x1; rng) -> BinaryValue\n\nArcTanPi over 1 scale/value pair (s1, x1) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before ArcTanPi is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to ArcTanPi on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledArcTanPi"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledArcTanh","page":"Blocks and scaled operations","text":"ScaledArcTanh(fr, ρ, s1, x1; rng) -> BinaryValue\n\nArcTanh over 1 scale/value pair (s1, x1) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before ArcTanh is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to ArcTanh on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledArcTanh"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledClamp","page":"Blocks and scaled operations","text":"ScaledClamp(fr, ρ, s1, x1, s2, x2, s3, x3; rng) -> BinaryValue\n\nClamp over 3 scale/value pairs (s1, x1), (s2, x2), (s3, x3) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before Clamp is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to Clamp on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledClamp"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledCopySign","page":"Blocks and scaled operations","text":"ScaledCopySign(fr, ρ, s1, x1, s2, x2; rng) -> BinaryValue\n\nCopySign over 2 scale/value pairs (s1, x1), (s2, x2) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before CopySign is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to CopySign on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledCopySign"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledCos","page":"Blocks and scaled operations","text":"ScaledCos(fr, ρ, s1, x1; rng) -> BinaryValue\n\nCos over 1 scale/value pair (s1, x1) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before Cos is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to Cos on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledCos"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledCosPi","page":"Blocks and scaled operations","text":"ScaledCosPi(fr, ρ, s1, x1; rng) -> BinaryValue\n\nCosPi over 1 scale/value pair (s1, x1) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before CosPi is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to CosPi on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledCosPi"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledCosh","page":"Blocks and scaled operations","text":"ScaledCosh(fr, ρ, s1, x1; rng) -> BinaryValue\n\nCosh over 1 scale/value pair (s1, x1) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before Cosh is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to Cosh on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledCosh"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledDivide","page":"Blocks and scaled operations","text":"ScaledDivide(fr, ρ, s1, x1, s2, x2; rng) -> BinaryValue\n\nDivide over 2 scale/value pairs (s1, x1), (s2, x2) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before Divide is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to Divide on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledDivide"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledExp","page":"Blocks and scaled operations","text":"ScaledExp(fr, ρ, s1, x1; rng) -> BinaryValue\n\nExp over 1 scale/value pair (s1, x1) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before Exp is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to Exp on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledExp"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledExp2","page":"Blocks and scaled operations","text":"ScaledExp2(fr, ρ, s1, x1; rng) -> BinaryValue\n\nExp2 over 1 scale/value pair (s1, x1) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before Exp2 is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to Exp2 on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledExp2"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledExpMinusOne","page":"Blocks and scaled operations","text":"ScaledExpMinusOne(fr, ρ, s1, x1; rng) -> BinaryValue\n\nExpMinusOne over 1 scale/value pair (s1, x1) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before ExpMinusOne is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to ExpMinusOne on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledExpMinusOne"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledFAA","page":"Blocks and scaled operations","text":"ScaledFAA(fr, ρ, s1, x1, s2, x2, s3, x3; rng) -> BinaryValue\n\nFAA over 3 scale/value pairs (s1, x1), (s2, x2), (s3, x3) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before FAA is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to FAA on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledFAA"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledFMA","page":"Blocks and scaled operations","text":"ScaledFMA(fr, ρ, s1, x1, s2, x2, s3, x3; rng) -> BinaryValue\n\nFMA over 3 scale/value pairs (s1, x1), (s2, x2), (s3, x3) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before FMA is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to FMA on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledFMA"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledHypot","page":"Blocks and scaled operations","text":"ScaledHypot(fr, ρ, s1, x1, s2, x2; rng) -> BinaryValue\n\nHypot over 2 scale/value pairs (s1, x1), (s2, x2) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before Hypot is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to Hypot on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledHypot"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledLog","page":"Blocks and scaled operations","text":"ScaledLog(fr, ρ, s1, x1; rng) -> BinaryValue\n\nLog over 1 scale/value pair (s1, x1) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before Log is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to Log on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledLog"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledLog2","page":"Blocks and scaled operations","text":"ScaledLog2(fr, ρ, s1, x1; rng) -> BinaryValue\n\nLog2 over 1 scale/value pair (s1, x1) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before Log2 is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to Log2 on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledLog2"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledLogOnePlus","page":"Blocks and scaled operations","text":"ScaledLogOnePlus(fr, ρ, s1, x1; rng) -> BinaryValue\n\nLogOnePlus over 1 scale/value pair (s1, x1) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before LogOnePlus is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to LogOnePlus on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledLogOnePlus"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledMaximum","page":"Blocks and scaled operations","text":"ScaledMaximum(fr, ρ, s1, x1, s2, x2; rng) -> BinaryValue\n\nMaximum over 2 scale/value pairs (s1, x1), (s2, x2) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before Maximum is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to Maximum on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledMaximum"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledMaximumFinite","page":"Blocks and scaled operations","text":"ScaledMaximumFinite(fr, ρ, s1, x1, s2, x2; rng) -> BinaryValue\n\nMaximumFinite over 2 scale/value pairs (s1, x1), (s2, x2) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before MaximumFinite is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to MaximumFinite on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledMaximumFinite"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledMaximumMagnitude","page":"Blocks and scaled operations","text":"ScaledMaximumMagnitude(fr, ρ, s1, x1, s2, x2; rng) -> BinaryValue\n\nMaximumMagnitude over 2 scale/value pairs (s1, x1), (s2, x2) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before MaximumMagnitude is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to MaximumMagnitude on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledMaximumMagnitude"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledMaximumMagnitudeNumber","page":"Blocks and scaled operations","text":"ScaledMaximumMagnitudeNumber(fr, ρ, s1, x1, s2, x2; rng) -> BinaryValue\n\nMaximumMagnitudeNumber over 2 scale/value pairs (s1, x1), (s2, x2) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before MaximumMagnitudeNumber is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to MaximumMagnitudeNumber on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledMaximumMagnitudeNumber"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledMaximumNumber","page":"Blocks and scaled operations","text":"ScaledMaximumNumber(fr, ρ, s1, x1, s2, x2; rng) -> BinaryValue\n\nMaximumNumber over 2 scale/value pairs (s1, x1), (s2, x2) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before MaximumNumber is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to MaximumNumber on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledMaximumNumber"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledMinimum","page":"Blocks and scaled operations","text":"ScaledMinimum(fr, ρ, s1, x1, s2, x2; rng) -> BinaryValue\n\nMinimum over 2 scale/value pairs (s1, x1), (s2, x2) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before Minimum is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to Minimum on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledMinimum"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledMinimumFinite","page":"Blocks and scaled operations","text":"ScaledMinimumFinite(fr, ρ, s1, x1, s2, x2; rng) -> BinaryValue\n\nMinimumFinite over 2 scale/value pairs (s1, x1), (s2, x2) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before MinimumFinite is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to MinimumFinite on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledMinimumFinite"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledMinimumMagnitude","page":"Blocks and scaled operations","text":"ScaledMinimumMagnitude(fr, ρ, s1, x1, s2, x2; rng) -> BinaryValue\n\nMinimumMagnitude over 2 scale/value pairs (s1, x1), (s2, x2) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before MinimumMagnitude is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to MinimumMagnitude on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledMinimumMagnitude"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledMinimumMagnitudeNumber","page":"Blocks and scaled operations","text":"ScaledMinimumMagnitudeNumber(fr, ρ, s1, x1, s2, x2; rng) -> BinaryValue\n\nMinimumMagnitudeNumber over 2 scale/value pairs (s1, x1), (s2, x2) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before MinimumMagnitudeNumber is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to MinimumMagnitudeNumber on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledMinimumMagnitudeNumber"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledMinimumNumber","page":"Blocks and scaled operations","text":"ScaledMinimumNumber(fr, ρ, s1, x1, s2, x2; rng) -> BinaryValue\n\nMinimumNumber over 2 scale/value pairs (s1, x1), (s2, x2) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before MinimumNumber is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to MinimumNumber on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledMinimumNumber"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledMultiply","page":"Blocks and scaled operations","text":"ScaledMultiply(fr, ρ, s1, x1, s2, x2; rng) -> BinaryValue\n\nMultiply over 2 scale/value pairs (s1, x1), (s2, x2) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before Multiply is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to Multiply on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledMultiply"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledNegate","page":"Blocks and scaled operations","text":"ScaledNegate(fr, ρ, s1, x1; rng) -> BinaryValue\n\nNegate over 1 scale/value pair (s1, x1) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before Negate is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to Negate on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledNegate"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledRSqrt","page":"Blocks and scaled operations","text":"ScaledRSqrt(fr, ρ, s1, x1; rng) -> BinaryValue\n\nRSqrt over 1 scale/value pair (s1, x1) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before RSqrt is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to RSqrt on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledRSqrt"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledRecip","page":"Blocks and scaled operations","text":"ScaledRecip(fr, ρ, s1, x1; rng) -> BinaryValue\n\nRecip over 1 scale/value pair (s1, x1) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before Recip is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to Recip on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledRecip"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledSin","page":"Blocks and scaled operations","text":"ScaledSin(fr, ρ, s1, x1; rng) -> BinaryValue\n\nSin over 1 scale/value pair (s1, x1) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before Sin is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to Sin on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledSin"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledSinPi","page":"Blocks and scaled operations","text":"ScaledSinPi(fr, ρ, s1, x1; rng) -> BinaryValue\n\nSinPi over 1 scale/value pair (s1, x1) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before SinPi is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to SinPi on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledSinPi"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledSinh","page":"Blocks and scaled operations","text":"ScaledSinh(fr, ρ, s1, x1; rng) -> BinaryValue\n\nSinh over 1 scale/value pair (s1, x1) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before Sinh is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to Sinh on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledSinh"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledSoftplus","page":"Blocks and scaled operations","text":"ScaledSoftplus(fr, ρ, s1, x1; rng) -> BinaryValue\n\nSoftplus over 1 scale/value pair (s1, x1) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before Softplus is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to Softplus on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledSoftplus"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledSqrt","page":"Blocks and scaled operations","text":"ScaledSqrt(fr, ρ, s1, x1; rng) -> BinaryValue\n\nSqrt over 1 scale/value pair (s1, x1) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before Sqrt is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to Sqrt on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledSqrt"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledSubtract","page":"Blocks and scaled operations","text":"ScaledSubtract(fr, ρ, s1, x1, s2, x2; rng) -> BinaryValue\n\nSubtract over 2 scale/value pairs (s1, x1), (s2, x2) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before Subtract is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to Subtract on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledSubtract"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledTan","page":"Blocks and scaled operations","text":"ScaledTan(fr, ρ, s1, x1; rng) -> BinaryValue\n\nTan over 1 scale/value pair (s1, x1) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before Tan is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to Tan on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledTan"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledTanPi","page":"Blocks and scaled operations","text":"ScaledTanPi(fr, ρ, s1, x1; rng) -> BinaryValue\n\nTanPi over 1 scale/value pair (s1, x1) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before TanPi is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to TanPi on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledTanPi"},{"category":"function","location":"95-reference/30-blocks.html#AIFloats.ScaledTanh","page":"Blocks and scaled operations","text":"ScaledTanh(fr, ρ, s1, x1; rng) -> BinaryValue\n\nTanh over 1 scale/value pair (s1, x1) — the draft §5.8 scaled form, and one of the specializations §4.5 requires for Fs = {Binary8p1uf}.\n\nEach si * xi product is formed exactly on the common carrier before Tanh is applied, so no intermediate is projected. The single result is projected once into fr under ρ.\n\nWhen every si is one this reduces to Tanh on the values alone.\n\n\n\n\n\n","title":"AIFloats.ScaledTanh"},{"category":"section","location":"70-examples/30-advanced.html#examples-advanced","page":"Advanced examples","text":"","title":"Advanced examples"},{"category":"section","location":"70-examples/30-advanced.html#Pack-code-points-densely","page":"Advanced examples","text":"PackedVector stores each code point in exactly the format's K bits. Packing is useful when K is smaller than the ordinary UInt8 or UInt16 storage unit.\n\nusing AIFloats\n\nT = BinaryValue(Binary5p2se)\nA = T[0.0, 0.5, 1.0, 1.5, 2.0, 3.0]\nP = PackedVector(A)\n\n(packing_saves(T), length(P), Base.summarysize(P), collect(P) == A)\n(true, 6, 40, true)\n\nPacked unary operations return an ordinary vector:\n\nusing AIFloats\n\nT = BinaryValue(Binary5p2se)\nA = T[0.0, 0.5, 1.0, 1.5, 2.0, 3.0]\nP = PackedVector(A)\nF = Binary5p2se\nnegated = vmap(:Negate, F, RTE_SN, P)\n(negated, typeof(negated))\n(BinaryValue(Binary5p2se)[0.0, -0.5, -1.0, -1.5, -2.0, -3.0], Vector{BinaryValue(Binary5p2se)})\n\nUse packed storage to reduce memory traffic or retained size, not automatically for compute speed; bit extraction has a cost.","title":"Pack code points densely"},{"category":"section","location":"70-examples/30-advanced.html#Serialize-packed-storage-portably","page":"Advanced examples","text":"packedwords/packedbytes and their inverses are the wire forms. Both are logical — defined on the bit stream, not on this host's byte order — so what one machine writes another reads. The byte form is the minimal cld(n*K, 8) bytes and is the shorter of the two whenever the payload does not fill its last 64-bit word:\n\nusing AIFloats\n\nT = BinaryValue(Binary5p2se)\nA = T[0.0, 0.5, 1.0, 1.5, 2.0, 3.0]\nP = PackedVector(A)\n\nbytes = AIFloats.packedbytes(P)\nback = AIFloats.packedfrombytes(T, bytes, length(A))\n\n(length(bytes), 8 * length(AIFloats.packedwords(P)), collect(back) == A)\n(4, 8, true)\n\nBoth readers validate rather than guess. A reader cannot tell a corrupt stream from a differently-conventioned writer, so it refuses:\n\nusing AIFloats\n\nT = BinaryValue(Binary5p2se)\nA = T[0.0, 0.5, 1.0, 1.5, 2.0, 3.0]\nbytes = AIFloats.packedbytes(PackedVector(A))\n\nwrong_length = try\n    AIFloats.packedfrombytes(T, bytes, length(A) + 50)\ncatch err\n    err\nend\n\ncorrupt_padding = copy(bytes)\ncorrupt_padding[end] |= 0x80          # set a bit in the unused high padding\nnonzero_padding = try\n    AIFloats.packedfrombytes(T, corrupt_padding, length(A))\ncatch err\n    err\nend\n\n(wrong_length, nonzero_padding)\n(DimensionMismatch(\"packed wire form for 56 Binary5p2se values is 35 byte(s), got 4\"), ArgumentError(\"packed wire form has nonzero padding in the unused high bits of its final byte; the canonical form zeroes them\"))","title":"Serialize packed storage portably"},{"category":"section","location":"70-examples/30-advanced.html#Copy-packed-and-block-storage","page":"Advanced examples","text":"copy, similar, and an exact-type copyto! are defined for both containers. similar returns the same concrete container type — packed storage stays packed, and a BlockVector keeps its block size and both formats in its type:\n\nusing AIFloats\n\nT = BinaryValue(Binary5p2se)\nA = T[0.0, 0.5, 1.0, 1.5, 2.0, 3.0]\nP = PackedVector(A)\n\nQ = similar(P)\ncopyto!(Q, P)\n\n(typeof(Q), typeof(Q) === typeof(P), collect(Q) == A, copy(P) !== P)\n(PackedVector{BinaryValue(Binary5p2se)}, true, true, true)\n\nusing AIFloats\n\nS = Binary8p1uf\nE = Binary5p2se\nbx = Block(S(1.0), (E(1.5), E(0.25), E(-0.5), E(2.0)))\nbv = BlockVector([bx, bx])\n\nbw = similar(bv)\ncopyto!(bw, bv)\n\n(typeof(bw) === typeof(bv), size(bw), bw[1] == bx)\n(true, (2,), true)\n\ncopyto! requires the exact element type. A BlockVector's type carries its block size and both its formats, so a copy between differently shaped containers cannot type-check rather than silently reshaping.","title":"Copy packed and block storage"},{"category":"section","location":"70-examples/30-advanced.html#Build-shared-scale-blocks","page":"Advanced examples","text":"A Block combines one scale datum with a fixed tuple of element datums.\n\nusing AIFloats\n\nS = Binary8p1uf   # 8-bit scale, 1 bit of precision\nE = Binary5p2se   # 5-bit elements, 2 bits of precision\n\nbx = Block(S(1.0), (E(1.5), E(0.25), E(-0.5), E(2.0)))\nby = Block(S(1.0), (E(0.5), E(0.75), E(1.0), E(-1.0)))\n\n(blocksize(bx), scaleformat(bx), elemformat(bx))\n(4, Binary{8, 1, UNSIGNED, FINITE}, Binary{5, 2, SIGNED, EXTENDED})\n\nEvery registered operation has a generated Block* form. The last argument is the result scale:\n\nusing AIFloats\n\nS = Binary8p1uf\nE = Binary5p2se\nbx = Block(S(1.0), (E(1.5), E(0.25), E(-0.5), E(2.0)))\nby = Block(S(1.0), (E(0.5), E(0.75), E(1.0), E(-1.0)))\nbz = BlockAdd(E, RTE_SN, bx, by, S(1.0))\n(bz, AIFloats.blockdecode(bz))\n(Block(1.0, (2.0, 1.0, 0.5, 1.0)), (2.0, 1.0, 0.5, 1.0))","title":"Build shared-scale blocks"},{"category":"section","location":"70-examples/30-advanced.html#Reduce-a-block","page":"Advanced examples","text":"Block reductions first try an allocation-free exact accumulator. That fast path is used only after checking that the decoded lanes are finite, each required product is represented exactly, and aligning or multiplying the significands will stay within the accumulator's fixed-width limits. If any check fails, the operation restarts from the original block values using BigFloat at a precision derived to hold the complete reduction exactly. Thus these checks affect performance only: both paths form the same exact reduction and project once into the requested result format.\n\nusing AIFloats\n\nS = Binary8p1uf\nE = Binary5p2se\nbx = Block(S(1.0), (E(1.5), E(0.25), E(-0.5), E(2.0)))\nby = Block(S(1.0), (E(0.5), E(0.75), E(1.0), E(-1.0)))\n(\n    sum = BlockReduceAdd(E, RTE_SN, bx),\n    product = BlockReduceMultiply(E, RTE_SN, bx),\n    dot = BlockDotProduct(E, RTE_SN, bx, by),\n)\n(sum = 3.0, product = -0.375, dot = -1.5)\n\nAll three reductions share the guarded route — including BlockReduceMultiply, which has a checked exact fast path like the others. Its guard simply fails sooner, because accumulating products leaves the exact fixed-point band quickly, so in practice it reaches the BigFloat fallback more often. Which path ran is not observable in the result, and the documentation deliberately does not promise a particular carrier for a particular input — only that both paths form the same exact reduction and project once.","title":"Reduce a block"},{"category":"section","location":"70-examples/30-advanced.html#Blocks-of-different-widths-and-precisions","page":"Advanced examples","text":"The scale and element formats are independent, and neither has to match the result format:\n\nusing AIFloats\n\nS = Binary8p1uf          # 8-bit scale, precision 1 — the scale format the\n                         # Interim Report requires at its §4.5\nE = Binary4p2sf          # 4-bit elements, precision 2\nR = Binary8p4se          # a wider, more precise result format\n\nb = Block(S(2.0), (E(1.5), E(0.5), E(-1.0), E(0.25)))\n\n(\n    scale = scaleformat(b),\n    elements = elemformat(b),\n    lanes = AIFloats.blockdecode(b),\n    reduced_into_R = BlockReduceAdd(R, RTE_SN, b),\n    reduced_into_E = BlockReduceAdd(E, RTE_SN, b),\n)\n(scale = Binary{8, 1, UNSIGNED, FINITE}, elements = Binary{4, 2, SIGNED, FINITE}, lanes = (3.0, 1.0, -2.0, 0.5), reduced_into_R = 2.5, reduced_into_E = 2.0)","title":"Blocks of different widths and precisions"},{"category":"section","location":"70-examples/30-advanced.html#Convert-values-to-and-from-block-form","page":"Advanced examples","text":"using AIFloats\n\nS = Binary8p1uf\nE = Binary5p2se\nvalues = (E(0.5), E(1.0), E(1.5), E(2.0))\nblock = ConvertToBlock(E, RTE_SN, values, S(1.0))\nroundtrip = ConvertFromBlock(E, RTE_SN, block)\n(block, roundtrip)\n(Block(1.0, (0.5, 1.0, 1.5, 2.0)), (0.5, 1.0, 1.5, 2.0))\n\nFor collections of equally shaped blocks, BlockVector uses a structure-of-arrays layout:\n\nusing AIFloats\n\nS = Binary8p1uf\nE = Binary5p2se\nbx = Block(S(1.0), (E(1.5), E(0.25), E(-0.5), E(2.0)))\nby = Block(S(1.0), (E(0.5), E(0.75), E(1.0), E(-1.0)))\nbz = BlockAdd(E, RTE_SN, bx, by, S(1.0))\nblocks = [bx, by, bz]\nstorage = BlockVector(blocks)\n(size(storage), storage[2] == by)\n((3,), true)\n\nContinue with Technical examples to inspect policy and correctness machinery.","title":"Convert values to and from block form"},{"category":"section","location":"96-algorithms.html#algorithms","page":"Algorithms","text":"This page describes how AIFloats obtains the results the other pages state as contracts. Nothing here is an interface: the algorithms may be replaced by better ones without notice, and code should depend on the documented behaviour rather than on the mechanism producing it.\n\nWhat is not negotiable is the specification each algorithm implements. The normative source is the designated IEEE P3109 Interim Report, cited throughout as [P3109] and listed with the other references at the foot of this page. Section numbers always belong to that document. References to the wider literature are background and motivation — they explain why an approach is the usual one, and they never establish a guarantee this package makes.\n\nThree algorithms earn a place here because a reader's trust or decisions depend on them: how a stochastic mode turns N random bits into a rounding decision, how a transcendental result is proved correctly rounded rather than merely computed carefully, and how predicates written over exact real numbers are evaluated on carriers that cannot hold them.\n\n","title":"Algorithms"},{"category":"section","location":"96-algorithms.html#alg-stochastic","page":"Algorithms","text":"","title":"Stochastic rounding"},{"category":"section","location":"96-algorithms.html#Common-aspects","page":"Algorithms","text":"A stochastic mode rounds the same value up sometimes and down other times, so that the expected result tracks the exact value instead of a fixed neighbour. It needs a source of randomness, and [P3109] §4.7.4 fixes its form: an unsigned integer R of N bits, 0 ≤ R < 2^N.\n\nEverything else follows from one quantity. After truncating the exact significand onto the target grid, some fraction of an ulp is left over; call it ν, with 0 ≤ ν < 1. ν = 0 means the value is exactly representable; ν = ¾ means the true value sits three quarters of the way toward the next datum. A rounding mode is then a single predicate: round away from zero, or not?\n\nFor a deterministic mode that predicate reads ν alone — RTZ never rounds away, RTA rounds away when ν ≥ ½. A stochastic mode reads ν and R. And since R is uniform on 0:2^N-1, the probability of rounding away is just the count of R values satisfying the predicate, divided by 2^N:\n\nP(textaway) = fracR  mathrmRoundAway(ν R)2^N\n\nThat is the useful way to read the three variants — StochasticA, StochasticB and StochasticC, abbreviated RSA, RSB and RSC. (Unlike the deterministic modes, these long names are the report's own; see Report names and package names.)\n\nIdeally P(away) = ν exactly — then the expected result is the exact value and rounding is unbiased. But R takes only 2^N values, so P(away) can only be a multiple of 2^-N. Each variant is a different way of snapping ν onto that grid of 2^N steps, and that is the whole of the difference between them.","title":"Common aspects"},{"category":"section","location":"96-algorithms.html#The-three-predicates","page":"Algorithms","text":"[P3109] §4.7.4 defines them directly. Counting the satisfying R gives the probability in the third column:\n\nMode RoundAway iff P(away) ν is snapped by\nRSA{N} ⌊ν·2^N⌋ + R ≥ 2^N ⌊ν·2^N⌋ / 2^N truncation\nRSB{N} ⌊ν·2^(N+1)⌋ + (2R + 1) ≥ 2^(N+1) ⌊ν·2^N + ½⌋ / 2^N nearest, ties up\nRSC{N} RNITE(ν·2^N) + R ≥ 2^N RNITE(ν·2^N) / 2^N nearest, ties to even\n\nRNITE is the round-to-nearest-ties-even that [P3109] §4.7.4 defines alongside these predicates.\n\nRSA is the direct construction: compare the N-bit truncation of ν against a uniform draw. Simple, and one shift, one floor, one compare.\n\nRSB looks unlike the others until the counting is done. Evaluating at doubled resolution and adding 2R + 1 places the comparison a half-step off the grid, and a half-step offset is exactly what turns truncation into round-to-nearest. It buys nearest-quantization without a second random bit — R is still N bits wide. That is the trick the variant exists for.\n\nRSC reaches nearest-quantization the direct way, by actually round-to-nearest-even-ing the scaled fraction before the comparison. Same accuracy as RSB, different tie-break, more work.","title":"The three predicates"},{"category":"section","location":"96-algorithms.html#Where-they-differ,-concretely","page":"Algorithms","text":"RSB and RSC can only disagree when ν·2^N lands exactly halfway between two grid points. Everywhere else they are identical; RSA differs almost everywhere.\n\nN ν RSA RSB RSC \n1 0.25 0 ½ 0 tie at 0.5: RSB up, RSC to even (down)\n1 0.75 ½ 1 1 tie at 1.5: both go to 2\n2 0.125 0 ¼ 0 tie at 0.5 again\n3 0.375 ⅜ ⅜ ⅜ on the grid: all three agree\n3 0.8 ¾ ¾ ¾ off the grid, no tie: RSB, RSC agree\n\nThe last row is the one to remember. ν = 0.8 is not representable on the 3-bit grid, so every variant rounds away with probability ¾, not 0.8.\n\nwarning: No variant is exactly unbiased at finite `N`\nStochastic rounding is unbiased when P(away) = ν. At a finite budget that equality holds only for the ν that land on the grid. The residual bias is P(away) − ν, bounded by:Mode bias range worst case\nRSA (−2^-N, 0] — never positive 2^-N\nRSB [−2^-(N+1), +2^-(N+1)] 2^-(N+1)\nRSC [−2^-(N+1), +2^-(N+1)] 2^-(N+1)RSA's bias is systematic: it is one-signed, always toward zero, so it accumulates across many roundings rather than cancelling. RSB and RSC are centred, so their errors tend to cancel — except at exact ties, where RSB always goes up and RSC alternates by parity.So RSC is the most nearly unbiased of the three, and what bias it does carry is directionally non-deterministic: with no preferred direction, it does not accumulate. In a natural sense RSC is the fairest of the three. RSA is the fastest. RSB sits between them — centred like RSC, but with RSA's one-signed behaviour where it matters most, at the ties.How much that separation is worth depends on how often ties occur, which is a property of N and the format rather than of the mode. How often ties actually occur measures it.AIFloats claims no bias figure beyond these bounds.","title":"Where they differ, concretely"},{"category":"section","location":"96-algorithms.html#alg-ties","page":"Algorithms","text":"RSB and RSC differ only at ties, so the question decides whether the choice between them matters at all. It has a sharp answer, and it is not the intuitive one.\n\nA tie is ν·2^N landing exactly halfway between two grid points, which requires ν to have exactly N+1 fractional bits. So ties are not primarily a property of the data. They are a property of how many fractional bits ν tends to carry, against N. Two things supply those bits:\n\nPrecision. The exact product of two P-bit significands has at most 2P bits, leaving at most about P fractional bits in ν. More precision, longer ν, more ties — up to the point where N outruns the supply.\nExponent spread. Adding values many binades apart, or landing in the subnormal region where the exponent is clamped, gives ν bits in proportion to the exponent range rather than to P. This is what produces a small tie fraction that persists at every N.\n\nThe tables below are exact counts, not samples: every ordered pair — every ordered triple, for FMA — of finite operands, keeping the cases whose exact result is nonzero and inexact. Entries are the fraction of those results that tie; the final column is how many there were.\n\nEach table pools both domains, FINITE and EXTENDED, because the choice barely moves the numbers: domain decides which code points exist at the top of the range, not the arithmetic that produces ν. Format labels therefore carry the report's own metavariable for the domain, δ ([P3109] §3.2), where an e or an f would otherwise go. 0 means exactly zero, and <0.01 a nonzero fraction below one in two hundred — a distinction worth keeping, because where ties stop entirely is the useful boundary.\n\nOnly the signed formats are tabulated. Unsigned formats need no table of their own, for the reason stated immediately below the last one.\n\nAdd and Multiply are tabulated over K = 3…8, FMA over K = 3…6; P runs 2, 4, 6 capped at K-1.","title":"How often ties actually occur"},{"category":"section","location":"96-algorithms.html#Add","page":"Algorithms","text":"Format N=1 2 3 4 5 6 7 8 inexact\nBinary3p2sδ 0 0 0 0 0 0 0 0 4\nBinary4p2sδ 0.32 0 0 0 0 0 0 0 136\nBinary5p2sδ 0.29 0.19 0.14 0.08 0.03 0 0 0 1160\nBinary5p4sδ 0 0 0 0 0 0 0 0 196\nBinary6p2sδ 0.14 0.12 0.11 0.10 0.09 0.08 0.07 0.06 6280\nBinary6p4sδ 0.38 0.02 0 0 0 0 0 0 3016\nBinary7p2sδ 0.07 0.06 0.06 0.06 0.05 0.05 0.05 0.05 28808\nBinary7p4sδ 0.26 0.20 0.15 0.09 0.04 0 0 0 20936\nBinary7p6sδ 0 0 0 0 0 0 0 0 3844\nBinary8p2sδ 0.04 0.03 0.03 0.03 0.03 0.03 0.03 0.03 123016\nBinary8p4sδ 0.13 0.12 0.11 0.10 0.09 0.08 0.07 0.06 105928\nBinary8p6sδ 0.38 0.03 0 0 0 0 0 0 51976","title":"Add"},{"category":"section","location":"96-algorithms.html#Multiply","page":"Algorithms","text":"Format N=1 2 3 4 5 6 7 8 inexact\nBinary3p2sδ 0.20 0 0 0 0 0 0 0 20\nBinary4p2sδ 0.61 0 0 0 0 0 0 0 124\nBinary5p2sδ 0.67 0.11 0.06 0 0 0 0 0 580\nBinary5p4sδ 0.36 0.35 0.04 0 0 0 0 0 1316\nBinary6p2sδ 0.66 0.08 0.06 0.05 0.04 0.03 0.01 0 2548\nBinary6p4sδ 0.32 0.32 0.19 0 0 0 0 0 5644\nBinary7p2sδ 0.65 0.04 0.04 0.04 0.03 0.03 0.03 0.02 10708\nBinary7p4sδ 0.30 0.31 0.20 0.04 0.02 0 0 0 23276\nBinary7p6sδ 0.13 0.20 0.27 0.28 0.04 0 0 0 28852\nBinary8p2sδ 0.64 0.02 0.02 0.02 0.02 0.02 0.02 0.02 43924\nBinary8p4sδ 0.28 0.30 0.20 0.03 0.03 0.02 0.02 0.01 95436\nBinary8p6sδ 0.12 0.17 0.24 0.25 0.15 0 0 0 117612","title":"Multiply"},{"category":"section","location":"96-algorithms.html#FMA","page":"Algorithms","text":"Format N=1 2 3 4 5 6 7 8 inexact\nBinary3p2sδ 0.17 0 0 0 0 0 0 0 144\nBinary4p2sδ 0.39 0.13 0.03 0 0 0 0 0 2864\nBinary5p2sδ 0.24 0.18 0.15 0.10 0.07 0.04 0.02 0.01 39372\nBinary5p4sδ 0.34 0.34 0.07 <0.01 0 0 0 0 40440\nBinary6p2sδ 0.11 0.10 0.10 0.09 0.08 0.08 0.07 0.06 410236\nBinary6p4sδ 0.25 0.27 0.20 0.08 0.02 <0.01 0 0 381324\n\n<p style=\"font-size: calc(1em + 2pt);\">\nThe practical reading: <strong>an unsigned format ties like a signed one a bit\nwider.</strong> Choosing between <code>RSB</code> and <code>RSC</code>, read the\nunsigned row at <code>K</code> as the signed row at <code>K&nbsp;+&nbsp;1</code>.\n</p>\n\nDropping the sign bit hands it to the exponent, so BinaryKpPu… and Binary(K+1)pPs… carry the same precision and the same exponent bias — the same magnitude lattice. Measured over the unsigned formats separately, their rows track the signed rows one K up throughout; for Multiply, which is sign-symmetric, the correspondence between BinaryKpPuf and Binary(K+1)pPse is not merely close but exact, verified on 5p4/6p4, 6p4/7p4, 7p4/8p4, 6p2/7p2 and 7p2/8p2. Add only tracks, rather than matching, because a signed Add also admits cancelling pairs an unsigned format cannot form.","title":"FMA"},{"category":"section","location":"96-algorithms.html#What-the-tables-say","page":"Algorithms","text":"N dominates, and raising it is the reliable lever. Almost every row falls as N grows, and many reach exactly zero. At the package default N = 8 a good number of these format/operation pairs tie on no input at all, and the RSB/RSC choice is then a choice between identical answers.\n\nNarrower is not tie-ier. At N = 4, signed Multiply ties rise from 0 on Binary5p2sδ to 0.25 on Binary8p6sδ — more precision, more ties, because a longer ν is what a tie needs. A very narrow format produces a ν so coarse it lands on grid points, where all three variants agree.\n\nFor Multiply, ties die once N reaches about P. Binary8p6sδ is the clean case: substantial through N = 5, exactly zero from N = 6. The product of two P-bit significands simply runs out of fractional bits for a tie to occupy.\n\nA wide exponent range keeps a floor under them. The P = 2 rows are the exception that proves the mechanism: Binary8p2sδ has one stored significand bit and an exponent bias of 32, so alignment and subnormals feed ν bits that precision never could. It still ties on 3% of inexact Add results at N = 8, where the better-balanced Binary8p6sδ ties on none.\n\nFMA ties the most, and for the longest. This is the finding that matters most in practice, because FMA is not a rare operation. Its exact result x·y + z carries a product's bits and an alignment against z, so ν is longer than for either parent operation — and ties survive well past the point where both give up. Compare Binary6p4sδ across the three:\n\nBinary6p4sδ N=1 2 3 4 5 6 7 8\nAdd 0.38 0.02 0 0 0 0 0 0\nMultiply 0.32 0.32 0.19 0 0 0 0 0\nFMA 0.25 0.27 0.20 0.08 0.02 <0.01 0 0\n\nAdd is finished by N = 3 and Multiply by N = 4; FMA is still tying at N = 5. The gap is wider still on unsigned formats: measured, Binary6p4uδ FMA ties on 2% of inexact results at N = 8, where its own Add and Multiply have both been zero since N = 6.\n\nnote: Scope of the measurement\nAdd, Multiply and FMA, over every ordered pair or triple of finite operands. Enclosure-evaluated operations (Exp, Log, …) are not tabulated: their exact results are irrational most of the time, so ν has no finite binary expansion and an exact sub-grid tie essentially never arises.That omission does not weaken the guidance. Add, Multiply and FMA are the operations training and inference perform most, so what is tabulated is what dominates a real workload — and FMA, the one that ties most stubbornly, is among them. The elementary functions both tie less often and are called less often. The figures above are reasonable guidance for choosing between RSB and RSC, not a bound to be discounted.","title":"What the tables say"},{"category":"section","location":"96-algorithms.html#Choosing-a-variant","page":"Algorithms","text":"In one line: RSC is the fairest, RSA is the fastest, RSB sits between them. Start from what your budget is really constrained by.\n\nRSB is the reasonable default. It gets centred, nearest-quantization at RSA's bit cost and close to RSA's arithmetic cost. If nothing in your problem argues for another, use it.\n\nChoose RSC when ties are actually frequent — which, as the next section measures, means a small N, or a format whose exponent range is wide relative to its precision. RSB pushes every tie upward; RSC alternates by parity so they cancel. Where ties are rare the two are indistinguishable, and RSC is the most expensive of the three, so this is a choice to make from the table rather than from intuition.\n\nChoose RSA when the random bits or the gate count are the binding constraint, and a known one-signed bias of at most 2^-N is acceptable — or when you are reproducing a result from the literature, where \"stochastic rounding with N bits\" almost always means this construction. Its one-signed bias is a real hazard in long accumulations; compensate by raising N rather than by hoping.\n\nChoosing N matters more than choosing the variant. The grid spacing 2^-N is the bias floor, and no variant escapes it. If the quantity you are trying to preserve — a weight update, a residual — is around 2^-k of an ulp, then N must be comfortably above k or the update is invisible to every variant. The package default is N = 8; the range is 1:60.\n\njulia> nrandbits(RSA_SN), isstochastic(RSA_SN), isstochastic(RTE_SN)\n(8, true, false)\n\njulia> AIFloats.ρRSB{16}()          # a wider budget\nRSB\n\nIf you need determinism, do not reach for a pure mode by default. A stochastic projection is reproducible when you supply R outright, or a seeded rng — see the stochastic contract. Reproducibility and randomness are not in conflict here.","title":"Choosing a variant"},{"category":"section","location":"96-algorithms.html#Citations","page":"Algorithms","text":"The three variants and the trade they represent are from [Fitzgibbon2025], which [P3109] cites at its §4.7.4 NOTE 2 for exactly this point — the paper's subject is stochastic rounding on a small bit budget, which is why the RSB half-step construction exists at all. [Croci2022] is the standard survey of stochastic rounding, its error analysis and its applications. [Gupta2015] is the result usually credited with establishing stochastic rounding for low-precision training. [Higham2002] is the general reference for accumulated rounding-error analysis.\n\nThe training motivation — that an update smaller than one ulp is discarded by every deterministic mode but can accumulate under a random one — comes from that literature. It is not a guarantee this package makes.\n\n","title":"Citations"},{"category":"section","location":"96-algorithms.html#alg-enclosure","page":"Algorithms","text":"","title":"Interval enclosure"},{"category":"section","location":"96-algorithms.html#The-problem:-you-cannot-decide-by-computing-harder","page":"Algorithms","text":"Ask for Exp(x) in a format with a 4-bit significand. The exact e^x is irrational; the answer must be one of two neighbouring datums; and choosing between them means knowing which side of the midpoint the true value falls on.\n\nComputing e^x in double precision does not settle that. If the true value lies close enough to the midpoint, a Float64 approximation lands on the midpoint too — and the approximation error, however small, is larger than the distance that decides the answer. Increasing the working precision helps until it doesn't: for any fixed precision there are inputs whose true results sit closer to a rounding boundary than that precision can resolve. This is the Table Maker's Dilemma [Muller2018].\n\nThe consequence is sharp, and it is why this section exists: no approximate evaluation, at any fixed precision, can be trusted to produce a correctly rounded result. A faster elementary function does not fix it. A wider carrier does not fix it. What fixes it is not computing a better value — it is computing a value you can prove is good enough, and knowing when you have not.","title":"The problem: you cannot decide by computing harder"},{"category":"section","location":"96-algorithms.html#The-strategy:-bracket,-decide,-refine","page":"Algorithms","text":"The standard answer is Ziv's strategy [Ziv1991]: evaluate at some working precision, obtain a bracket that provably contains the true value, and check whether the bracket is narrow enough to settle the question. If it is, you are done and you have a proof. If it is not, raise the precision and try again.\n\nAIFloats applies that with one adaptation that makes it cheap: the question is settled in the target format's code-point space, not in the carrier. The bracket does not have to be narrow in absolute terms. It only has to be narrow enough that both of its endpoints project to the same code point.\n\nprec ← starting precision\nloop\n    (lo, hi) ← evaluate f, giving lo ≤ truth ≤ hi     # directed MPFR endpoints\n    if lo == hi\n        return project(lo)                            # the result was exact\n    c_lo ← project(lo, sticky = +1)                   # truth is above lo\n    c_hi ← project(hi, sticky = −1)                   # truth is below hi\n    if c_lo == c_hi\n        return c_lo                                   # PROVED: any value in\n                                                      # [lo, hi] rounds here\n    if prec ≥ cap: error                              # never a wrong answer\n    prec ← 2 · prec\n\nThe middle step is the whole argument. Projection is monotone, and the true value lies somewhere in [lo, hi]. So if the two endpoints project to one code point, every value in the interval projects to that code point — including the true one. The result is correct whatever the true value is, and no further knowledge of it is needed.\n\nTwo properties of that argument are worth drawing out:\n\nIt holds for every rounding mode, including the stochastic ones at a fixed R, because it never assumes anything about projection beyond monotonicity. One loop serves all 27 projections.\nIt cannot silently fail. The alternative to agreement is another rung, and the alternative to the cap is an error. There is no branch that returns an unproved answer.\n\nThe endpoints are projected with a sticky direction — +1 on the lower endpoint because the truth is strictly above it, −1 on the upper because the truth is strictly below. Without that, an endpoint landing exactly on a rounding boundary would be judged as a tie, which it is not. That mechanism is the sticky protocol below, and this is one of the two places it is essential.","title":"The strategy: bracket, decide, refine"},{"category":"section","location":"96-algorithms.html#What-AIFloats-does-specifically","page":"Algorithms","text":"Where the ladder starts. At the operands' own width plus a margin — 72 bits for Float64 operands, 136 for Float128 — rather than at a precision chosen to resolve anything. Correctness does not depend on the starting point at all: the loop escalates regardless. Only the number of MPFR calls does. Measured over a sweep of every code point of ten enclosure-evaluated operations across three formats, 97.6% of enclosures resolve on the first rung, so a high start is waste on almost everything and the 2.4% tail pays one extra doubling.\n\nHow the bracket is obtained. MPFR computes each of its primitives with correct rounding [Fousse2007], so a single round-to-nearest evaluation r satisfies |truth − r| ≤ ½ulp(r), and [prevfloat(r), nextfloat(r)] therefore strictly contains the true value. One MPFR call plus two cheap steps replaces two directed evaluations and two rounding-mode round-trips. The enclosure is 2 ulps wide instead of 1 — nothing against a target grid of at most 16 bits.\n\nwarning: That shortcut has a precondition\nIt is sound only when the evaluated function is one MPFR primitive. A composite closure rounds more than once, its error can exceed ½ulp, and the 2-ulp bracket would no longer be a bracket. Composite rows use two directed evaluations instead. The precondition is not checkable at the call site, so it is a discipline in ops/oracle.jl, not an assertion.\n\nThe fast stages are not shortcuts around the proof. Before the ladder runs, an eager Float64 estimate may be tried — but it is accepted only when the two-sided sticky projection of its error envelope agrees on one code point, which is the same test the ladder uses. If it does not agree, the ladder runs. AIFloats.FAST_ENCLOSURE switches the stage off, and the test suite pins both paths to identical results.\n\nwarning: `Float128` is a carrier, not an oracle\nlibquadmath's elementary functions are not assumed to be correctly rounded, and no Quadmath transcendental result is ever accepted as an answer on its own. Float128 is used to hold values exactly; deciding a code point is the ladder's job. The pure-Julia AIFloats.fma128 and AIFloats.faa128 carry their own documented guarantees and are a separate matter.\n\nWhich operations use it. The registry records a group per operation (see Operations). Group A and C operations — Add, Multiply, FMA, the extremum family — have exact results reachable without a ladder, so they never enter it. Group B — Exp, Log, Sqrt, Divide, the trigonometric, hyperbolic and π-scaled families, Hypot, Softplus — is what this section is for.\n\nThe quotients (Divide, Recip, Sqrt, RSqrt) sit between the two. They try an exactness proof first: for a computed quotient q, a fused multiply-add giving fma(q, y, -x) == 0 establishes that q·y is exactly x, so q is the exact quotient and no rounding decision is in doubt. Many quotients of small-format datums are exact, and those cost one proof. The ladder runs only when the proof fails.","title":"What AIFloats does specifically"},{"category":"section","location":"96-algorithms.html#Citations-2","page":"Algorithms","text":"[Ziv1991] introduced the evaluate-and-refine strategy for correctly rounded elementary functions. [Muller2018] is the standard reference for the Table Maker's Dilemma and for correctly rounded function evaluation generally. [Fousse2007] describes MPFR, whose correctly rounded primitives are what make the 2-ulp bracket sound.\n\n","title":"Citations"},{"category":"section","location":"96-algorithms.html#alg-sticky","page":"Algorithms","text":"","title":"The sticky protocol"},{"category":"section","location":"96-algorithms.html#The-problem:-the-specification-is-written-over-exact-reals","page":"Algorithms","text":"Every rounding predicate in [P3109] §4.7.4 is a statement about the exact leftover fraction ν. Read them literally:\n\nRoundAway(TowardPositive) = ν > 0 and X > 0\nRoundAway(NearestTiesToEven) = ν > 0.5 or (ν = 0.5 and not CodeIsEven)\nRoundAway(StochasticA) = ⌊ν·2^N⌋ + R ≥ 2^N\n\nThese are exact-arithmetic conditions. ν > 0 distinguishes \"exactly representable\" from \"inexact by any amount whatsoever\". ν = 0.5 means exactly one half, not approximately.\n\nAn implementation computes ν on a carrier — Float64, Float128, a fixed-point Dyadic. That carrier is finite, and ν frequently is not representable in it. So the implementation holds some ν̃, and ν̃ may be a rounded version of a value that was never equal to it.\n\nThe failure this causes is not a rounding error. It is a wrong answer to a different question. Suppose the true ν is a hair above ½, and the carrier rounds it to exactly ½. Under RTE the specification says \"greater than a half, round away, unconditionally\". The implementation sees a tie and consults code parity, so half the time it rounds the wrong way — and it does so silently, producing a plausible datum with no indication anything went wrong. The same trap sits under RTO at ν > 0, and under the stochastic modes wherever ν·2^N lands on a sub-grid boundary.\n\nWidening the carrier does not fix this; it only moves which inputs trip it.","title":"The problem: the specification is written over exact reals"},{"category":"section","location":"96-algorithms.html#The-idea:-carry-a-direction,-not-a-better-number","page":"Algorithms","text":"The protocol's move is to stop trying to represent ν exactly, and instead represent it as a pair: a carrier value together with a direction saying where the truth lies relative to that value.\n\nνs = 0    the carrier value is exact — ν̃ IS ν\nνs = +1   the true ν is strictly ABOVE ν̃\nνs = −1   the true ν is strictly BELOW ν̃\n\nRead the pair as the exact quantity ν̃ + νs·ε, where ε is infinitesimal — positive, and smaller than any difference that could matter. Every predicate is then evaluated on that, and each of the three comparisons the specification needs has a two-line definition:\n\nSpecification asks Evaluated as\nν > c ν̃ > c, or ν̃ == c and νs > 0\nν == c ν̃ == c and νs == 0\nν ≥ c ν̃ > c, or ν̃ == c and νs ≥ 0\n\nThat is the entire mechanism. It is not an approximation of the specification — it is the specification, evaluated exactly, on a carrier that cannot hold the number the specification talks about.","title":"The idea: carry a direction, not a better number"},{"category":"section","location":"96-algorithms.html#It-resolves-the-ambiguity-rather-than-guessing-at-it","page":"Algorithms","text":"Take the RTE case above. One carrier value ν̃ = ½, three different exact situations, three different correct answers:\n\nν̃ νs true ν RTE says Why\n0.5 +1 just above ½ round away ν > ½ is satisfied — not a tie at all\n0.5 −1 just below ½ round down ν > ½ fails and ν = ½ fails\n0.5 0 exactly ½ consult CodeIsEven a genuine tie\n\nWithout the direction all three collapse into the third row, and two of them are answered wrongly. With it, each gets the answer [P3109] §4.7.4 specifies.\n\nThe stochastic modes need the same care one level down, on the 2^N sub-grid rather than on the ulp. When the scaled fraction ν̃·2^N lands exactly on a grid point but the truth is strictly below it, the floor must step down — the truth has not reached that grid point. _νfloorscaled does that when νs < 0. Symmetrically, _νrnite treats a sub-grid tie as rounding away when νs > 0, because the truth is past the halfway point even though the carrier value is not.","title":"It resolves the ambiguity rather than guessing at it"},{"category":"section","location":"96-algorithms.html#Where-the-direction-comes-from","page":"Algorithms","text":"Three sources, and every value reaching the rounding core carries one:\n\nExact carriers report νs = 0. A Dyadic value inside its exact band, or a Float64 operand whose decode is exact, knows it is exact and says so.\nThe enclosure ladder supplies +1 for its lower endpoint and −1 for its upper, since the truth is bracketed strictly between them. This is why interval enclosure can decide a code point at all: the endpoints are not values to be rounded, they are bounds, and the sticky direction is what tells the projector so.\nThe rung-3 Add protocol produces an AIFloats.Sticky when a sum's tail falls past the exact accumulator's alignment band. Rather than widen the carrier to keep bits it will not need, it discards the tail and records which way it pointed.","title":"Where the direction comes from"},{"category":"section","location":"96-algorithms.html#Why-a-direction-is-enough","page":"Algorithms","text":"That last case is the one that has to be argued, because it deliberately throws information away. It is sound because of a width inequality: the accumulator's alignment band is 94 bits, and 94 > P + N + 2 for every precision P ≤ 16 and every stochastic budget N ≤ 60 that the package admits.\n\nSo a discarded tail is below the last bit of the target grid, below the finest stochastic sub-grid step 2^-N, and below any rounding threshold in play. It cannot change which side of a boundary the value falls on — it can only say which side, which is exactly what a direction records. Its magnitude is provably irrelevant, so not carrying it costs nothing.","title":"Why a direction is enough"},{"category":"section","location":"96-algorithms.html#The-payoff","page":"Algorithms","text":"The rounding core is a direct transcription of [P3109] §4.7.4. There is no epsilon tolerance anywhere in it, no \"close enough to a tie\" heuristic, and no carrier- dependent behaviour — the same predicates, evaluated the same way, whether the value arrived as a Float64, a Float128, an exact Dyadic, or a bound from an MPFR ladder. That uniformity is what makes the whole projection path testable against an independent reference, which is how it is verified.\n\n","title":"The payoff"},{"category":"section","location":"96-algorithms.html#References","page":"Algorithms","text":"Normative\n\n[P3109] IEEE Working Group P3109, Interim Report on Arithmetic Formats for Machine Learning, version 4.0.3, 1 September 2026. https://github.com/P3109/Public/blob/main/IEEE%20P3109%20Interim%20Report.pdf — the sole normative source; see draft_identity for the PDF digest this release was compared against. An unapproved draft: its cover states it must not be used for conformance or compliance purposes.\n\nCited by the report\n\n[Fitzgibbon2025] A. W. Fitzgibbon and S. Felix, \"On stochastic rounding with few random bits,\" in IEEE 32nd Symposium on Computer Arithmetic (ARITH 2025), El Paso, TX, USA, 4–7 May 2025, pp. 133–140. https://doi.org/10.1109/ARITH64983.2025.00029 — reference [3] of [P3109], cited in its §4.7.4 NOTE 2 for the accuracy/complexity balance among StochasticA, StochasticB and StochasticC.\n[Higham2002] N. J. Higham, Accuracy and Stability of Numerical Algorithms, 2nd ed., SIAM, 2002 — reference [6] of [P3109].\n\nBackground (not normative, and not a guarantee of this package)\n\n[Ziv1991] A. Ziv, \"Fast evaluation of elementary mathematical functions with correctly rounded last bit,\" ACM Transactions on Mathematical Software, vol. 17, no. 3, pp. 410–423, 1991.\n[Muller2018] J.-M. Muller et al., Handbook of Floating-Point Arithmetic, 2nd ed., Birkhäuser, 2018.\n[Fousse2007] L. Fousse, G. Hanrot, V. Lefèvre, P. Pélissier and P. Zimmermann, \"MPFR: A multiple-precision binary floating-point library with correct rounding,\" ACM Transactions on Mathematical Software, vol. 33, no. 2, article 13, 2007.\n[Croci2022] M. Croci, M. Fasi, N. J. Higham, T. Mary and M. Mikaitis, \"Stochastic rounding: implementation, error analysis and applications,\" Royal Society Open Science, vol. 9, no. 3, 211631, 2022.\n[Gupta2015] S. Gupta, A. Agrawal, K. Gopalakrishnan and P. Narayanan, \"Deep learning with limited numerical precision,\" in Proceedings of the 32nd International Conference on Machine Learning (ICML), 2015, pp. 1737–1746.","title":"References"},{"category":"section","location":"99-faa128.html#faa128","page":"FAA","text":"warning: Unstable — not part of the public interface\nImplementation detail. Names and behavior may change in any release without a deprecation. The supported surface is the Reference.\n\nAIFloats.faa128 is a pure-Julia fused add–add for Quadmath.Float128, vendored from SmallFloats.jl (provenance header in src/carriers/faa128.jl).\n\nIt is the companion to fma128, and exists for the same reason: the draft's FAA operation is defined as a single correctly rounded x + y + z, so evaluating it as two roundings would not implement it. A fused three-term addition with a stated guarantee is what lets the operation take an exact path rather than the enclosure ladder.","title":"FAA — fused add–add"},{"category":"method","location":"99-faa128.html#AIFloats.Float128FAA.choosenan-Tuple{UInt128, UInt128}","page":"FAA","text":"NaN choice, matching libquadmath: larger raw fraction wins, ties to first.\n\n\n\n\n\n","title":"AIFloats.Float128FAA.choosenan"},{"category":"method","location":"99-faa128.html#AIFloats.Float128FAA.faa-Tuple{Quadmath.Float128, Quadmath.Float128, Quadmath.Float128}","page":"FAA","text":"faa(x::Float128, y::Float128, z::Float128) -> Float128\nfaa128(x, y, z)\n\nCorrectly rounded (round-to-nearest, ties-to-even) x + y + z with a single rounding, computed in pure Julia integer arithmetic. Handles signed zeros, subnormals (gradual underflow), infinities, and NaN payload propagation with the same semantics as libquadmath addition applied to (x + y) + z — except that, being fused, only actual input infinities produce an infinite result.\n\n\n\n\n\n","title":"AIFloats.Float128FAA.faa"},{"category":"method","location":"99-faa128.html#AIFloats.Float128FAA.shl256-Tuple{UInt128, UInt128, Int64}","page":"FAA","text":"Left shift by 0 <= s < 256 (callers never shift set bits past bit 255).\n\n\n\n\n\n","title":"AIFloats.Float128FAA.shl256"},{"category":"method","location":"99-faa128.html#AIFloats.Float128FAA.shr256jam-Tuple{UInt128, UInt128, Int64}","page":"FAA","text":"Right shift by s >= 0 with \"jamming\": if any shifted-out bit is nonzero, the result's least-significant bit is set (a sticky bit in the lowest position).\n\n\n\n\n\n","title":"AIFloats.Float128FAA.shr256jam"},{"category":"method","location":"99-faa128.html#AIFloats.Float128FAA.sub256-NTuple{4, UInt128}","page":"FAA","text":"a - b for 256-bit values, assuming a >= b.\n\n\n\n\n\n","title":"AIFloats.Float128FAA.sub256"},{"category":"section","location":"50-status.html#status","page":"Implementation","text":"AIFloats.jl models P3109 formats completely, and now also their values and arithmetic: every datum of every format can be named, decoded, enumerated, ordered, classified, and operated on — through the draft's register or through Julia's own operators — as scalars, arrays, shared-scale blocks, and packed storage. This page says plainly what exists and what the deliberate limits are.","title":"Implementation status"},{"category":"section","location":"50-status.html#Implemented","page":"Implementation","text":"Area What works\nFormat specifiers Binary construction, canonicalization, validation (3 <= K <= 16)\nFormat fields BitwidthOf, PrecisionOf, SignednessOf, DomainOf, TrailingSignificantBitsOf, ExponentBiasOf, ExponentBitwidthOf\nFormat predicates is_signed, is_unsigned, is_finite, is_extended\nAxis singletons SIGNED, UNSIGNED, FINITE, EXTENDED, with Bool interop\nStorage selection CodeType, ValueType\nDatums BinaryValue — a format's member as its code point; codepoint, BinaryFormatOf\nThe codec decode (exact, on the format's carrier: Float64 for B ≤ 512, Float128 for B ≤ 8192, the exact dyadic carrier AIFloats.Dyadic above); the canonical field split and its encode inverse, verified over every code point of all 504 formats\nSpecial values MaxFiniteOf, MinFiniteOf, MinPositiveOf, MaxSubnormalOf, MinNormalOf; NaN/Inf/zero placement\nEnumeration codetable, printcodetable; the 504 capitalized aliases (Binary8p4se), K ≤ 8 exported, the rest via AIFloats.Formats\nTotal order NaN-first order keys, Class/FPClass, NextGreaterThan/NextLessThan\nDatum display four styles — :value (default), :codepoint, :datum, :typed — via set_show_style!\nProjection vocabulary 9 rounding modes (stochastic ones carry their random-bit budget N; isstochastic, nrandbits), 3 saturation modes, Projection and its 27 constants\nProjection behavior project — the single write path: round to precision → saturate → encode, for every rounding mode (sticky protocol, stochastic at fixed R) and all three saturation modes, from Float64/Float128/BigFloat; the interval oracle AIFloats.project_interval\nOperations the draft register — Add, Subtract, Multiply, Divide, FMA, FAA, Sqrt, Exp, Log, the trig/hyperbolic families and their π-scaled forms, Hypot, Clamp, the extremum family — correctly rounded via exact evaluation or interval enclosure; Convert from datums, floats, and integers\nValue construction Binary8p4se(1.5) (via Convert under the task's default projection or an explicit projection keyword)\nDefault projection DefaultProjection, bound for a dynamic extent by with_projection; DefaultRoundingMode/DefaultSaturationMode derive from it\nRandom datums rand (default RTZ_SN, provably < 1) and randn (default RTE_SF, tails clamp; signed formats only)\nArray kernels every register op elementwise over arrays — Add(F, ρ, A, B), Exp(A) — via vmap/vmap!; pure projections gather from a memoized table when policy grants one, otherwise compute per element (same answer either way); stochastic projections run a sequential, seeded-reproducible loop\nTables memoized result tables per (op, formats, projection), fetched internally by the kernels. Public surface: table_policy (prospective, never mutates a counter), AIFloats.table_stats and AIFloats.table_entries (one locked snapshot each, details summing to totals), AIFloats.empty_tables!. Byte and build-time budgets refuse loudly; ternary eager/adaptive bands evict LRU\nBase surface same-format + - * /, abs, sqrt, exp, log, the trig/hyperbolic families, hypot, copysign, max/min (NaN-propagating), fma/muladd, clamp — each one register call under the task's default projection; Op(ρ, x) projection-first convenience; comparison (==/< unordered on NaN), isless/sort in the draft's NaN-first total order (a counting sort, AIFloats.CodeCountingSort); hash/Dict/Set via Base.decompose\nAbstractFloat contract zero/one/eps/typemin/typemax/floatmin/floatmax/precision; exponent/significand/frexp/ldexp; round/floor/ceil/trunc and round(x, RoundUp) etc. as one projection each; nextfloat/prevfloat (off-lattice into NaN)\nConversion & promotion Float64/Float32/Float16/BFloat16/Float128/BigFloat/Int(x); convert (value semantics, Unsigned included); x + 1.0 promotes to the format's public carrier AIFloats.promotecarrier — Float64, Float128, or BigFloat by exponent bias; similar keeps element types concrete; reinterpret checks the representation invariant\nStated refusals rem/mod, round(x, RoundNearestTiesUp)/RoundFromZero, Rational inputs — each throws an ArgumentError that says why, never a bare MethodError\nBlocks (draft §5) Block (scale, elements); BlockOp/ScaledOp for every register op (BlockAdd(FR, ρ, b1, b2, sr), ScaledAdd(FR, ρ, s1, x1, s2, x2)), the reductions BlockReduceAdd/BlockReduceMultiply/BlockDotProduct with exact accumulators at derived precision, the conversions ConvertFromBlock/ConvertToBlock/ConvertToBlockMaxAbsFinite; BlockVector structure-of-arrays storage\nPacked storage PackedVector — code points at K bits per element in 64-bit words; kernels run unpack → compute → emit (AIFloats.vmap(op, F, ρ, pv)); packing_saves\nPerformance layers the Float64 bit path and the Dyadic fixed-point path of round_to_precision, pinned bit-identical to the generic core (35M comparisons); Float128 exactness proofs and the Sticky wide-spread escape for Group A; eager Float64/Float128 envelope stages before the MPFR ladder for every enclosure — each switchable (AIFloats.FAST_ARITH, AIFloats.FAST_ENCLOSURE) and pinned equal to the rigorous path; the Dyadic carrier is verified against golden digests captured from SmallFloats' original; pure-Julia fma128/faa128 for Float128\nGovernance conformance — a declaration shaped by draft §4.6, derived live from the registry, the table cache, and the κ registry (see the note below: this is not a compliance determination) (conformance_report, conformance_dict); register_approx!/measure_kappa — κ measured by enumeration, understatement rejected; ftz_variant, the Annex's worked example; draft_identity with the retained transliteration's digest\nIEEE 754 aliases binary16, binary32, binary64, binary128, bfloat16\n\nEvery accessor and predicate works on the Binary format type, the BinaryValue datum type, and a datum. A format has no instance form: F() raises, because F already is the format. Calling a format constructs a datum, so F(x) isa F is false and the datum type is spelled BinaryValue(F).\n\nConstruction has one semantic axis. F(x), BinaryValue(F, x), BinaryValue{F}(x), and convert(BinaryValue(F), x) all mean the number x, projected into F — Unsigned included, so F(0x03) is three. A code point is a different question with a different spelling, fromcode.","title":"Implemented"},{"category":"section","location":"50-status.html#performance","page":"Implementation","text":"Facts a caller needs in order to predict cost. All are measured; the benchmark suite that produces them is benchmark/runbenchmarks.jl (an isolated environment — Chairmarks is not a dependency of the package). That suite is run during every documentation build and its output is reproduced verbatim under Benchmark results, so the numbers on this page can be checked against the machine that built it.\n\nDecoding. K ≤ 8 decodes through a @generated constant table; wider formats compute the value from its fields. Both are allocation-free. Wider is not one cost: the computed path's carrier follows the format's exponent span, so a K = 16 format on a wider rung decodes materially slower than a K = 12 one. Compare the decode rows on the benchmark page rather than assuming a single figure for \"wider formats\".\n\nTables. A pure-projection array call may gather from a memoized result table instead of computing each element. Which happens is decided by table_policy, from the operand bit-sum ΣK, the arity, the byte budget, and — in the adaptive bands — how many elements that signature has already processed. Every outcome:\n\nstate When Effect\n:eager ΣK ≤ TABLE_EAGER_BITS[] (unary/binary) or TERNARY_EAGER_BITS[] (ternary) fetch or build now, whatever the call's length\n:adaptive_cached in the adaptive band and already built gather\n:adaptive_earned in the adaptive band, and cumulative elements reach the threshold build, then gather\n:adaptive_pending in the adaptive band, threshold not yet reached compute per element\n:over_adaptive_band ΣK beyond TABLE_ADAPTIVE_BITS[] / TERNARY_ADAPTIVE_BITS[] compute per element\n:over_byte_budget the table would exceed the per-table ceiling compute per element\n:stochastic the projection is stochastic compute per element; never tabled\n\nBinary signatures have an adaptive band too — AIFloats.TABLE_ADAPTIVE_BITS and AIFloats.TABLE_BUILD_ELEMS — not only ternary ones. The eager gate cannot see the call's length, so it stays where a first call can afford the build; the adaptive gate does see it, and admits signatures whose tables cost milliseconds once the work justifies them. Thresholds live in src/tables/policy.jl, each with the measurement that set it; ask table_policy rather than transcribing a number.\n\nThe gather is a flat indexed read and is dramatically cheaper than recomputing; see the array rows on the benchmark page for the ratio on the machine that built them.\n\nThreading. Compute kernels thread above AIFloats.THREAD_MIN_ELEMS[], which was fitted at four threads; ask the constant rather than assuming its value. The speedup there is a large fraction of the thread count, and holds for both cheap and expensive operations — compare the compute and compute 1thr rows on the benchmark page. The table gather is never threaded: it already runs at memory bandwidth. Threading costs a fixed ~1.6 KB of scheduler state per call, independent of length; the sequential kernels allocate nothing.\n\nBroadcasting. f.(A, B) routes through the array kernels when f is a registered veneer and every operand is a same-format datum array sharing the destination's axes — so A .+ B costs what vmap! costs. Fused chains ((A .+ B) .* C), scalar operands (A .+ B[1]), mixed element types (A .+ 1.0), predicates (A .< B), and shape broadcasts keep Base's element-at-a-time loop and are correspondingly slower.\n\nStochastic projection. Array results are produced sequentially in index order from a single RNG stream, so a seeded call is reproducible and never depends on the scheduler. Stochastic signatures are never tabled.\n\nPacked storage. PackedVector saves memory only when packing_saves is true (that is, when K is not the storage unit's width). It trades compute for that memory: bit extraction adds cost before a code can index a table, and how much depends on the operation. Measure retained size and throughput together, against the packed rows on the benchmark page, rather than assuming a single ratio.\n\nBlocks. When a block's scale and elements both decode to Float64 and every lane product is exact, BlockReduceAdd, BlockReduceMultiply and BlockDotProduct accumulate exactly in the Dyadic carrier and allocate nothing. Wider carriers, non-finite lanes, or a lane spread beyond Dyadic's exact alignment band fall back to BigFloat at a derived precision — always correct, substantially slower.\n\nAll three reductions take that same guarded route; none of them is committed to BigFloat. Cost scales with the block size and rises on a wider rung, and a product reduction is cheaper than a sum of the same length, because it accumulates one significand instead of aligning B of them. Both paths form the same exact reduction and project once — the guard is a performance decision, never a correctness one. Compare the block rows on the benchmark page against each other.\n\nDefault projection. DefaultProjection() reads a ScopedValue{Projection}. Reading it unbound is nearly free; reading it inside a with_projection block costs an order of magnitude more, because Base.ScopedValues walks its scope. That read, not the arithmetic, is the whole difference between the scoped and unscoped paths.\n\nThe convenience methods and value constructors speculate on the untouched default (RTE_SN) by identity, so the overwhelmingly common path is a static, allocation-free call, and the guard costs only a fraction of the operation it guards. Under a bound projection the same seam recognizes all 27 exported projection constants by identity, so the call stays static and allocates nothing: subtract the scoped read and the arithmetic is already at explicit-projection speed.\n\nA Projection with no constant to match — ρRSA{N} at a non-default stochastic budget — falls through to a projection-typed function barrier and pays one dynamic dispatch, which also costs arity + 1 small boxes: Julia's generic calling convention boxes the isbits return value and every isbits argument it cannot constant-fold. The barrier recovers everything after that boundary — without it the same call is an order of magnitude slower — but the boxes are inherent to the boundary itself.\n\nThe rows to compare on the benchmark page are Add explicit ρ, Add task default ρ, Add scoped non-RTE ρ, and the two DefaultProjection() reads.\n\nThree details of that barrier were each worth more than they look, and are commented at the seam in ops/scalar.jl: it must not be @inlined; it must match Projection{RM,SM} rather than ρ::P where P<:Projection, because an abstract Projection argument satisfies the latter and Julia then binds P = Projection and compiles the barrier away; and it must take the format from a datum rather than from a leading ::Type{F} argument, which alone cost 120 ns in a dynamically dispatched call.\n\nWhere that matters, pass the projection explicitly. Add(F, ρ, x, y) never reads the default at all, allocates nothing under any projection, and is the only form whose cost does not depend on the caller's scope — it is faster than even the unscoped default, because it skips the read entirely.\n\nnote: Reading the benchmark numbers\nAbsolute times on the benchmark page include the harness's per-iteration overhead, and a tight loop can hoist work that an isolated call cannot. A literal argument is worse still: it lets the compiler fold the whole projection, which is why convert(T8, 1.3) once read far slower than T8(1.3) — one of the two was inlinable and therefore foldable and the other was not. Every measured value in the suite is interpolated now, and the two spellings agree. Compare rows against each other, not against an absolute budget.\n\nArray operations resolve the default once per call, never per element, so the scoped-read penalty is a constant that does not grow with the array.\n\nFirst call. Package load and first-call compilation are measured in fresh processes by the latency suite and reported on the benchmark page; they are machine-dependent and are not restated here. The precompile workload covers the standard profile's hot entries — constructors, arithmetic, broadcasting (including four scoped projections), and B ∈ {4, 16, 32} block reductions — so those first calls are fast rather than free. A block size outside that set, an operation outside the workload, or a scoped broadcast under a projection the workload does not name compiles on first use, and a first scoped broadcast is the expensive case.\n\nwarning: `conformance()` is a query, not a certification\nThe designated Interim Report is an unapproved draft whose cover states that it \"must not be utilized for conformance / compliance purposes.\" conformance reports what this package implements, in the shape §4.6 describes. It is not IEEE approval, not a certification, and not a compliance determination — by AIFloats or by anyone else.","title":"Performance characteristics"},{"category":"section","location":"50-status.html#Deliberate-limits","page":"Implementation","text":"Cross-format arithmetic. Binary8p4se(1) + Binary8p3se(1) does not promote — mixing formats is an explicit Convert.\nIn-place packed arithmetic. Packed vectors are storage; computation reads codes out of the bit stream, and the result is an ordinary vector. A packed operand is supported for unary operations only.\nK > 16. The grid stops at UInt16 code units.","title":"Deliberate limits"},{"category":"section","location":"50-status.html#Consequences","page":"Implementation","text":"Everything a datum can do runs through the register under an explicit or task-local-default Projection — Add(F, ρ, x, y), Add(x, y), or x + y.\nsort places NaN first (the draft's total order), not last as for Float64.\nThe engine is verified against an independent BigInt reference — millions of compared decisions per test run, every rounding and saturation mode, plus a totality sweep of every operation at every carrier rung.\nStochastic projection is reproducible: pass R explicitly, or a seeded rng.","title":"Consequences"},{"category":"section","location":"30-formats.html#formats","page":"Binary formats","text":"Binary{K,P,S,D} is the one format specifier. This page covers its construction, its accessors, its aliases, and how it displays. For what the four parameters mean, read Concepts first.","title":"Binary formats"},{"category":"section","location":"30-formats.html#The-canonical-triad","page":"Binary formats","text":"Three spellings, three different questions. Every page uses these letters:\n\njulia> F = Binary8p4se          # the FORMAT — a type, and there is no instance\nBinary{8, 4, ±, ∞}\n\njulia> T = BinaryValue(F)       # the DATUM TYPE — a concrete AbstractFloat\nBinaryValue(Binary8p4se)\n\njulia> x = F(1.5)               # a DATUM of that type\n1.5\n\njulia> x isa T, x isa F\n(true, false)\n\nF() is an error, on purpose — F already is the format.\nF(x) consumes a number, every Integer and Unsigned included.\nfromcode(F, c) consumes a code point. The two are different questions with different spellings, so F(codepoint(y)) can never silently mean y.\n\njulia> fromcode(F, 0x45), F(0x45)\n(1.625, 72.0)\n\nThe report's vocabulary differs from Julia's here. In Interim Report §3.1 a floating-point datum is the mathematical element and a floating-point value is its code point in an associated format. In Julia, BinaryValue is the object that stores that code point and behaves numerically as the datum. This documentation says \"datum\" for the number and \"code point\" for the encoding, and never uses \"value\" for both in one breath.","title":"The canonical triad"},{"category":"section","location":"30-formats.html#Construction","page":"Binary formats","text":"julia> B = Binary(16, 10, UNSIGNED, EXTENDED)\nBinary{16, 10, +, ∞}","title":"Construction"},{"category":"section","location":"30-formats.html#It-returns-a-type","page":"Binary formats","text":"Binary(K, P, S, D) gives back the parameterized type, and that type is the format — there is no second, instance-shaped spelling of it:\n\njulia> B isa Type\ntrue\n\njulia> B()\nERROR: ArgumentError: Binary{16, 10, UNSIGNED, EXTENDED} is already the format; formats have no instances.\n[...]\n\nEvery accessor and predicate in this package takes the format type. B(x) is not the exception it looks like: it constructs the datum of B nearest x, and the datum type is BinaryValue(B).","title":"It returns a type"},{"category":"section","location":"30-formats.html#Singletons-and-Bools-are-interchangeable","page":"Binary formats","text":"S accepts SIGNED/UNSIGNED or a Bool; D accepts EXTENDED/FINITE or a Bool. In both cases true is the second-named option — signed, extended.\n\njulia> Binary(16, 10, false, true) === B\ntrue\n\nThe singletons are clearer at a call site and are what the accessors' documentation refers to; the Bools are convenient when the value is computed. Both reach the same type, because both are canonicalized by AIFloats.resolve_fields before becoming parameters.","title":"Singletons and Bools are interchangeable"},{"category":"section","location":"30-formats.html#Arguments-are-canonicalized","page":"Binary formats","text":"K and P are narrowed to Int8 (IntParam), so the argument type does not affect the result:\n\njulia> Binary(16, 10, SIGNED, FINITE) === Binary(Int32(16), Int32(10), SIGNED, FINITE)\ntrue\n\nwarning: `K` and `P` must have the same type\nThe constructor is Binary(K::I, P::I, ...) where {I<:Integer} — one type variable for both. Mixing them is a MethodError, so promote first:julia> Binary(Int8(8), 4, SIGNED, FINITE)\nERROR: MethodError: no method matching Binary(::Int8, ::Int64, ...)\n\njulia> Binary(promote(Int8(8), 4)..., SIGNED, FINITE)\nBinary{8, 4, ±, ⏥}","title":"Arguments are canonicalized"},{"category":"section","location":"30-formats.html#Invalid-formats-throw","page":"Binary formats","text":"julia> Binary(2, 1, SIGNED, FINITE)\nERROR: ArgumentError: Invalid format: K=2, P=1, S=SIGNED, D=FINITE\n[...]\n\nThe rules are K > 2, P > 0, and P <= K - S; Concepts derives them from the bit budget. Binary is the validator — it checks and throws, and there is no supported non-throwing form. To test a combination without an exception, catch it:\n\njulia> isvalid_format(K, P, S, D) = try Binary(K, P, S, D); true catch; false end\nisvalid_format (generic function with 1 method)\n\njulia> isvalid_format(8, 4, SIGNED, FINITE), isvalid_format(2, 1, SIGNED, FINITE)\n(true, false)","title":"Invalid formats throw"},{"category":"section","location":"30-formats.html#Accessors","page":"Binary formats","text":"The four parameters come back out under names that say what they are:\n\njulia> BitwidthOf(B), PrecisionOf(B), SignednessOf(B), DomainOf(B)\n(16, 10, false, true)\n\nBitwidthOf and PrecisionOf return the stored Int8s; SignednessOf and DomainOf return Bools.\n\nAIFloats.TrailingSignificantBitsOf gives the stored significand width, one less than the precision:\n\njulia> PrecisionOf(B), AIFloats.TrailingSignificantBitsOf(B)\n(10, 9)","title":"Accessors"},{"category":"section","location":"30-formats.html#Predicates","page":"Binary formats","text":"Four predicates read better than comparing the Bools:\n\njulia> is_unsigned(B), is_signed(B)\n(true, false)\n\njulia> is_extended(B), is_finite(B)\n(true, false)\n\nThey also accept the singletons and raw Bools directly, which is useful when you hold a signedness rather than a format:\n\njulia> is_signed(SIGNED), is_extended(false)\n(true, false)\n\nnote: `is_finite` is about formats, not numbers\nis_finite asks whether a format excludes the infinities. It is unrelated to Base.isfinite, which asks whether a number is one.","title":"Predicates"},{"category":"section","location":"30-formats.html#Storage-types","page":"Binary formats","text":"CodeType gives an unsigned integer wide enough for any code point of the format; ValueType gives a float wide enough to hold any of its values exactly.\n\nK CodeType ValueType\nK ≤ 8 UInt8 Float32\n9 ≤ K ≤ 10 UInt16 Float64\nK > 10 UInt16 Float128\n\nBoth accept a bitwidth as well as a format:\n\njulia> CodeType(B), ValueType(B)\n(UInt16, Quadmath.Float128)\n\njulia> CodeType(8), ValueType(8)\n(UInt8, Float32)","title":"Storage types"},{"category":"section","location":"30-formats.html#Display","page":"Binary formats","text":"A format has two written forms, and which appears depends on how it is shown.\n\njulia> B                                    # the REPL uses MIME\"text/plain\": glyphs\nBinary{16, 10, +, ∞}\n\njulia> repr(B)                              # plain show: words\n\"Binary{16, 10, UNSIGNED, EXTENDED}\"\n\nThe same split is available as functions — lowercase string for words, capital String for glyphs:\n\njulia> string(B)\n\"Binary{16, 10, UNSIGNED, EXTENDED}\"\n\njulia> String(B)\n\"Binary{16, 10, +, ∞}\"\n\nThe glyphs are one per axis:\n\nAxis Glyph Meaning\nsignedness ± SIGNED\n + UNSIGNED\ndomain ∞ EXTENDED\n ⏥ FINITE\n\nThis is format display. Datum display is a separate axis with its own scopes; see Datum display scope.","title":"Display"},{"category":"section","location":"30-formats.html#Aliases","page":"Binary formats","text":"Every valid format in the 3 ≤ K ≤ 16 grid has a generated alias spelled the way the report names formats (§3.2): Binary⟨K⟩p⟨P⟩⟨s|u⟩⟨f|e⟩. Those with K ≤ 8 are exported; the rest live in AIFloats.Formats.\n\njulia> Binary8p4se === Binary(8, 4, SIGNED, EXTENDED)\ntrue\n\njulia> AIFloats.Formats.Binary16p10ue === Binary(16, 10, UNSIGNED, EXTENDED)\ntrue\n\nAn alias names a format. It is not a datum type and not a datum — BinaryValue(F) and F(x) are those.","title":"Aliases"},{"category":"section","location":"30-formats.html#Two-spellings-of-every-query","page":"Binary formats","text":"Each capitalized query has a lower-case Julia-style spelling. The pair is the same generic function object — not a wrapper, not an adapter, not a compatibility shim, and with no call-site cost either way:\n\nCapitalized Julia-style\nBitwidthOf bitwidth\nSignednessOf signedness\nDomainOf domain\nBinaryFormatOf formatof\nCodeType codetype\nValueType valuetype\n\njulia> bitwidth === BitwidthOf, formatof === BinaryFormatOf\n(true, true)\n\nUse formatof(x) to recover a format from a datum you were handed. Do not write formatof(F) when F is already the format.\n\nformatinfo answers every static question at once, and folds to a literal when the format is known at compile time:\n\njulia> info = formatinfo(Binary8p4se);\n\njulia> info.name, info.bitwidth, info.precision, info.exponentbits\n(:Binary8p4se, 8, 4, 4)\n\njulia> info.datumtype\nBinaryValue(Binary8p4se)","title":"Two spellings of every query"},{"category":"section","location":"30-formats.html#display-scope","page":"Binary formats","text":"How a datum prints is a separate question from how a format prints, and it has two mechanisms with different reach:\n\nMechanism Scope\nset_show_style! process-wide fallback\nan IOContext with :binary_show_style that one show, composable\n\njulia> x = Binary8p4se(1.5);\n\njulia> sprint(show, x; context = :binary_show_style => :codepoint)\n\"0x44\"\n\njulia> sprint(show, x; context = :binary_show_style => :typed)\n\"Binary8p4se(1.5 ⇆ 0x44)\"\n\njulia> get_show_style()\n:value\n\nwarning: Library code should not call `set_show_style!`\nIt changes a process-wide preference that the caller may be relying on. Pass an IOContext instead. This is display state only — it is unrelated to the task-local numerical DefaultProjection, which is bound rather than set for exactly the same reason.","title":"Datum display scope"},{"category":"section","location":"70-examples/20-intermediate.html#examples-intermediate","page":"Intermediate examples","text":"","title":"Intermediate examples"},{"category":"section","location":"70-examples/20-intermediate.html#Define-a-custom-format","page":"Intermediate examples","text":"Binary(K, P, signedness, domain) returns a format type. Wrap it in BinaryValue to obtain its concrete datum type.\n\nusing AIFloats\n\nF = Binary(8, 3, SIGNED, EXTENDED)\nT = BinaryValue(F)\n(T, BitwidthOf(T), PrecisionOf(T), ExponentBiasOf(T))\n(BinaryValue(Binary8p3se), 8, 3, 16)","title":"Define a custom format"},{"category":"section","location":"70-examples/20-intermediate.html#Compare-projection-choices","page":"Intermediate examples","text":"using AIFloats\n\nF = Binary(8, 3, SIGNED, EXTENDED)\nvalue = 1.2\nprojections = (\n    RTE = RTE_SN,\n    RTP = RTP_SN,\n    RTN = RTN_SN,\n    RTZ = RTZ_SN,\n    RTO = RTO_SN,\n)\nresults = map(p -> Convert(F, p, value), projections)\n(\n    input = value,\n    decoded = map(decode, results),\n    codes = map(codepoint, results),\n)\n(input = 1.2, decoded = (RTE = 1.25, RTP = 1.25, RTN = 1.0, RTZ = 1.0, RTO = 1.25), codes = (RTE = 0x41, RTP = 0x41, RTN = 0x40, RTZ = 0x40, RTO = 0x41))\n\nSeveral modes may select the same neighbor: one inexact input lies between only two adjacent datums. Here upward, nearest-even, and round-to-odd select 1.25, while downward and toward-zero select 1.0.","title":"Compare projection choices"},{"category":"section","location":"70-examples/20-intermediate.html#Tell-the-saturation-modes-apart","page":"Intermediate examples","text":"Saturation matters outside the format's finite range — but a large finite input does not separate the modes. SF and SP both clamp it, so the answers are identical:\n\nusing AIFloats\n\nF = Binary(8, 3, SIGNED, EXTENDED)\nfinite = (SF = Convert(F, RTE_SF, 1.0e100), SP = Convert(F, RTE_SP, 1.0e100))\n@assert finite.SF === finite.SP\nfinite\n(SF = 49152.0, SP = 49152.0)\n\nThe defining difference is what happens to a genuine infinity. SF clamps it; SP keeps it when the format can represent it:\n\nusing AIFloats\n\nF = Binary(8, 3, SIGNED, EXTENDED)\ninfinite = (SF = Convert(F, RTE_SF, Inf), SP = Convert(F, RTE_SP, Inf))\n@assert infinite.SF !== infinite.SP\n@assert isinf(infinite.SP) && isfinite(infinite.SF)\ninfinite\n(SF = 49152.0, SP = Inf)\n\nSN is governed by rounding direction, signedness, and domain, so it can yield an extremal finite, an infinity, or NaN from the same input:\n\nusing AIFloats\n\nE = Binary(8, 3, SIGNED, EXTENDED)     # signed, has infinities\nU = Binary(8, 3, UNSIGNED, EXTENDED)   # no −Inf to reach\nV = Binary(8, 3, SIGNED, FINITE)       # no infinities at all\n(\n    directed_back_into_range = Convert(E, RTZ_SN, 1.0e100),   # extremal finite\n    extended_signed          = Convert(E, RTE_SN, -1.0e100),  # −Inf\n    extended_unsigned        = Convert(U, RTE_SN, -1.0e100),  # NaN: no −Inf\n    finite_domain            = Convert(V, RTE_SN, 1.0e100),   # NaN: no infinities\n)\n(directed_back_into_range = 49152.0, extended_signed = -Inf, extended_unsigned = NaN, finite_domain = NaN)\n\nMode out-of-range finite ±Inf\nSF clamps to the extremal finite clamps to the extremal finite\nSP clamps to the extremal finite kept when representable, else clamped\nSN extremal finite if the rounding direction points back into range; else the infinity if EXTENDED; else NaN the infinity if EXTENDED and signed as needed, else NaN","title":"Tell the saturation modes apart"},{"category":"section","location":"70-examples/20-intermediate.html#Change-the-projection-for-a-block","page":"Intermediate examples","text":"Convenience operations and constructors read the task's default projection. Bind it for a dynamic extent with with_projection; it is restored on return and on exception, it nests, and a task started inside inherits it:\n\nusing AIFloats\n\nF = Binary(8, 3, SIGNED, EXTENDED)\nT = BinaryValue(F)\nwith_projection(RTZ_SF) do\n    (DefaultProjection(), T(1.3))\nend\n(ρ(RoundTowardZero, SatFinite), 1.25)\n\nThere is no setter. A process-wide one would make two concurrently running tasks silently fight over what arithmetic means, which is exactly what a projection must never be.\n\nLibrary code should still generally prefer explicit projections: Add(F, ρ, x, y) never reads the default at all, and it is the fastest path by a wide margin.","title":"Change the projection for a block"},{"category":"section","location":"70-examples/20-intermediate.html#Reproduce-stochastic-rounding","page":"Intermediate examples","text":"An explicit R fixes the random draw used by a stochastic projection. This is especially useful in tests:\n\nusing AIFloats\n\nF = Binary(8, 3, SIGNED, EXTENDED)\nT = BinaryValue(F)\nx, y = T(1.0), T(0.125)\n# With neither `R` nor `rng`, AIFloats draws from Random.default_rng()\n# (the current task's TaskLocalRNG on Julia 1.12).\n# Here the explicit `R` supplies the draw instead, making the call repeatable.\na = Add(F, RSA_SN, x, y; R=3)\nb = Add(F, RSA_SN, x, y; R=3)\n(a, b, a === b)\n(1.0, 1.0, true)\n\nIf neither R nor rng is supplied, AIFloats draws from Random.default_rng(), just as rand() does. On Julia 1.12 this is the current task's TaskLocalRNG, backed by Xoshiro256++; a fresh Julia session is seeded from system randomness, so calls are not reproducible across sessions by default. The concrete default-RNG type and its exact stream are Julia implementation details and may change between Julia versions.\n\nFor a reproducible sequence, pass an explicitly seeded RNG and retain it across calls:\n\nusing AIFloats, Random\n\nF = Binary(8, 3, SIGNED, EXTENDED)\nT = BinaryValue(F)\nx, y = T(1.0), T(0.125)\nrng1 = Xoshiro(2026)\nrng2 = Xoshiro(2026)\nseq1 = [Add(F, RSA_SN, x, y; rng=rng1) for _ in 1:8]\nseq2 = [Add(F, RSA_SN, x, y; rng=rng2) for _ in 1:8]\n(seq1, seq1 == seq2)\n(BinaryValue(Binary8p3se)[1.0, 1.25, 1.0, 1.0, 1.25, 1.0, 1.25, 1.0], true)\n\nOmitting rng is appropriate when nondeterministic session-to-session behavior is wanted. Pass an RNG when reproducibility, stream ownership, or isolation from unrelated random draws matters.","title":"Reproduce stochastic rounding"},{"category":"section","location":"70-examples/20-intermediate.html#The-whole-contract,-in-one-place","page":"Intermediate examples","text":"using AIFloats, Random\n\nF = Binary(8, 3, SIGNED, EXTENDED)\nT = BinaryValue(F)\nx, y = T(1.0), T(0.125)\n\n# N — the random-bit budget — is carried in the mode's type, and is in 1:60.\nbudget = (nrandbits(RSA_SN), isstochastic(RSA_SN), isstochastic(RTE_SN))\n\n# R takes precedence when both R and rng are supplied: the draw is named\n# outright, so the rng is not consulted and its stream does not advance.\nrng = Xoshiro(2026)\nbefore = copy(rng)\nwithR = Add(F, RSA_SN, x, y; R = 3, rng = rng)\nrng_untouched = (rng == before)\n\n# A pure projection touches no RNG state at all, whatever you pass.\nrng2 = Xoshiro(2026)\nbefore2 = copy(rng2)\npure = Add(F, RTE_SN, x, y; rng = rng2)\npure_untouched = (rng2 == before2)\n\n(budget = budget, withR = withR, rng_untouched = rng_untouched,\n pure = pure, pure_untouched = pure_untouched)\n(budget = (8, true, false), withR = 1.0, rng_untouched = true, pure = 1.0, pure_untouched = true)\n\nAn out-of-range R is refused rather than wrapped:\n\nusing AIFloats\n\nF = Binary(8, 3, SIGNED, EXTENDED)\nT = BinaryValue(F)\ntry\n    Add(F, RSA_SN, T(1.0), T(0.125); R = 1 << 60)   # RSA_SN has N = 8, so R < 256\ncatch err\n    err\nend\nArgumentError(\"explicit R=1152921504606846976 outside 0:255 for N=8 random bits\")\n\nArrays take an rng and consume one sequential stream in eachindex order. There is no per-element R, and the result never depends on the scheduler:\n\nusing AIFloats, Random\n\nF = Binary(8, 3, SIGNED, EXTENDED)\nT = BinaryValue(F)\nA = T[1.0, 1.0, 1.0, 1.0]\nB = T[0.125, 0.125, 0.125, 0.125]\n\nfirst_run  = vmap(:Add, F, RSA_SN, A, B; rng = Xoshiro(7))\nsecond_run = vmap(:Add, F, RSA_SN, A, B; rng = Xoshiro(7))\n@assert first_run == second_run\n(first_run, first_run == second_run)\n(BinaryValue(Binary8p3se)[1.0, 1.25, 1.25, 1.25], true)","title":"The whole contract, in one place"},{"category":"section","location":"70-examples/20-intermediate.html#Apply-operations-to-arrays","page":"Intermediate examples","text":"vmap allocates the destination; vmap! writes into an existing array. The kernel chooses a table gather or scalar computation without changing the result.\n\nusing AIFloats\n\nF = Binary(8, 3, SIGNED, EXTENDED)\nT = BinaryValue(F)\nA = T[0.25, 0.5, 1.0, 2.0]\nB = T[2.0, 1.0, 0.5, 0.25]\n\nC = vmap(:Add, F, RTE_SN, A, B)\nD = similar(A)\nvmap!(D, :Multiply, RTE_SN, A, B)\n(C, D)\n(BinaryValue(Binary8p3se)[2.0, 1.5, 1.5, 2.0], BinaryValue(Binary8p3se)[0.5, 0.5, 0.5, 0.5])\n\nRegistered same-format broadcasts use the same array machinery:\n\nusing AIFloats\n\nT = BinaryValue(Binary(8, 3, SIGNED, EXTENDED))\nA = T[0.25, 0.5, 1.0, 2.0]\nB = T[2.0, 1.0, 0.5, 0.25]\n(A .+ B, exp.(A))\n(BinaryValue(Binary8p3se)[2.0, 1.5, 1.5, 2.0], BinaryValue(Binary8p3se)[1.25, 1.75, 2.5, 7.0])","title":"Apply operations to arrays"},{"category":"section","location":"70-examples/20-intermediate.html#Convert-a-whole-array","page":"Intermediate examples","text":"Convert(F, A) resolves the task default once, before the loop, and never per element:\n\nusing AIFloats\n\nF = Binary(8, 3, SIGNED, EXTENDED)\nA = [0.1, 1.2, 3.7, 100.0]\nconverted = Convert(F, A)\n(converted, eltype(converted), axes(converted) == axes(A))\n(BinaryValue(Binary8p3se)[0.09375, 1.25, 3.5, 96.0], BinaryValue(Binary8p3se), true)\n\nMismatched axes are a DimensionMismatch, not a silently truncated result:\n\nusing AIFloats\n\nF = Binary(8, 3, SIGNED, EXTENDED)\nT = BinaryValue(F)\ntry\n    vmap(:Add, F, RTE_SN, T[1.0, 2.0], T[1.0])\ncatch err\n    err\nend\nDimensionMismatch(\"dest, A, B must share axes\")\n\nContinue with Advanced examples for packed and shared-scale storage.","title":"Convert a whole array"},{"category":"section","location":"95-reference.html#reference","page":"Overview","text":"Every public name in AIFloats.jl: the exported bindings and those declared public.\n\nThe listing pages below are generated during the documentation build from Base.isexported and Base.ispublic, and the build fails if a public binding is neither documented nor listed as a reviewed exemption. The reference is therefore complete by construction, and holds nothing private.\n\nNames you can call unqualified are exported; the rest are public and are reached as AIFloats.name.\n\nThe 504 generated format aliases are deliberately absent. Each says only that it is the format its own name spells out — Binary6p3ue is Binary(6, 3, UNSIGNED, EXTENDED) — so listing the 120 exported ones buried the entries that carry information. They keep their docstrings and still answer to ?Binary6p3ue; Concepts gives the naming scheme once and in full.\n\nPage What it lists\nFormats, datums, projections Binary and its aliases, the four axes, the format queries, BinaryValue and the codec, datum display, and the projection vocabulary\nOperations, kernels, storage the register operations, vmap/vmap! and the table policy surface, and PackedVector\nBlocks and scaled operations Block, BlockVector, and the generated Block*, Scaled*, and block-conversion families\nConformance conformance and the ϰ registry, the IEEE 754 aliases, and the carrier-level types and switches\n\nFor the narrative introduction to these names, start at Getting started; for the operation family as a whole, see Operations.\n\nImplementation-only material — including the AIFloats.DyadicNumbers carrier and its Dyadic type — lives on Dyadic Numbers and is explicitly unstable.","title":"Reference"},{"category":"section","location":"70-examples/40-technical.html#examples-technical","page":"Technical examples","text":"These examples are useful when integrating AIFloats into a numerical library, investigating performance, or checking conformance assumptions.","title":"Technical examples"},{"category":"section","location":"70-examples/40-technical.html#Inspect-carrier-selection","page":"Technical examples","text":"The format's exponent span determines its internal carrier rung independently of its storage width.\n\nusing AIFloats\n\nformats = (\n    Binary(8, 4, SIGNED, EXTENDED),\n    Binary(16, 4, SIGNED, EXTENDED),\n    Binary(16, 1, UNSIGNED, FINITE),\n)\n\n[(F, AIFloats.rung(F), AIFloats.datumcarrier(F)) for F in formats]\n3-element Vector{Tuple{DataType, AIFloats.Head, DataType}}:\n (Binary{8, 4, SIGNED, EXTENDED}, AIFloats.HeadF64(), Float64)\n (Binary{16, 4, SIGNED, EXTENDED}, AIFloats.HeadF128(), Quadmath.Float128)\n (Binary{16, 1, UNSIGNED, FINITE}, AIFloats.HeadExact(), AIFloats.DyadicNumbers.Dyadic)\n\nCarrier types are implementation details and are not part of the stable interface. Use decode, registered operations, and project rather than depending on a particular rung.","title":"Inspect carrier selection"},{"category":"section","location":"70-examples/40-technical.html#Inspect-table-policy-without-building-a-table","page":"Technical examples","text":"using AIFloats\n\nF8 = BinaryFormatOf(Binary8p4se)\nF9 = Binary(9, 4, SIGNED, EXTENDED)\n\neager = AIFloats.table_policy(:Add, F8, F8, F8, RTE_SN)\nadaptive = AIFloats.table_policy(:Add, F9, F9, F9, RTE_SN; nelems=65_536)\n(eager, adaptive)\n((shape = :A, entries = 65536, bytes = 65536, state = :eager, cumulative = 0, threshold = 0, reason = \"eager build band\"), (shape = :B, entries = 262144, bytes = 524288, state = :adaptive_pending, cumulative = 0, threshold = 1000000, reason = \"adaptive table needs 1000000 more cumulative elements\"))\n\nThe returned state explains the decision. shape = :A means the matching kernel call is eligible to fetch/build a table and gather from it; shape = :B means it will compute each element through the scalar operation.\n\nstate Meaning\n:eager The operand-code bits fit the eager build band. The next call may fetch an existing table or build it immediately, regardless of array length.\n:adaptive_pending The signature fits the adaptive band but is not cached and has not processed enough cumulative elements to pay for a build. The call computes scalarly.\n:adaptive_earned The cached use count plus the supplied prospective nelems reaches the build threshold. A real call of that size would build and then use the table. Calling table_policy itself does not build it or change the count.\n:adaptive_cached An adaptively earned table has already been built and is available for a gather.\n:stochastic Stochastic projection depends on a random draw, so it is never represented by one deterministic result table.\n:over_byte_budget The complete table would exceed the configured per-table memory ceiling.\n:over_adaptive_band The operand-code space is beyond the largest band for which policy permits a table build.\n\ncumulative is the number of elements previously seen for that adaptive signature, and threshold is the count required to earn its table. Supplying nelems asks what a future call of that size would do without incrementing cumulative or changing the cache.","title":"Inspect table policy without building a table"},{"category":"section","location":"70-examples/40-technical.html#Inspect-the-table-cache","page":"Technical examples","text":"AIFloats.table_stats() is one locked snapshot: its counts and its byte total describe the same moment, which is why it exists rather than a pair of separate queries.\n\nusing AIFloats\n\ns = AIFloats.table_stats()\n(s.entries, s.bytes, s.by_arity)\n(4, 131360, (unary = 2, binary = 2, ternary = 0))\n\nAIFloats.table_entries() is a second locked snapshot, per entry, naming format types and the mode names you write. Populate the cache through the ordinary array path — vmap — rather than reaching for a private builder:\n\nusing AIFloats\n\nAIFloats.empty_tables!()\n\nF = Binary(3, 2, SIGNED, FINITE)          # ΣK = 3: comfortably in the eager band\nT = BinaryValue(F)\nA = T[0.5, 1.0, 1.5]\nvmap(:Negate, F, RTE_SN, A)               # this is what builds and caches the table\n\nentries = AIFloats.table_entries()\n[(e.op, e.result, e.operands, e.rounding, e.saturation, e.bytes) for e in entries]\n1-element Vector{Tuple{Symbol, DataType, Tuple{DataType}, Symbol, Symbol, Int64}}:\n (:Negate, Binary{3, 2, SIGNED, FINITE}, (Binary{3, 2, SIGNED, FINITE},), :RTE, :SN, 8)\n\nwarning: Two snapshots are two moments\nEach of table_stats() and table_entries() is internally coherent, but they are separate calls. The entry bytes sum to a separately obtained table_stats().bytes only if the cache is not mutated between them — another task's first array call is enough to break it.Where one coherent count-and-byte declaration is required, use conformance: it derives its byte total from the very entry vector it captured.\n\nusing AIFloats\n\nc = conformance()\n(length(c.cached_specializations), c.cached_bytes)\n(1, 8)","title":"Inspect the table cache"},{"category":"section","location":"70-examples/40-technical.html#Compare-the-fast-enclosure-with-the-rigorous-ladder","page":"Technical examples","text":"The performance switch is intended for differential testing. Both paths must produce the same final code point:\n\nusing AIFloats\n\nT = Binary8p4se\nx = T(1.5)\nfast = Exp(T, RTE_SN, x)\n\nsaved = AIFloats.FAST_ENCLOSURE[]\nrigorous = try\n    AIFloats.FAST_ENCLOSURE[] = false\n    Exp(T, RTE_SN, x)\nfinally\n    AIFloats.FAST_ENCLOSURE[] = saved\nend\n\n(fast, rigorous, codepoint(fast) == codepoint(rigorous))\n(4.5, 4.5, true)\n\nThis switch is diagnostic, not a request for weaker rounding. The fallback is the correctly rounded MPFR enclosure ladder. In particular, no Quadmath transcendental result is assumed to be correctly rounded.","title":"Compare the fast enclosure with the rigorous ladder"},{"category":"section","location":"70-examples/40-technical.html#Examine-raw-codes-and-the-total-order","page":"Technical examples","text":"using AIFloats\n\nT = BinaryValue(Binary8p4se)\nsamples = T[fromcode(T, c) for c in (0x00, 0x01, 0x40, 0x7f, 0x80, 0xff)]\n[(codepoint(x), decode(x), Class(x), AIFloats.order_key(x)) for x in samples]\n6-element Vector{Tuple{UInt8, Float64, FPClass, UInt16}}:\n (0x00, 0.0, ClassZero, 0x0081)\n (0x01, 0.0009765625, ClassPosSubnormal, 0x0082)\n (0x40, 1.0, ClassPosNormal, 0x00c1)\n (0x7f, Inf, ClassPosInf, 0x0100)\n (0x80, NaN, ClassNaN, 0x0000)\n (0xff, -Inf, ClassNegInf, 0x0001)\n\nSorting uses the draft's NaN-first total order:\n\nusing AIFloats\n\nT = BinaryValue(Binary8p4se)\nsamples = T[fromcode(T, c) for c in (0x00, 0x01, 0x40, 0x7f, 0x80, 0xff)]\nsort(samples)\n6-element Vector{BinaryValue(Binary8p4se)}:\n NaN\n -Inf\n   0.0\n   0.0009765625\n   1.0\n  Inf","title":"Examine raw codes and the total order"},{"category":"section","location":"70-examples/40-technical.html#Query-the-conformance-declaration","page":"Technical examples","text":"using AIFloats\n\nc = conformance()\n(draft_revision(), draft_identity(), typeof(c))\n(\"IEEE P3109 Interim Report on Arithmetic Formats for Machine Learning, version 4.0.3 (2026-09-01) — unapproved draft; not for conformance/compliance use\", (designation = \"IEEE P3109 Interim Report on Arithmetic Formats for Machine Learning\", report_revision = \"4.0.3\", report_date = \"2026-09-01\", report_url = \"https://github.com/P3109/Public/blob/main/IEEE%20P3109%20Interim%20Report.pdf\", report_sha256 = \"7de115ed6882b7550b8fa61e81e5173857b340c3bfe30db8d4ad74b472229b9e\", report_status = \"unapproved draft; not for conformance/compliance use\", retained_designation = \"IEEE P3109/D1\", retained_uploaded = \"2026-07-17\", retained_source = \"docs/other/IEEE_D1.md\", transliteration_sha256 = \"820cb5009cd6fe9032f5bdfb661bc639e33296f716a552eafc81f899411bb5f2\"), AIFloats.ConformanceDeclaration)\n\nUse conformance_report for the human-readable declaration and conformance_dict for structured tooling.\n\ndraft_identity() names the designated Interim Report — revision, date, URL, and the SHA-256 of the PDF the implementation was compared against — alongside the retained transliteration it was originally written from. The report is an unapproved draft; its cover states it must not be used for conformance or compliance purposes. conformance() reports what this package implements, in the shape §4.6 describes. It is not IEEE approval, not a certification, and not a compliance determination.","title":"Query the conformance declaration"},{"category":"section","location":"70-examples/40-technical.html#Discover-the-operation-register","page":"Technical examples","text":"The registry is queryable without touching its private storage:\n\nusing AIFloats\n\nops = AIFloats.operations()\nby_arity = Dict(a => count(o -> o.arity == a, ops) for a in 1:3)\n(total = length(ops), by_arity = by_arity, add = AIFloats.operationinfo(:Add))\n(total = 52, by_arity = Dict(2 => 18, 3 => 3, 1 => 31), add = (name = :Add, arity = 2, group = :A, factors = 1))\n\nAIFloats.operations() and AIFloats.operationinfo are the supported entry points; OP_REGISTRY itself is private and its shape is not part of the interface. See Operations.","title":"Discover the operation register"},{"category":"section","location":"95-reference/40-governance.html#Conformance","page":"Conformance","text":"Part of the Reference; see that page for how this listing is generated and what it guarantees.\n\nPages = [\"40-governance.md\"]\nDepth = 2","title":"Conformance"},{"category":"section","location":"95-reference/40-governance.html#Conformance-and-ϰ","page":"Conformance","text":"","title":"Conformance and ϰ"},{"category":"section","location":"95-reference/40-governance.html#External-formats","page":"Conformance","text":"","title":"External formats"},{"category":"section","location":"95-reference/40-governance.html#Types-and-formats","page":"Conformance","text":"","title":"Types and formats"},{"category":"section","location":"95-reference/40-governance.html#Index","page":"Conformance","text":"Pages = [\"40-governance.md\"]","title":"Index"},{"category":"type","location":"95-reference/40-governance.html#AIFloats.ApproxImpl","page":"Conformance","text":"ApproxImpl\n\nA registered κ-approximate implementation of one operation specialization: name, op, result format fr, argformats, ρ, the callable fn, and the declared and measured κ (with whether the measurement was exhaustive).\n\n\n\n\n\n","title":"AIFloats.ApproxImpl"},{"category":"type","location":"95-reference/40-governance.html#AIFloats.ConformanceDeclaration","page":"Conformance","text":"ConformanceDeclaration\n\nThe package's draft-§4.6-style conformance declaration. Fields: package, package_version, draft, draft_identity, interpretations, formats, operations, rounding_modes, saturation_modes, block_surface, cached_specializations, cached_bytes, approximate. Build with conformance.\n\n\n\n\n\n","title":"AIFloats.ConformanceDeclaration"},{"category":"function","location":"95-reference/40-governance.html#AIFloats.approx","page":"Conformance","text":"Retrieve a registered approximate implementation (callable via .fn).\n\n\n\n\n\n","title":"AIFloats.approx"},{"category":"function","location":"95-reference/40-governance.html#AIFloats.conformance","page":"Conformance","text":"conformance() -> ConformanceDeclaration\n\nThe live conformance declaration: formats from the alias grid, operations from OP_REGISTRY, the block surface generated from it, the pure-ρ table specializations actually instantiated (AIFloats.table_entries() — both code-unit caches, in one locked snapshot alongside their total bytes), and the κ registry. Serialize with conformance_dict or render with conformance_report.\n\n\n\n\n\n","title":"AIFloats.conformance"},{"category":"function","location":"95-reference/40-governance.html#AIFloats.conformance_dict","page":"Conformance","text":"conformance_dict(c = conformance()) -> Dict{String,Any}\n\nNested Dict form of the declaration — serializable by any JSON/TOML writer.\n\n\n\n\n\n","title":"AIFloats.conformance_dict"},{"category":"function","location":"95-reference/40-governance.html#AIFloats.conformance_report","page":"Conformance","text":"conformance_report([io], c = conformance())\n\nHuman-readable rendering of the conformance declaration.\n\n\n\n\n\n","title":"AIFloats.conformance_report"},{"category":"function","location":"95-reference/40-governance.html#AIFloats.draft_identity","page":"Conformance","text":"draft_identity() -> NamedTuple\n\nStructured identity of the designated P3109 Interim Report — revision, date, URL, and PDF SHA-256 — together with the retained transliteration and its digest. See AIFloats.DRAFT_IDENTITY for what each field means and which one is authoritative.\n\n\n\n\n\n","title":"AIFloats.draft_identity"},{"category":"function","location":"95-reference/40-governance.html#AIFloats.draft_revision","page":"Conformance","text":"draft_revision() -> String\n\nHuman-readable designation of the normative P3109 report this release is measured against, including its unapproved-draft status. See draft_identity for the structured form.\n\n\n\n\n\n","title":"AIFloats.draft_revision"},{"category":"function","location":"95-reference/40-governance.html#AIFloats.ftz_variant","page":"Conformance","text":"ftz_variant(op, FR, F1, ρ) -> fn\n\nThe Annex-style approximate unary implementation: compute the defined result, then flush subnormal results to zero or MinNormalOf(FR), whichever is nearer, ties toward zero (magnitude-symmetric for signed formats). Suitable for register_approx!; for precision P ≥ 2 its κ is 2^(P-2), and 0 when P == 1 (no subnormals exist).\n\n\n\n\n\n","title":"AIFloats.ftz_variant"},{"category":"function","location":"95-reference/40-governance.html#AIFloats.kappa","page":"Conformance","text":"kappa(a::ApproxImpl) -> Float64\nkappa(name::Symbol) -> Float64\n\nThe declared κ of a registered approximate implementation — the error bound its author stands behind, in the sense of Interim Report §4.4.\n\n§4.6 requires an implementation that supplies an approximate specialization to declare κ. This is the declared figure; kappa_measured is what enumeration actually found. A declaration that understates the measured value is rejected at registration.\n\nNot exported; call it as AIFloats.kappa.\n\n\n\n\n\n","title":"AIFloats.kappa"},{"category":"function","location":"95-reference/40-governance.html#AIFloats.kappa_measured","page":"Conformance","text":"kappa_measured(a::ApproxImpl) -> Float64\n\nThe κ that measure_kappa found by enumeration, as distinct from the value the implementation declared (kappa).\n\nFor an exhaustively enumerated specialization this is the exact worst case over every operand code point, and a.exhaustive is true; otherwise it is the worst case over the sampled domain and is a lower bound on the true κ.\n\nNot exported; call it as AIFloats.kappa_measured.\n\n\n\n\n\n","title":"AIFloats.kappa_measured"},{"category":"function","location":"95-reference/40-governance.html#AIFloats.list_approx","page":"Conformance","text":"Sorted names of all registered κ-approximate implementations.\n\n\n\n\n\n","title":"AIFloats.list_approx"},{"category":"function","location":"95-reference/40-governance.html#AIFloats.measure_kappa","page":"Conformance","text":"measure_kappa(fn, op, FR, argformats, ρ; max_exhaustive = 2^22, rng, samples = 2^20)\n    -> (κ::Float64, exhaustive::Bool)\n\nMeasure κ of fn(x::argformats[1], …)::BinaryValue{FR} against the defined results of draft operation op under the deterministic projection ρ. Enumerates the full input cross-product when it has at most max_exhaustive points; otherwise measures on samples uniform draws and reports exhaustive = false. Stochastic ρ is refused — κ is defined against deterministic defined results.\n\n\n\n\n\n","title":"AIFloats.measure_kappa"},{"category":"function","location":"95-reference/40-governance.html#AIFloats.register_approx!","page":"Conformance","text":"register_approx!(name, op, FR, argformats, ρ, fn; κ = nothing, kwargs...) -> ApproxImpl\n\nRegister fn as the named κ-approximate implementation of op⟨argformats → FR, ρ⟩. κ is measured by enumeration (measure_kappa); a declared κ smaller than the measured value is rejected. Omitting κ declares the measured value. An implementation whose measured κ is NaN (non-finite mismatches) can be registered only by declaring κ = NaN.\n\n\n\n\n\n","title":"AIFloats.register_approx!"},{"category":"function","location":"95-reference/40-governance.html#AIFloats.unregister_approx!","page":"Conformance","text":"Remove a registered κ-approximate implementation (no-op if absent).\n\n\n\n\n\n","title":"AIFloats.unregister_approx!"},{"category":"type","location":"95-reference/40-governance.html#AIFloats.bfloat16","page":"Conformance","text":"bfloat16\n\nBFloat16s.BFloat16, the 16-bit brain float.\n\nNot an IEEE 754 format: it keeps Float32's 8 exponent bits and cuts the significand to 8, trading precision for range.\n\n\n\n\n\n","title":"AIFloats.bfloat16"},{"category":"type","location":"95-reference/40-governance.html#AIFloats.binary128","page":"Conformance","text":"binary128\n\nQuadmath.Float128 under its IEEE 754 name. See binary16.\n\nThis is also the type ValueType selects for formats wider than 10 bits.\n\n\n\n\n\n","title":"AIFloats.binary128"},{"category":"type","location":"95-reference/40-governance.html#AIFloats.binary16","page":"Conformance","text":"binary16\n\nFloat16 under its IEEE 754 name.\n\nProvided so that established formats can be named the same way P3109 formats are.\n\n\n\n\n\n","title":"AIFloats.binary16"},{"category":"type","location":"95-reference/40-governance.html#AIFloats.binary32","page":"Conformance","text":"binary32\n\nFloat32 under its IEEE 754 name. See binary16.\n\n\n\n\n\n","title":"AIFloats.binary32"},{"category":"type","location":"95-reference/40-governance.html#AIFloats.binary64","page":"Conformance","text":"binary64\n\nFloat64 under its IEEE 754 name. See binary16.\n\n\n\n\n\n","title":"AIFloats.binary64"},{"category":"module","location":"95-reference/40-governance.html#AIFloats.AIFloats","page":"Conformance","text":"AIFloats\n\nSmall binary floating-point formats for machine learning, following IEEE P3109.\n\nA format is completely determined by four parameters — bitwidth, precision, signedness, and domain — and Binary is the specifier that carries them. A member of a format's value set is a BinaryValue datum, stored as its code point. Projection names the rounding and saturation choice that maps a real onto a format's values.\n\nImplemented today: the format layer; datums and the code ⇆ value codec; the projection engine (project — the single write path, every rounding and saturation mode); and the draft's operation register (Add … Convert, correctly rounded via exact evaluation or interval enclosure); array kernels and memoized tables; and the Base surface (+, sort, round, promotion, the AbstractFloat contract); blocks (Block, BlockVector, the Block*/Scaled* operations) and packed storage (PackedVector). See the documentation's status page.\n\n\n\n\n\n","title":"AIFloats.AIFloats"},{"category":"type","location":"95-reference/40-governance.html#AIFloats.CarrierValue","page":"Conformance","text":"CarrierValue\n\nThe four types a decoded datum can be in flight: Float64, Float128, BigFloat, and the exact fixed-point AIFloats.Dyadic.\n\nWhich one a format uses is its rung (AIFloats.rung), chosen from the format's exponent span, not its storage width. Methods written against CarrierValue rather than AbstractFloat are exactly those that must also accept Dyadic, which is not an AbstractFloat.\n\nCarrier selection is an implementation detail and is not part of the stable interface: depend on decode, the register operations, and project instead of on a particular rung.\n\nNot exported; call it as AIFloats.CarrierValue.\n\n\n\n\n\n","title":"AIFloats.CarrierValue"},{"category":"type","location":"95-reference/40-governance.html#AIFloats.Enclosure","page":"Conformance","text":"Enclosure(f[, yd])\n\nA deferred enclosure of a true value. f(prec) returns directed MPFR endpoints (lo, hi) with the true value in [lo, hi] — the rigorous ladder. yd (NaN when absent) is a faithful Float64 estimate with |truth − yd| ≤ |yd|·2^-45, built only for Float64 operands. Float128 transcendentals are deliberately not used as an acceptance proof because libquadmath does not assure a suitable error bound. Not exported.\n\n\n\n\n\n","title":"AIFloats.Enclosure"},{"category":"constant","location":"95-reference/40-governance.html#AIFloats.FAST_ARITH","page":"Conformance","text":"FAST_ARITH :: Ref{Bool}\n\nWhether Group A operations may take their fast exactness route: direct exactness proofs on Float128 (twosum128, an fma residual) before escalating to exact BigFloat, plus the wide-spread Sticky escape past the alignment band.\n\nSetting it to false forces the rigorous route. Both routes are pinned equal by the test suite, so this changes speed and never results. It is a global diagnostic switch, not a per-call option — restore the previous value in a finally.\n\nNot exported; call it as AIFloats.FAST_ARITH[].\n\n\n\n\n\n","title":"AIFloats.FAST_ARITH"},{"category":"constant","location":"95-reference/40-governance.html#AIFloats.FAST_ENCLOSURE","page":"Conformance","text":"FAST_ENCLOSURE :: Ref{Bool}\n\nWhether Group B operations and the quotients may take their fast enclosure route: an eager Float64 estimate from a faithful libm (envelope 2^-45), accepted only when the two-sided sticky projection of its envelope agrees on one code point. Everything else runs the rigorous MPFR ladder.\n\nSetting it to false forces the ladder. Disabling it costs speed, never correctness — in particular no Quadmath transcendental is ever accepted as correctly rounded on its own. Restore the previous value in a finally.\n\nNot exported; call it as AIFloats.FAST_ENCLOSURE[].\n\n\n\n\n\n","title":"AIFloats.FAST_ENCLOSURE"},{"category":"type","location":"95-reference/40-governance.html#AIFloats.Sticky","page":"Conformance","text":"Sticky(v, sgn)\n\nA carrier value v whose true tail is known to lie strictly below its last bit, in the direction sgn (+1 above, -1 below, 0 exact).\n\nThis is the rung-3 Add protocol past the alignment band: rather than widen the carrier, the exact residue is summarized as a direction. It is sound because DYADIC_ALIGN_MAX (94) exceeds P + N + 2 for every P <= 16 and N <= 60, so the tail is below both the finest stochastic sub-grid unit and any rounding threshold — every §4.7.4 predicate is evaluated on the exact fraction ν + sgn·ε.\n\nNot exported; call it as AIFloats.Sticky.\n\n\n\n\n\n","title":"AIFloats.Sticky"},{"category":"function","location":"95-reference/40-governance.html#AIFloats.bigprec","page":"Conformance","text":"bigprec(F)\n\nWorking MPFR precision sufficient for exact arithmetic on F's datums: 2(B + P) + 64. Not exported.\n\n\n\n\n\n","title":"AIFloats.bigprec"},{"category":"function","location":"95-reference/40-governance.html#AIFloats.datumcarrier","page":"Conformance","text":"datumcarrier(F)\n\nThe type decode returns for format F: the narrowest carrier that represents every datum of F exactly and leaves room for two-factor products.\n\nFloat64 for B ≤ 512, Float128 for B ≤ 8192, the exact Dyadic carrier above (a Real, not an AbstractFloat). Internal counterpart of the public ValueType; evaluation decisions use this, never ValueType. Not exported.\n\n\n\n\n\n","title":"AIFloats.datumcarrier"},{"category":"function","location":"95-reference/40-governance.html#AIFloats.decodepolicy","page":"Conformance","text":"decodepolicy(F)\n\nTableDecode() for UInt8-coded formats (a 2^K constant tuple), otherwise ComputeDecode() — a 65,536-entry tuple literal is not a table, it is a compile-time hazard. Not exported.\n\n\n\n\n\n","title":"AIFloats.decodepolicy"},{"category":"function","location":"95-reference/40-governance.html#AIFloats.Float128FAA.faa128","page":"Conformance","text":"faa(x::Float128, y::Float128, z::Float128) -> Float128\nfaa128(x, y, z)\n\nCorrectly rounded (round-to-nearest, ties-to-even) x + y + z with a single rounding, computed in pure Julia integer arithmetic. Handles signed zeros, subnormals (gradual underflow), infinities, and NaN payload propagation with the same semantics as libquadmath addition applied to (x + y) + z — except that, being fused, only actual input infinities produce an infinite result.\n\n\n\n\n\n","title":"AIFloats.Float128FAA.faa128"},{"category":"function","location":"95-reference/40-governance.html#AIFloats.Float128FMA.fma128","page":"Conformance","text":"fma128(x::Float128, y::Float128, z::Float128) -> Float128\n\nCorrectly rounded (round-to-nearest, ties-to-even) x*y + z with a single rounding, computed in pure Julia integer arithmetic; bit-for-bit compatible with libquadmath's fmaq, including signed zeros, subnormals (gradual underflow), infinities, NaN payload propagation, and invalid-operation NaN generation.\n\n\n\n\n\n","title":"AIFloats.Float128FMA.fma128"},{"category":"function","location":"95-reference/40-governance.html#AIFloats.lift","page":"Conformance","text":"lift(h::Head, x) -> carriertype(h)\n\nMove a carrier value up to the rung h, exactly. No narrowing method exists, by design. Not exported.\n\n\n\n\n\n","title":"AIFloats.lift"},{"category":"function","location":"95-reference/40-governance.html#AIFloats.promotecarrier","page":"Conformance","text":"promotecarrier(F)\n\nThe public promotion target for format F against external numeric types — what x + 1.0 promotes to. Always a Julia float with the complete Real interface: Float64 for B ≤ 512, Float128 for B ≤ 8192, BigFloat above. Never the internal exact carrier. Not exported.\n\n\n\n\n\n","title":"AIFloats.promotecarrier"},{"category":"function","location":"95-reference/40-governance.html#AIFloats.rung","page":"Conformance","text":"rung(F)\n\nThe carrier rung of format F — the tag naming the narrowest carrier whose exponent range holds any two-factor monomial of F's datums exactly.\n\nNot exported; call it as AIFloats.rung.\n\n\n\n\n\nrung(op::Val, Fs::Type{<:Binary}...) -> Head\n\nThe carrier for evaluating op over operands of formats Fs: the JOIN of the operand bound (no head is narrower than any operand's own rung — decode has already produced that carrier) and the monomial bound (_rungindex_span(opfactors(op) · maxB)). Omitting the operand bound is the recorded subtle error. For one format at two factors this equals rung(F).\n\n\n\n\n\n","title":"AIFloats.rung"},{"category":"section","location":"95-reference/20-operations.html#Operations,-kernels,-storage","page":"Operations, kernels, storage","text":"Part of the Reference; see that page for how this listing is generated and what it guarantees.\n\nPages = [\"20-operations.md\"]\nDepth = 2","title":"Operations, kernels, storage"},{"category":"section","location":"95-reference/20-operations.html#Scalar-operations","page":"Operations, kernels, storage","text":"","title":"Scalar operations"},{"category":"section","location":"95-reference/20-operations.html#Array-kernels-and-tables","page":"Operations, kernels, storage","text":"","title":"Array kernels and tables"},{"category":"section","location":"95-reference/20-operations.html#Packed-storage","page":"Operations, kernels, storage","text":"","title":"Packed storage"},{"category":"section","location":"95-reference/20-operations.html#Index","page":"Operations, kernels, storage","text":"Pages = [\"20-operations.md\"]","title":"Index"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.Abs","page":"Operations, kernels, storage","text":"Abs(fr, ρ, x1; rng, R)\nAbs(x1)\n\nThe P3109 register operation Abs, over 1 datum operand.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated exactly (an error-free transform or an exact escalation), then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:Abs, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockAbs and ScaledAbs. Registry metadata is AIFloats.operationinfo(:Abs).\n\n\n\n\n\n","title":"AIFloats.Abs"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.Add","page":"Operations, kernels, storage","text":"Add(fr, ρ, x1, x2; rng, R)\nAdd(x1, x2)\n\nThe P3109 register operation Add, over 2 datum operands.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated exactly (an error-free transform or an exact escalation), then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:Add, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockAdd and ScaledAdd. Registry metadata is AIFloats.operationinfo(:Add).\n\n\n\n\n\n","title":"AIFloats.Add"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.ArcCos","page":"Operations, kernels, storage","text":"ArcCos(fr, ρ, x1; rng, R)\nArcCos(x1)\n\nThe P3109 register operation ArcCos, over 1 datum operand.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated through the correctly rounded interval-enclosure ladder, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:ArcCos, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockArcCos and ScaledArcCos. Registry metadata is AIFloats.operationinfo(:ArcCos).\n\n\n\n\n\n","title":"AIFloats.ArcCos"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.ArcCosPi","page":"Operations, kernels, storage","text":"ArcCosPi(fr, ρ, x1; rng, R)\nArcCosPi(x1)\n\nThe P3109 register operation ArcCosPi, over 1 datum operand.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated through the correctly rounded interval-enclosure ladder, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:ArcCosPi, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockArcCosPi and ScaledArcCosPi. Registry metadata is AIFloats.operationinfo(:ArcCosPi).\n\n\n\n\n\n","title":"AIFloats.ArcCosPi"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.ArcCosh","page":"Operations, kernels, storage","text":"ArcCosh(fr, ρ, x1; rng, R)\nArcCosh(x1)\n\nThe P3109 register operation ArcCosh, over 1 datum operand.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated through the correctly rounded interval-enclosure ladder, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:ArcCosh, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockArcCosh and ScaledArcCosh. Registry metadata is AIFloats.operationinfo(:ArcCosh).\n\n\n\n\n\n","title":"AIFloats.ArcCosh"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.ArcSin","page":"Operations, kernels, storage","text":"ArcSin(fr, ρ, x1; rng, R)\nArcSin(x1)\n\nThe P3109 register operation ArcSin, over 1 datum operand.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated through the correctly rounded interval-enclosure ladder, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:ArcSin, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockArcSin and ScaledArcSin. Registry metadata is AIFloats.operationinfo(:ArcSin).\n\n\n\n\n\n","title":"AIFloats.ArcSin"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.ArcSinPi","page":"Operations, kernels, storage","text":"ArcSinPi(fr, ρ, x1; rng, R)\nArcSinPi(x1)\n\nThe P3109 register operation ArcSinPi, over 1 datum operand.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated through the correctly rounded interval-enclosure ladder, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:ArcSinPi, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockArcSinPi and ScaledArcSinPi. Registry metadata is AIFloats.operationinfo(:ArcSinPi).\n\n\n\n\n\n","title":"AIFloats.ArcSinPi"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.ArcSinh","page":"Operations, kernels, storage","text":"ArcSinh(fr, ρ, x1; rng, R)\nArcSinh(x1)\n\nThe P3109 register operation ArcSinh, over 1 datum operand.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated through the correctly rounded interval-enclosure ladder, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:ArcSinh, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockArcSinh and ScaledArcSinh. Registry metadata is AIFloats.operationinfo(:ArcSinh).\n\n\n\n\n\n","title":"AIFloats.ArcSinh"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.ArcTan","page":"Operations, kernels, storage","text":"ArcTan(fr, ρ, x1; rng, R)\nArcTan(x1)\n\nThe P3109 register operation ArcTan, over 1 datum operand.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated through the correctly rounded interval-enclosure ladder, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:ArcTan, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockArcTan and ScaledArcTan. Registry metadata is AIFloats.operationinfo(:ArcTan).\n\n\n\n\n\n","title":"AIFloats.ArcTan"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.ArcTan2","page":"Operations, kernels, storage","text":"ArcTan2(fr, ρ, x1, x2; rng, R)\nArcTan2(x1, x2)\n\nThe P3109 register operation ArcTan2, over 2 datum operands.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated through the correctly rounded interval-enclosure ladder, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:ArcTan2, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockArcTan2 and ScaledArcTan2. Registry metadata is AIFloats.operationinfo(:ArcTan2).\n\n\n\n\n\n","title":"AIFloats.ArcTan2"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.ArcTan2Pi","page":"Operations, kernels, storage","text":"ArcTan2Pi(fr, ρ, x1, x2; rng, R)\nArcTan2Pi(x1, x2)\n\nThe P3109 register operation ArcTan2Pi, over 2 datum operands.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated through the correctly rounded interval-enclosure ladder, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:ArcTan2Pi, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockArcTan2Pi and ScaledArcTan2Pi. Registry metadata is AIFloats.operationinfo(:ArcTan2Pi).\n\n\n\n\n\n","title":"AIFloats.ArcTan2Pi"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.ArcTanPi","page":"Operations, kernels, storage","text":"ArcTanPi(fr, ρ, x1; rng, R)\nArcTanPi(x1)\n\nThe P3109 register operation ArcTanPi, over 1 datum operand.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated through the correctly rounded interval-enclosure ladder, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:ArcTanPi, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockArcTanPi and ScaledArcTanPi. Registry metadata is AIFloats.operationinfo(:ArcTanPi).\n\n\n\n\n\n","title":"AIFloats.ArcTanPi"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.ArcTanh","page":"Operations, kernels, storage","text":"ArcTanh(fr, ρ, x1; rng, R)\nArcTanh(x1)\n\nThe P3109 register operation ArcTanh, over 1 datum operand.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated through the correctly rounded interval-enclosure ladder, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:ArcTanh, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockArcTanh and ScaledArcTanh. Registry metadata is AIFloats.operationinfo(:ArcTanh).\n\n\n\n\n\n","title":"AIFloats.ArcTanh"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.Clamp","page":"Operations, kernels, storage","text":"Clamp(fr, ρ, x1, x2, x3; rng, R)\nClamp(x1, x2, x3)\n\nThe P3109 register operation Clamp, over 3 datum operands.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated exactly (an error-free transform or an exact escalation), then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:Clamp, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockClamp and ScaledClamp. Registry metadata is AIFloats.operationinfo(:Clamp).\n\n\n\n\n\n","title":"AIFloats.Clamp"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.Convert","page":"Operations, kernels, storage","text":"Convert(F, ρ, x; rng, R) -> BinaryValue\nConvert(F, ρ, A::AbstractArray; rng) -> Array\nConvert(F, A::AbstractArray; rng) -> Array\n\nProject x into format F under projection ρ — the draft's Convert.\n\nThe accepted sources are the closed set AIFloats.ConvertSource: a BinaryValue of any format, Float64/Float32/Float16/BFloat16 (exact widening), Float128, BigFloat, or any Integer (widened exactly). Every one of them reaches the same _convert_value seam, so the scalar and array surfaces cannot disagree about which carrier a value takes.\n\nUnsigned is not special: Convert(F, ρ, 0x03) projects the number three. For the datum at code point three, write fromcode(F, 3).\n\nRational and Irrational inputs are refused: their exact projection is not representable as a single rounding of a float, and silently double-rounding would be a lie. An array whose element type is outside the accepted set is refused for the same reason, rather than hiding a dynamic dispatch and an unproved double rounding inside the element loop.\n\nThe array form allocates with similar, preserving axes and shape, resolves the RNG once, and draws exactly once per element in eachindex order. The two-argument form resolves the task's DefaultProjection once, before the loop.\n\nExamples\n\njulia> Convert(Binary(8, 4, SIGNED, EXTENDED), RTE_SF, 1.6)\n1.625\n\njulia> F = Binary8p4se;\n\njulia> codepoint.(Convert(F, RTE_SN, [1.6, 0.25])) == [codepoint(Convert(F, RTE_SN, x)) for x in (1.6, 0.25)]\ntrue\n\n\n\n\n\n","title":"AIFloats.Convert"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.CopySign","page":"Operations, kernels, storage","text":"CopySign(fr, ρ, x1, x2; rng, R)\nCopySign(x1, x2)\n\nThe P3109 register operation CopySign, over 2 datum operands.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is an exact selection among the operands, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:CopySign, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockCopySign and ScaledCopySign. Registry metadata is AIFloats.operationinfo(:CopySign).\n\n\n\n\n\n","title":"AIFloats.CopySign"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.Cos","page":"Operations, kernels, storage","text":"Cos(fr, ρ, x1; rng, R)\nCos(x1)\n\nThe P3109 register operation Cos, over 1 datum operand.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated through the correctly rounded interval-enclosure ladder, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:Cos, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockCos and ScaledCos. Registry metadata is AIFloats.operationinfo(:Cos).\n\n\n\n\n\n","title":"AIFloats.Cos"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.CosPi","page":"Operations, kernels, storage","text":"CosPi(fr, ρ, x1; rng, R)\nCosPi(x1)\n\nThe P3109 register operation CosPi, over 1 datum operand.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated through the correctly rounded interval-enclosure ladder, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:CosPi, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockCosPi and ScaledCosPi. Registry metadata is AIFloats.operationinfo(:CosPi).\n\n\n\n\n\n","title":"AIFloats.CosPi"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.Cosh","page":"Operations, kernels, storage","text":"Cosh(fr, ρ, x1; rng, R)\nCosh(x1)\n\nThe P3109 register operation Cosh, over 1 datum operand.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated through the correctly rounded interval-enclosure ladder, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:Cosh, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockCosh and ScaledCosh. Registry metadata is AIFloats.operationinfo(:Cosh).\n\n\n\n\n\n","title":"AIFloats.Cosh"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.Divide","page":"Operations, kernels, storage","text":"Divide(fr, ρ, x1, x2; rng, R)\nDivide(x1, x2)\n\nThe P3109 register operation Divide, over 2 datum operands.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated through the correctly rounded interval-enclosure ladder, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:Divide, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockDivide and ScaledDivide. Registry metadata is AIFloats.operationinfo(:Divide).\n\n\n\n\n\n","title":"AIFloats.Divide"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.Exp","page":"Operations, kernels, storage","text":"Exp(fr, ρ, x1; rng, R)\nExp(x1)\n\nThe P3109 register operation Exp, over 1 datum operand.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated through the correctly rounded interval-enclosure ladder, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:Exp, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockExp and ScaledExp. Registry metadata is AIFloats.operationinfo(:Exp).\n\n\n\n\n\n","title":"AIFloats.Exp"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.Exp2","page":"Operations, kernels, storage","text":"Exp2(fr, ρ, x1; rng, R)\nExp2(x1)\n\nThe P3109 register operation Exp2, over 1 datum operand.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated through the correctly rounded interval-enclosure ladder, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:Exp2, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockExp2 and ScaledExp2. Registry metadata is AIFloats.operationinfo(:Exp2).\n\n\n\n\n\n","title":"AIFloats.Exp2"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.ExpMinusOne","page":"Operations, kernels, storage","text":"ExpMinusOne(fr, ρ, x1; rng, R)\nExpMinusOne(x1)\n\nThe P3109 register operation ExpMinusOne, over 1 datum operand.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated through the correctly rounded interval-enclosure ladder, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:ExpMinusOne, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockExpMinusOne and ScaledExpMinusOne. Registry metadata is AIFloats.operationinfo(:ExpMinusOne).\n\n\n\n\n\n","title":"AIFloats.ExpMinusOne"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.FAA","page":"Operations, kernels, storage","text":"FAA(fr, ρ, x1, x2, x3; rng, R)\nFAA(x1, x2, x3)\n\nThe P3109 register operation FAA, over 3 datum operands.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated exactly (an error-free transform or an exact escalation), then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:FAA, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockFAA and ScaledFAA. Registry metadata is AIFloats.operationinfo(:FAA).\n\n\n\n\n\n","title":"AIFloats.FAA"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.FMA","page":"Operations, kernels, storage","text":"FMA(fr, ρ, x1, x2, x3; rng, R)\nFMA(x1, x2, x3)\n\nThe P3109 register operation FMA, over 3 datum operands.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated exactly (an error-free transform or an exact escalation), then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:FMA, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockFMA and ScaledFMA. Registry metadata is AIFloats.operationinfo(:FMA).\n\n\n\n\n\n","title":"AIFloats.FMA"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.Hypot","page":"Operations, kernels, storage","text":"Hypot(fr, ρ, x1, x2; rng, R)\nHypot(x1, x2)\n\nThe P3109 register operation Hypot, over 2 datum operands.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated through the correctly rounded interval-enclosure ladder, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:Hypot, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockHypot and ScaledHypot. Registry metadata is AIFloats.operationinfo(:Hypot).\n\n\n\n\n\n","title":"AIFloats.Hypot"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.Log","page":"Operations, kernels, storage","text":"Log(fr, ρ, x1; rng, R)\nLog(x1)\n\nThe P3109 register operation Log, over 1 datum operand.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated through the correctly rounded interval-enclosure ladder, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:Log, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockLog and ScaledLog. Registry metadata is AIFloats.operationinfo(:Log).\n\n\n\n\n\n","title":"AIFloats.Log"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.Log2","page":"Operations, kernels, storage","text":"Log2(fr, ρ, x1; rng, R)\nLog2(x1)\n\nThe P3109 register operation Log2, over 1 datum operand.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated through the correctly rounded interval-enclosure ladder, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:Log2, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockLog2 and ScaledLog2. Registry metadata is AIFloats.operationinfo(:Log2).\n\n\n\n\n\n","title":"AIFloats.Log2"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.LogOnePlus","page":"Operations, kernels, storage","text":"LogOnePlus(fr, ρ, x1; rng, R)\nLogOnePlus(x1)\n\nThe P3109 register operation LogOnePlus, over 1 datum operand.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated through the correctly rounded interval-enclosure ladder, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:LogOnePlus, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockLogOnePlus and ScaledLogOnePlus. Registry metadata is AIFloats.operationinfo(:LogOnePlus).\n\n\n\n\n\n","title":"AIFloats.LogOnePlus"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.Maximum","page":"Operations, kernels, storage","text":"Maximum(fr, ρ, x1, x2; rng, R)\nMaximum(x1, x2)\n\nThe P3109 register operation Maximum, over 2 datum operands.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is an exact selection among the operands, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:Maximum, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockMaximum and ScaledMaximum. Registry metadata is AIFloats.operationinfo(:Maximum).\n\n\n\n\n\n","title":"AIFloats.Maximum"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.MaximumFinite","page":"Operations, kernels, storage","text":"MaximumFinite(fr, ρ, x1, x2; rng, R)\nMaximumFinite(x1, x2)\n\nThe P3109 register operation MaximumFinite, over 2 datum operands.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is an exact selection among the operands, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:MaximumFinite, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockMaximumFinite and ScaledMaximumFinite. Registry metadata is AIFloats.operationinfo(:MaximumFinite).\n\n\n\n\n\n","title":"AIFloats.MaximumFinite"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.MaximumMagnitude","page":"Operations, kernels, storage","text":"MaximumMagnitude(fr, ρ, x1, x2; rng, R)\nMaximumMagnitude(x1, x2)\n\nThe P3109 register operation MaximumMagnitude, over 2 datum operands.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is an exact selection among the operands, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:MaximumMagnitude, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockMaximumMagnitude and ScaledMaximumMagnitude. Registry metadata is AIFloats.operationinfo(:MaximumMagnitude).\n\n\n\n\n\n","title":"AIFloats.MaximumMagnitude"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.MaximumMagnitudeNumber","page":"Operations, kernels, storage","text":"MaximumMagnitudeNumber(fr, ρ, x1, x2; rng, R)\nMaximumMagnitudeNumber(x1, x2)\n\nThe P3109 register operation MaximumMagnitudeNumber, over 2 datum operands.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is an exact selection among the operands, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:MaximumMagnitudeNumber, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockMaximumMagnitudeNumber and ScaledMaximumMagnitudeNumber. Registry metadata is AIFloats.operationinfo(:MaximumMagnitudeNumber).\n\n\n\n\n\n","title":"AIFloats.MaximumMagnitudeNumber"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.MaximumNumber","page":"Operations, kernels, storage","text":"MaximumNumber(fr, ρ, x1, x2; rng, R)\nMaximumNumber(x1, x2)\n\nThe P3109 register operation MaximumNumber, over 2 datum operands.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is an exact selection among the operands, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:MaximumNumber, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockMaximumNumber and ScaledMaximumNumber. Registry metadata is AIFloats.operationinfo(:MaximumNumber).\n\n\n\n\n\n","title":"AIFloats.MaximumNumber"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.Minimum","page":"Operations, kernels, storage","text":"Minimum(fr, ρ, x1, x2; rng, R)\nMinimum(x1, x2)\n\nThe P3109 register operation Minimum, over 2 datum operands.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is an exact selection among the operands, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:Minimum, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockMinimum and ScaledMinimum. Registry metadata is AIFloats.operationinfo(:Minimum).\n\n\n\n\n\n","title":"AIFloats.Minimum"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.MinimumFinite","page":"Operations, kernels, storage","text":"MinimumFinite(fr, ρ, x1, x2; rng, R)\nMinimumFinite(x1, x2)\n\nThe P3109 register operation MinimumFinite, over 2 datum operands.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is an exact selection among the operands, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:MinimumFinite, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockMinimumFinite and ScaledMinimumFinite. Registry metadata is AIFloats.operationinfo(:MinimumFinite).\n\n\n\n\n\n","title":"AIFloats.MinimumFinite"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.MinimumMagnitude","page":"Operations, kernels, storage","text":"MinimumMagnitude(fr, ρ, x1, x2; rng, R)\nMinimumMagnitude(x1, x2)\n\nThe P3109 register operation MinimumMagnitude, over 2 datum operands.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is an exact selection among the operands, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:MinimumMagnitude, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockMinimumMagnitude and ScaledMinimumMagnitude. Registry metadata is AIFloats.operationinfo(:MinimumMagnitude).\n\n\n\n\n\n","title":"AIFloats.MinimumMagnitude"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.MinimumMagnitudeNumber","page":"Operations, kernels, storage","text":"MinimumMagnitudeNumber(fr, ρ, x1, x2; rng, R)\nMinimumMagnitudeNumber(x1, x2)\n\nThe P3109 register operation MinimumMagnitudeNumber, over 2 datum operands.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is an exact selection among the operands, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:MinimumMagnitudeNumber, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockMinimumMagnitudeNumber and ScaledMinimumMagnitudeNumber. Registry metadata is AIFloats.operationinfo(:MinimumMagnitudeNumber).\n\n\n\n\n\n","title":"AIFloats.MinimumMagnitudeNumber"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.MinimumNumber","page":"Operations, kernels, storage","text":"MinimumNumber(fr, ρ, x1, x2; rng, R)\nMinimumNumber(x1, x2)\n\nThe P3109 register operation MinimumNumber, over 2 datum operands.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is an exact selection among the operands, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:MinimumNumber, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockMinimumNumber and ScaledMinimumNumber. Registry metadata is AIFloats.operationinfo(:MinimumNumber).\n\n\n\n\n\n","title":"AIFloats.MinimumNumber"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.Multiply","page":"Operations, kernels, storage","text":"Multiply(fr, ρ, x1, x2; rng, R)\nMultiply(x1, x2)\n\nThe P3109 register operation Multiply, over 2 datum operands.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated exactly (an error-free transform or an exact escalation), then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:Multiply, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockMultiply and ScaledMultiply. Registry metadata is AIFloats.operationinfo(:Multiply).\n\n\n\n\n\n","title":"AIFloats.Multiply"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.Negate","page":"Operations, kernels, storage","text":"Negate(fr, ρ, x1; rng, R)\nNegate(x1)\n\nThe P3109 register operation Negate, over 1 datum operand.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated exactly (an error-free transform or an exact escalation), then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:Negate, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockNegate and ScaledNegate. Registry metadata is AIFloats.operationinfo(:Negate).\n\n\n\n\n\n","title":"AIFloats.Negate"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.RSqrt","page":"Operations, kernels, storage","text":"RSqrt(fr, ρ, x1; rng, R)\nRSqrt(x1)\n\nThe P3109 register operation RSqrt, over 1 datum operand.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated through the correctly rounded interval-enclosure ladder, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:RSqrt, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockRSqrt and ScaledRSqrt. Registry metadata is AIFloats.operationinfo(:RSqrt).\n\n\n\n\n\n","title":"AIFloats.RSqrt"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.Recip","page":"Operations, kernels, storage","text":"Recip(fr, ρ, x1; rng, R)\nRecip(x1)\n\nThe P3109 register operation Recip, over 1 datum operand.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated through the correctly rounded interval-enclosure ladder, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:Recip, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockRecip and ScaledRecip. Registry metadata is AIFloats.operationinfo(:Recip).\n\n\n\n\n\n","title":"AIFloats.Recip"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.Sin","page":"Operations, kernels, storage","text":"Sin(fr, ρ, x1; rng, R)\nSin(x1)\n\nThe P3109 register operation Sin, over 1 datum operand.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated through the correctly rounded interval-enclosure ladder, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:Sin, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockSin and ScaledSin. Registry metadata is AIFloats.operationinfo(:Sin).\n\n\n\n\n\n","title":"AIFloats.Sin"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.SinPi","page":"Operations, kernels, storage","text":"SinPi(fr, ρ, x1; rng, R)\nSinPi(x1)\n\nThe P3109 register operation SinPi, over 1 datum operand.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated through the correctly rounded interval-enclosure ladder, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:SinPi, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockSinPi and ScaledSinPi. Registry metadata is AIFloats.operationinfo(:SinPi).\n\n\n\n\n\n","title":"AIFloats.SinPi"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.Sinh","page":"Operations, kernels, storage","text":"Sinh(fr, ρ, x1; rng, R)\nSinh(x1)\n\nThe P3109 register operation Sinh, over 1 datum operand.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated through the correctly rounded interval-enclosure ladder, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:Sinh, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockSinh and ScaledSinh. Registry metadata is AIFloats.operationinfo(:Sinh).\n\n\n\n\n\n","title":"AIFloats.Sinh"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.Softplus","page":"Operations, kernels, storage","text":"Softplus(fr, ρ, x1; rng, R)\nSoftplus(x1)\n\nThe P3109 register operation Softplus, over 1 datum operand.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated through the correctly rounded interval-enclosure ladder, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:Softplus, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockSoftplus and ScaledSoftplus. Registry metadata is AIFloats.operationinfo(:Softplus).\n\n\n\n\n\n","title":"AIFloats.Softplus"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.Sqrt","page":"Operations, kernels, storage","text":"Sqrt(fr, ρ, x1; rng, R)\nSqrt(x1)\n\nThe P3109 register operation Sqrt, over 1 datum operand.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated through the correctly rounded interval-enclosure ladder, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:Sqrt, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockSqrt and ScaledSqrt. Registry metadata is AIFloats.operationinfo(:Sqrt).\n\n\n\n\n\n","title":"AIFloats.Sqrt"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.Subtract","page":"Operations, kernels, storage","text":"Subtract(fr, ρ, x1, x2; rng, R)\nSubtract(x1, x2)\n\nThe P3109 register operation Subtract, over 2 datum operands.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated exactly (an error-free transform or an exact escalation), then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:Subtract, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockSubtract and ScaledSubtract. Registry metadata is AIFloats.operationinfo(:Subtract).\n\n\n\n\n\n","title":"AIFloats.Subtract"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.Tan","page":"Operations, kernels, storage","text":"Tan(fr, ρ, x1; rng, R)\nTan(x1)\n\nThe P3109 register operation Tan, over 1 datum operand.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated through the correctly rounded interval-enclosure ladder, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:Tan, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockTan and ScaledTan. Registry metadata is AIFloats.operationinfo(:Tan).\n\n\n\n\n\n","title":"AIFloats.Tan"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.TanPi","page":"Operations, kernels, storage","text":"TanPi(fr, ρ, x1; rng, R)\nTanPi(x1)\n\nThe P3109 register operation TanPi, over 1 datum operand.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated through the correctly rounded interval-enclosure ladder, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:TanPi, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockTanPi and ScaledTanPi. Registry metadata is AIFloats.operationinfo(:TanPi).\n\n\n\n\n\n","title":"AIFloats.TanPi"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.Tanh","page":"Operations, kernels, storage","text":"Tanh(fr, ρ, x1; rng, R)\nTanh(x1)\n\nThe P3109 register operation Tanh, over 1 datum operand.\n\nThe first form is the draft's shape: fr is the result format (a Binary format type, a datum type, or an alias) and ρ is the Projection. Operands may be datums of any formats; each is decoded onto a carrier wide enough for the exact result. The result is evaluated through the correctly rounded interval-enclosure ladder, then projected once, so the returned datum is the correctly rounded one for ρ.\n\nThe second form takes same-format operands and resolves the task-local DefaultProjection once, returning that same format. It is a convenience, not a second semantics.\n\nrng and R are consulted only under a stochastic ρ: R supplies the random bits directly and takes precedence over rng; with neither, a stochastic projection draws from Random.default_rng(). A pure projection touches no RNG state.\n\nElementwise over arrays, the same operation is vmap(:Tanh, fr, ρ, A...); see vmap!. Blocked and scaled forms are BlockTanh and ScaledTanh. Registry metadata is AIFloats.operationinfo(:Tanh).\n\n\n\n\n\n","title":"AIFloats.Tanh"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.operationinfo","page":"Operations, kernels, storage","text":"operationinfo(op::Symbol) -> NamedTuple\n\nWhat the registry knows about op: (name, arity, group, factors).\n\narity is the operand count.\ngroup is :A (exact arithmetic and selection), :B (enclosure), :C (extremum), or :conv.\nfactors is the largest number of datum factors in any monomial of the exact result — the carrier-width driver, and not the arity.\n\nWhat factors counts\n\nWrite the operation's exact result as monomials combined by + and -. A monomial is a product of datums: x, x * y, or x^p for a nonnegative integer p. factors is the greatest number of datum factors any one of those monomials carries — a maximum over the monomials, not a total across them.\n\nOperation Exact result Monomials factors\nAdd x + y x, y 1\nFAA x + y + z x, y, z 1\nMultiply x * y x * y 2\nFMA x * y + z x * y, z 2\n\nFMA is the case worth reading twice: the + z adds a monomial, not a factor, so the answer is max(2, 1) and not 3. FAA is the mirror image — three operands, three monomials, one factor apiece.\n\nThat distinction is the point, because it is what carrier width turns on. Multiplying two datums roughly doubles the exponent span; adding a term can at most double the magnitude, which is one binade and cannot cross a rung threshold on its own. So the widest single monomial decides the carrier, and rung multiplies the largest operand bias by exactly this number.\n\nnote: Not every exact result is a polynomial\n1/x^2 is a rational expression, not a monomial, and sqrt(x) is neither. Operations whose exact result leaves the polynomials — the quotients and everything in group :B — do not reach a carrier wide enough to hold that result, because in general no such carrier exists. They are evaluated by the interval-enclosure ladder instead, and factors bounds only the operand arithmetic feeding it.\n\nThrows ArgumentError naming operations for an unknown symbol.\n\nExamples\n\njulia> AIFloats.operationinfo(:Add)\n(name = :Add, arity = 2, group = :A, factors = 1)\n\njulia> AIFloats.operationinfo(:FAA).factors     # three operands, one factor\n1\n\njulia> AIFloats.operationinfo(:FMA).factors     # x*y + z: the x*y monomial\n2\n\n\n\n\n\n","title":"AIFloats.operationinfo"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.operations","page":"Operations, kernels, storage","text":"operations() -> Vector{NamedTuple}\n\nEvery registry operation, sorted by name — a deterministic listing, never registration order.\n\n\n\n\n\n","title":"AIFloats.operations"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.empty_tables!","page":"Operations, kernels, storage","text":"Drop every cached table and adaptive counter (they rebuild lazily on next use).\n\n\n\n\n\n","title":"AIFloats.empty_tables!"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.table_entries","page":"Operations, kernels, storage","text":"table_entries() -> Vector{AIFloats.TableEntry}\n\nPer-entry detail for every cached table, taken under a single lock and sorted by operation, arity, result format, operand formats, rounding, then saturation.\n\nEach element is a public named tuple naming FORMAT TYPES, not the internal key struct:\n\n(op, arity, result, operands, rounding, saturation, bytes)\n\nThe bytes field sums to table_stats().bytes.\n\n\n\n\n\n","title":"AIFloats.table_entries"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.table_policy","page":"Operations, kernels, storage","text":"table_policy(op, fr, f1[, f2[, f3]], ρ; nelems=0)\n\nReturn shape, sizes, and the adaptive state the matching kernel policy would observe. nelems is the size of a prospective array call; introspection reads but never increments the cumulative-use counter.\n\nWhich shape an array call on this signature will take, and why — :A for the table gather, :B for the per-element scalar path. Reads the same predicates the kernels do, so it cannot drift from them.\n\n\n\n\n\n","title":"AIFloats.table_policy"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.table_stats","page":"Operations, kernels, storage","text":"table_stats() -> NamedTuple\n\nOne coherent snapshot of the table caches, taken under a single lock.\n\n(entries, bytes, by_arity = (unary, binary, ternary),\n by_codeunit = (uint8, uint16), adaptive_signatures, ternary_budget)\n\nentries is the number of cached specializations and bytes the memory they hold; the by_arity and by_codeunit breakdowns each sum to entries. adaptive_signatures counts signatures with a nonzero cumulative-use counter, which is what the adaptive band builds against. ternary_budget is the current per-table byte cap.\n\nFor per-entry detail, see table_entries; to clear, empty_tables!.\n\nExamples\n\njulia> AIFloats.empty_tables!();\n\njulia> AIFloats.table_stats().entries\n0\n\n\n\n\n\n","title":"AIFloats.table_stats"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.vmap","page":"Operations, kernels, storage","text":"vmap(op::Symbol, fr, ρ, A[, B[, C]]; rng) -> Array\n\nElementwise draft operation over arrays of datums, allocating the result.\n\nop is a registry operation name (AIFloats.operations() lists them); fr is the result format, and the returned array has element type BinaryValue(fr) and A's axes. Operand arrays must share those axes. This is the array Seam: the same operation spelled Add(fr, ρ, x, y) for scalars is vmap(:Add, fr, ρ, A, B) over arrays.\n\nρ is resolved once per call, never per element. A pure ρ gathers from a memoized table whenever table_policy grants one and otherwise computes per element — the same answer either way. A stochastic ρ is never tabled and runs a sequential loop that draws from one rng stream in eachindex order, so a seeded call is reproducible and independent of the scheduler. There is no per-element R.\n\nUse vmap! to write into an existing array. A PackedVector operand is accepted for unary operations and produces an ordinary vector.\n\nExamples\n\njulia> F = Binary8p4se; T = BinaryValue(F);\n\njulia> A = T[0.5, 1.0]; B = T[1.0, 2.0];\n\njulia> vmap(:Add, F, RTE_SN, A, B)\n2-element Vector{BinaryValue(Binary8p4se)}:\n 1.5\n 3.0\n\n\n\n\n\n","title":"AIFloats.vmap"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.vmap!","page":"Operations, kernels, storage","text":"vmap!(dest, op::Symbol, ρ, A[, B[, C]]; rng) -> dest\n\nElementwise draft operation over arrays of datums, projecting into dest's (or fr's) format under ρ. Pure ρ runs the Shape-A table gather whenever policy grants a table; stochastic ρ (and untabled signatures) run the scalar path per element. The stochastic loop is sequential by construction — it draws from a single rng stream, so seeded results are reproducible in index order and never depend on the scheduler.\n\n\n\n\n\n","title":"AIFloats.vmap!"},{"category":"constant","location":"95-reference/20-operations.html#AIFloats.PACK_TILE","page":"Operations, kernels, storage","text":"PACK_TILE\n\nElements per tile when a kernel runs over PackedVector storage: codes are unpacked into a scratch buffer of this length, the ordinary array kernel runs over the scratch, and the loop repeats.\n\nThe tile keeps the scratch in cache while still amortizing the per-call kernel setup. It is a performance constant with no effect on results.\n\nNot exported; call it as AIFloats.PACK_TILE.\n\n\n\n\n\n","title":"AIFloats.PACK_TILE"},{"category":"type","location":"95-reference/20-operations.html#AIFloats.PackedVector","page":"Operations, kernels, storage","text":"PackedVector{T<:BinaryValue} <: AbstractVector{T}\n\nBit-packed storage of datums at K = BitwidthOf(T) bits per element. Construct with PackedVector(A::AbstractVector{T}); recover with Vector(pv) or collect(pv). Memory: ⌈n·K/64⌉ words against n · sizeof(CodeType(T)) unpacked.\n\nIt saves nothing when K equals the storage unit's width (K = 8 and K = 16), where the packed layout is bit-for-bit the unpacked one; packing_saves says so — a query, not a refusal, because both widths are equally the identity and remain correct.\n\n\n\n\n\n","title":"AIFloats.PackedVector"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.packedbytes","page":"Operations, kernels, storage","text":"packedbytes(pv) -> Vector{UInt8}\n\nThe canonical little-endian wire form of pv: exactly cld(n*K, 8) bytes, with byte j holding bits 8j .. 8j+7 of the bit stream and the unused high bits of the final byte zero.\n\nShorter than packedwords whenever the payload does not fill its last word, and byte-order independent on every host. Inverse of packedfrombytes.\n\n\n\n\n\n","title":"AIFloats.packedbytes"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.packedfrombytes","page":"Operations, kernels, storage","text":"packedfrombytes(T, bytes, n) -> PackedVector{T}\n\nRead the canonical little-endian wire form written by packedbytes: exactly cld(n*K, 8) bytes, with zero unused high bits in the final byte.\n\nValidates and copies. A wrong length is a DimensionMismatch; nonzero padding is an ArgumentError, because a reader cannot tell a corrupt stream from a differently-conventioned writer and must not guess.\n\n\n\n\n\n","title":"AIFloats.packedfrombytes"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.packedfromwords","page":"Operations, kernels, storage","text":"packedfromwords(T, words, n) -> PackedVector{T}\n\nA packed vector of n datums of T from cld(n*K, 64) logical UInt64 words, validated and copied.\n\nWords are the logical values, not a host byte layout: element i occupies bits (i-1)K .. iK-1 of the concatenated little-endian bit stream. The unused high bits of the final word must be zero — see packedwords for the inverse.\n\nCopies, so later mutation of words cannot invalidate this vector.\n\n\n\n\n\n","title":"AIFloats.packedfromwords"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.packedwords","page":"Operations, kernels, storage","text":"packedwords(pv) -> Vector{UInt64}\n\nThe cld(n*K, 64) logical words of pv, as an independent Vector. Inverse of packedfromwords.\n\n\n\n\n\n","title":"AIFloats.packedwords"},{"category":"function","location":"95-reference/20-operations.html#AIFloats.packing_saves","page":"Operations, kernels, storage","text":"packing_saves(T) -> Bool\n\nWhether PackedVector{T} is smaller than Vector{T}: false exactly when BitwidthOf(T) equals the storage unit's width (K = 8, K = 16).\n\n\n\n\n\n","title":"AIFloats.packing_saves"},{"category":"section","location":"70-examples/10-basic.html#examples-basic","page":"Basic examples","text":"These examples use the exported Binary8p4se alias: an eight-bit, four-bit precision, signed, extended format. Throughout the documentation F is a format, T a datum type, x a datum, c a code point, and ρ a projection.","title":"Basic examples"},{"category":"section","location":"70-examples/10-basic.html#Construct-and-inspect-values","page":"Basic examples","text":"using AIFloats\n\nF = Binary8p4se          # the format\nT = BinaryValue(F)       # the datum type\nx = F(1.5)\ny = F(0.25)\n\n(x, decode(x), codepoint(x), x isa T)\n(1.5, 1.5, 0x44, true)\n\nA datum stores a code point. decode returns its exact numeric value on the format's carrier; codepoint returns the stored encoding. formatof(x) recovers the format from a datum you were handed — there is no need for it when you already hold F.","title":"Construct and inspect values"},{"category":"section","location":"70-examples/10-basic.html#Numbers-and-code-points-are-distinct","page":"Basic examples","text":"Every constructor argument is a number, Unsigned included. To name a raw code point, use fromcode:\n\nusing AIFloats\n\nF = Binary8p4se\n(fromcode(F, 0x45), F(0x45), F(69))\n(1.625, 72.0, 72.0)\n\nfromcode(F, 0x45) is the datum living at code point 0x45. F(0x45) is the number 69 projected into F — the same thing F(69) means. The two questions never collide, so F(codepoint(y)) can never silently reinterpret y.\n\nAn out-of-range code point is refused rather than truncated:\n\nusing AIFloats\n\nF = Binary5p2se          # a 5-bit format: code points are 0x00:0x1f\ntry\n    fromcode(F, 0xff)\ncatch err\n    err\nend\nArgumentError(\"code point 255 is outside 0:31 for Binary{5, 2, SIGNED, EXTENDED}; for the numeric VALUE 255 write Binary5p2se(255)\")","title":"Numbers and code points are distinct"},{"category":"section","location":"70-examples/10-basic.html#Calculate-with-Julia-syntax","page":"Basic examples","text":"Same-format values support Julia's ordinary numeric spelling. These methods use the task's DefaultProjection, initially RTE_SN.\n\nusing AIFloats\n\nF = Binary8p4se\nx, y = F(1.5), F(0.25)\n@assert x + y === Add(x, y)          # each operator is one register call\n(x + y, x * y, x / y, sqrt(x), exp(y))\n(1.75, 0.375, 6.0, 1.25, 1.25)\n\nThe named register operations are available when the result format and projection should be visible at the call site:\n\nusing AIFloats\n\nF = Binary8p4se\nx, y = F(1.5), F(0.25)\n(Add(F, RTE_SN, x, y), Multiply(F, RTZ_SF, x, y))\n(1.75, 0.375)\n\nSee Operations for the full family: signatures, mixed operand formats, the correctness route each operation takes, and the refusals.","title":"Calculate with Julia syntax"},{"category":"section","location":"70-examples/10-basic.html#Convert-values-explicitly","page":"Basic examples","text":"Construction uses the task's default projection. Convert makes the format and projection explicit:\n\nusing AIFloats\n\nF = Binary8p4se\nvalue = 1.2\nnearest = Convert(F, RTE_SN, value)\ntoward_zero = Convert(F, RTZ_SN, value)\n(\n    input = value,\n    decoded = (decode(nearest), decode(toward_zero)),\n    codes = (codepoint(nearest), codepoint(toward_zero)),\n)\n(input = 1.2, decoded = (1.25, 1.125), codes = (0x42, 0x41))\n\nConvert between AIFloats formats explicitly as well:\n\nusing AIFloats\n\nx = Binary8p4se(1.5)\nSmall = Binary5p2se\ns = Convert(Small, RTE_SN, x)\n(s, decode(s), Float64(s))\n(1.5, 1.5, 1.5)","title":"Convert values explicitly"},{"category":"section","location":"70-examples/10-basic.html#Classify-and-navigate","page":"Basic examples","text":"using AIFloats\n\nF = Binary8p4se\nx = F(1.5)\n(Class(x), NextLessThan(x), NextGreaterThan(x), eps(x))\n(ClassPosNormal, 1.375, 1.625, 0.125)\n\nSpecial accessors make important endpoints easy to name:\n\nusing AIFloats\n\nF = Binary8p4se\n(MinPositiveOf(F), MinNormalOf(F), MaxFiniteOf(F))\n(0.0009765625, 0.0078125, 224.0)\n\nContinue with Intermediate examples for explicit rounding control and array operations.","title":"Classify and navigate"},{"category":"section","location":"95-reference/10-core.html#Formats,-datums,-projections","page":"Formats, datums, projections","text":"Part of the Reference; see that page for how this listing is generated and what it guarantees.\n\nPages = [\"10-core.md\"]\nDepth = 2","title":"Formats, datums, projections"},{"category":"section","location":"95-reference/10-core.html#Formats","page":"Formats, datums, projections","text":"","title":"Formats"},{"category":"section","location":"95-reference/10-core.html#Format-axes","page":"Formats, datums, projections","text":"","title":"Format axes"},{"category":"section","location":"95-reference/10-core.html#Format-queries","page":"Formats, datums, projections","text":"","title":"Format queries"},{"category":"section","location":"95-reference/10-core.html#Datums-and-the-codec","page":"Formats, datums, projections","text":"","title":"Datums and the codec"},{"category":"section","location":"95-reference/10-core.html#Datum-display","page":"Formats, datums, projections","text":"","title":"Datum display"},{"category":"section","location":"95-reference/10-core.html#Projections","page":"Formats, datums, projections","text":"","title":"Projections"},{"category":"section","location":"95-reference/10-core.html#Index","page":"Formats, datums, projections","text":"Pages = [\"10-core.md\"]","title":"Index"},{"category":"type","location":"95-reference/10-core.html#AIFloats.Binary","page":"Formats, datums, projections","text":"Binary{K,P,S,D}\n\nA 3109 complete binary format specifier.\n\nK is BitwidthOf(format)\nP is PrecisionOf(format)\nS is SignednessOf(format)\nD is DomainOf(format)\n\nThe four parameters fully determine a format, so Binary{K,P,S,D} carries no fields. K and P are Int8, S and D are Bool. Build one with Binary(K, P, S, D) rather than writing the parameters out, so that they are canonicalized and validated.\n\nA format has NO supertype, and in particular is not an AbstractFloat: it describes a value set rather than belonging to one. Its datums are BinaryValue, and those are the AbstractFloats. The distinction is not cosmetic — while Binary was an AbstractFloat, Base's fallbacks applied to format instances and isnan of a format returned false instead of failing. test-binary-format.jl pins this.\n\nSee also BitwidthOf, PrecisionOf, SignednessOf, DomainOf.\n\n\n\n\n\n","title":"AIFloats.Binary"},{"category":"function","location":"95-reference/10-core.html#AIFloats.CodeType","page":"Formats, datums, projections","text":"CodeType(K)\nCodeType(format)\n\nThe unsigned integer type that holds one code point of a K-bit format.\n\nUInt8 for K <= 8, UInt16 above. Accepts a bitwidth or a Binary format type. Formats have no instances, so there is no instance method.\n\nExamples\n\njulia> CodeType(8), CodeType(9)\n(UInt8, UInt16)\n\njulia> CodeType(Binary(16, 10, SIGNED, EXTENDED))\nUInt16\n\n\n\n\n\n","title":"AIFloats.CodeType"},{"category":"module","location":"95-reference/10-core.html#AIFloats.Formats","page":"Formats, datums, projections","text":"Formats\n\nOpt-in namespace re-exporting all 504 format aliases. using AIFloats exports the 120 names at K <= 8; using AIFloats.Formats adds the rest. Every alias is defined in AIFloats either way (AIFloats.Binary16p6se).\n\n\n\n\n\n","title":"AIFloats.Formats"},{"category":"type","location":"95-reference/10-core.html#AIFloats.IntParam","page":"Formats, datums, projections","text":"IntParam\n\nInt8 — the integer type in which a format's bitwidth K and precision P are stored.\n\nBinary narrows both to this type, so Binary(16, 10, SIGNED, FINITE) and Binary(Int32(16), Int32(10), SIGNED, FINITE) are the same type.\n\n\n\n\n\n","title":"AIFloats.IntParam"},{"category":"function","location":"95-reference/10-core.html#AIFloats.ValueType","page":"Formats, datums, projections","text":"ValueType(K)\nValueType(format)\n\nA Julia float type wide enough to hold every value of a K-bit format exactly.\n\nFloat32 for K <= 8, Float64 for K <= 10, Float128 above. Accepts a bitwidth or a Binary format type. Formats have no instances, so there is no instance method.\n\nExamples\n\njulia> ValueType(8), ValueType(10), ValueType(16)\n(Float32, Float64, Quadmath.Float128)\n\njulia> ValueType(Binary(8, 4, SIGNED, FINITE))\nFloat32\n\n\n\n\n\n","title":"AIFloats.ValueType"},{"category":"function","location":"95-reference/10-core.html#AIFloats.codetype","page":"Formats, datums, projections","text":"codetype(x)\n\nJulia-style spelling of CodeType — the same function.\n\n\n\n\n\n","title":"AIFloats.codetype"},{"category":"function","location":"95-reference/10-core.html#AIFloats.formatinfo","page":"Formats, datums, projections","text":"formatinfo(F) -> NamedTuple\n\nEverything statically known about a format, in one stable named tuple.\n\nPure, type-stable, and constant-foldable for a concrete F — the whole result folds to a literal, so this is a documentation and introspection convenience that costs nothing when the format is known at compile time.\n\nAccepts a format, a datum type, or a datum.\n\nDeliberately excludes decoded extrema and any allocated table: those have focused APIs (MaxFiniteOf, MinPositiveOf, AIFloats.table_stats) and putting them here would make a cheap query expensive.\n\nExamples\n\njulia> info = formatinfo(Binary8p4se);\n\njulia> info.name, info.bitwidth, info.precision\n(:Binary8p4se, 8, 4)\n\njulia> info.signed, info.extended\n(true, true)\n\njulia> info.datumtype\nBinaryValue(Binary8p4se)\n\njulia> formatinfo(Binary8p4se(1.5)) === info\ntrue\n\n\n\n\n\n","title":"AIFloats.formatinfo"},{"category":"function","location":"95-reference/10-core.html#AIFloats.formatname","page":"Formats, datums, projections","text":"formatname(F)\n\nThe P3109 name of a format (or of its datum type / a datum), as a Symbol: Binary⟨K⟩p⟨P⟩⟨u|s⟩⟨f|e⟩.\n\nExamples\n\njulia> formatname(Binary(8, 4, SIGNED, EXTENDED))\n:Binary8p4se\n\n\n\n\n\n","title":"AIFloats.formatname"},{"category":"function","location":"95-reference/10-core.html#AIFloats.resolve_fields","page":"Formats, datums, projections","text":"resolve_fields(K, P, S, D)\n\nprovide internal canonical forms for the field values\n\nReturns the 4-tuple (Int8(K), Int8(P), Bool(S), Bool(D)) that becomes the parameters of Binary, throwing an ArgumentError if the combination is invalid. This is how the singleton spellings (SIGNED, FINITE) and the raw Bools collapse onto one representation.\n\nNot exported; call it as AIFloats.resolve_fields.\n\njulia> AIFloats.resolve_fields(16, 10, SIGNED, FINITE)\n(16, 10, true, false)\n\njulia> AIFloats.resolve_fields(16, 10, true, false)\n(16, 10, true, false)\n\n\n\n\n\n","title":"AIFloats.resolve_fields"},{"category":"function","location":"95-reference/10-core.html#AIFloats.valuetype","page":"Formats, datums, projections","text":"valuetype(x)\n\nJulia-style spelling of ValueType — the same function.\n\n\n\n\n\n","title":"AIFloats.valuetype"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.EXTENDED","page":"Formats, datums, projections","text":"EXTENDED\n\nThe extended Domain: the format's values are extended with the infinities.\n\n\"Extended\" refers to the value domain, not to the bitwidth and not to the number of exponent bits. Interchangeable with true wherever a domain is expected. Displayed as ∞.\n\n\n\n\n\n","title":"AIFloats.EXTENDED"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.FINITE","page":"Formats, datums, projections","text":"FINITE\n\nThe finite Domain: the format holds reals and NaN, and no infinities.\n\nInterchangeable with false wherever a domain is expected. Displayed as ⏥.\n\n\n\n\n\n","title":"AIFloats.FINITE"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.SIGNED","page":"Formats, datums, projections","text":"SIGNED\n\nThe signed Signedness: negative values are representable, at the cost of one bit.\n\nInterchangeable with true wherever a signedness is expected.\n\n\n\n\n\n","title":"AIFloats.SIGNED"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.UNSIGNED","page":"Formats, datums, projections","text":"UNSIGNED\n\nThe unsigned Signedness: the format represents magnitudes only.\n\nHaving no sign bit leaves one more bit for the exponent, so an unsigned format admits P <= K where a signed one admits only P <= K - 1.\n\nInterchangeable with false wherever a signedness is expected.\n\n\n\n\n\n","title":"AIFloats.UNSIGNED"},{"category":"function","location":"95-reference/10-core.html#AIFloats.is_extended","page":"Formats, datums, projections","text":"is_extended(x)\n\nWhether x describes an extended format — one that includes the infinities.\n\nx may be a Binary format, a Domain singleton, or a Bool. Opposite of is_finite.\n\n\"Extended\" means the value domain is extended with ±Inf. It says nothing about bitwidth or about how many exponent bits the format has.\n\nExamples\n\njulia> is_extended(Binary(8, 4, SIGNED, EXTENDED))\ntrue\n\njulia> is_extended(FINITE)\nfalse\n\n\n\n\n\n","title":"AIFloats.is_extended"},{"category":"function","location":"95-reference/10-core.html#AIFloats.is_finite","page":"Formats, datums, projections","text":"is_finite(x)\n\nWhether x describes a finite format — reals and NaN, but no infinities.\n\nx may be a Binary format, a Domain singleton, or a Bool. Opposite of is_extended.\n\nThis asks about a format, not about a number, so it is unrelated to Base.isfinite.\n\nExamples\n\njulia> is_finite(Binary(8, 4, SIGNED, FINITE))\ntrue\n\njulia> is_finite(EXTENDED)\nfalse\n\n\n\n\n\n","title":"AIFloats.is_finite"},{"category":"function","location":"95-reference/10-core.html#AIFloats.is_signed","page":"Formats, datums, projections","text":"is_signed(x)\n\nWhether x describes a signed format — one in which negative values are representable.\n\nx may be a Binary format, a Signedness singleton, or a Bool. Opposite of is_unsigned.\n\nExamples\n\njulia> is_signed(Binary(8, 4, SIGNED, FINITE))\ntrue\n\njulia> is_signed(Binary(8, 4, UNSIGNED, FINITE))\nfalse\n\n\n\n\n\n","title":"AIFloats.is_signed"},{"category":"function","location":"95-reference/10-core.html#AIFloats.is_unsigned","page":"Formats, datums, projections","text":"is_unsigned(x)\n\nWhether x describes an unsigned format — one that represents magnitudes only.\n\nx may be a Binary format, a Signedness singleton, or a Bool. Opposite of is_signed.\n\nExamples\n\njulia> is_unsigned(Binary(8, 4, UNSIGNED, FINITE))\ntrue\n\njulia> is_unsigned(SIGNED)\nfalse\n\n\n\n\n\n","title":"AIFloats.is_unsigned"},{"category":"type","location":"95-reference/10-core.html#AIFloats.ΔBool","page":"Formats, datums, projections","text":"ΔBool\n\nUnion{ΔFINITE, ΔEXTENDED, Bool} — what a domain argument accepts.\n\nThis is why Binary takes either FINITE/EXTENDED or a plain Bool; both spellings canonicalize to the same Bool parameter.\n\n\n\n\n\n","title":"AIFloats.ΔBool"},{"category":"type","location":"95-reference/10-core.html#AIFloats.ΣBool","page":"Formats, datums, projections","text":"ΣBool\n\nUnion{ΣUNSIGNED, ΣSIGNED, Bool} — what a signedness argument accepts.\n\nThis is why Binary takes either SIGNED/UNSIGNED or a plain Bool; both spellings canonicalize to the same Bool parameter.\n\n\n\n\n\n","title":"AIFloats.ΣBool"},{"category":"function","location":"95-reference/10-core.html#AIFloats.BinaryFormatOf","page":"Formats, datums, projections","text":"BinaryFormatOf(x)\n\nThe Binary format of a BinaryValue datum or datum type.\n\nExamples\n\njulia> BinaryFormatOf(fromcode(Binary(8, 4, SIGNED, FINITE), 0x00))\nBinary{8, 4, ±, ⏥}\n\n\n\n\n\n","title":"AIFloats.BinaryFormatOf"},{"category":"function","location":"95-reference/10-core.html#AIFloats.BitwidthOf","page":"Formats, datums, projections","text":"BitwidthOf(format)\n\nThe total number of bits in the format, K.\n\nExamples\n\njulia> BitwidthOf(Binary(8, 4, SIGNED, FINITE))\n8\n\n\n\n\n\n","title":"AIFloats.BitwidthOf"},{"category":"function","location":"95-reference/10-core.html#AIFloats.DomainOf","page":"Formats, datums, projections","text":"DomainOf(format)\n\nThe domain parameter D, as a Bool: true for extended, false for finite.\n\nFor a predicate that also accepts singletons, use is_extended / is_finite.\n\nExamples\n\njulia> DomainOf(Binary(8, 4, SIGNED, EXTENDED))\ntrue\n\n\n\n\n\n","title":"AIFloats.DomainOf"},{"category":"function","location":"95-reference/10-core.html#AIFloats.ExponentBiasOf","page":"Formats, datums, projections","text":"ExponentBiasOf(format)\n\nThe exponent bias B: 2^(K-P-1) for a signed format, 2^(K-P) for unsigned.\n\nDropping the sign bit hands its bit to the exponent, doubling the bias — that is the unsigned formats' extra dynamic range. The biased exponent field Eb maps to the unbiased binade exponent Eb - B, so the normal exponent range is symmetric.\n\nExamples\n\njulia> ExponentBiasOf(Binary(8, 4, SIGNED, FINITE))\n8\n\njulia> ExponentBiasOf(Binary(8, 4, UNSIGNED, FINITE))\n16\n\n\n\n\n\n","title":"AIFloats.ExponentBiasOf"},{"category":"function","location":"95-reference/10-core.html#AIFloats.ExponentBitwidthOf","page":"Formats, datums, projections","text":"ExponentBitwidthOf(format)\n\nThe number of exponent bits: (K - S) - (P - 1) — what remains after the sign bit (if any) and the stored significand bits.\n\nExamples\n\njulia> ExponentBitwidthOf(Binary(8, 4, SIGNED, FINITE))\n4\n\njulia> ExponentBitwidthOf(Binary(8, 4, UNSIGNED, FINITE))\n5\n\n\n\n\n\n","title":"AIFloats.ExponentBitwidthOf"},{"category":"function","location":"95-reference/10-core.html#AIFloats.MaxFiniteOf","page":"Formats, datums, projections","text":"MaxFiniteOf(F)\n\nThe largest finite datum of format F, as a BinaryValue.\n\nExamples\n\njulia> decode(MaxFiniteOf(Binary(8, 4, SIGNED, FINITE)))\n240.0\n\n\n\n\n\n","title":"AIFloats.MaxFiniteOf"},{"category":"function","location":"95-reference/10-core.html#AIFloats.MaxSubnormalOf","page":"Formats, datums, projections","text":"MaxSubnormalOf(F)\n\nThe largest subnormal datum, code 2^(P-1) - 1. For P == 1 that code is 0 — the format has no subnormals and this returns its zero.\n\n\n\n\n\n","title":"AIFloats.MaxSubnormalOf"},{"category":"function","location":"95-reference/10-core.html#AIFloats.MinFiniteOf","page":"Formats, datums, projections","text":"MinFiniteOf(F)\n\nThe smallest (most negative for signed; zero for unsigned) finite datum of F.\n\n\n\n\n\n","title":"AIFloats.MinFiniteOf"},{"category":"function","location":"95-reference/10-core.html#AIFloats.MinNormalOf","page":"Formats, datums, projections","text":"MinNormalOf(F)\n\nThe smallest normal datum, code 2^(P-1).\n\n\n\n\n\n","title":"AIFloats.MinNormalOf"},{"category":"function","location":"95-reference/10-core.html#AIFloats.MinPositiveOf","page":"Formats, datums, projections","text":"MinPositiveOf(F)\n\nThe smallest positive datum: code point 1. The least subnormal when P > 1, the least normal when P == 1.\n\n\n\n\n\n","title":"AIFloats.MinPositiveOf"},{"category":"function","location":"95-reference/10-core.html#AIFloats.PrecisionOf","page":"Formats, datums, projections","text":"PrecisionOf(format)\n\nThe number of significand bits, P, counting the implicit leading bit.\n\nThe bits actually stored are TrailingSignificantBitsOf = P - 1; the rest of the format, (K - is_signed(format)) - (P - 1), is exponent — that is ExponentBitwidthOf.\n\nExamples\n\njulia> PrecisionOf(Binary(8, 4, SIGNED, FINITE))\n4\n\n\n\n\n\n","title":"AIFloats.PrecisionOf"},{"category":"function","location":"95-reference/10-core.html#AIFloats.RoundOf","page":"Formats, datums, projections","text":"RoundOf(p)\n\nThe AIFloats.RoundingMode half of a Projection.\n\nExamples\n\njulia> RoundOf(RTA_SN)\nRTA\n\n\n\n\n\n","title":"AIFloats.RoundOf"},{"category":"function","location":"95-reference/10-core.html#AIFloats.SatOf","page":"Formats, datums, projections","text":"SatOf(p)\n\nThe AIFloats.SaturationMode half of a Projection.\n\nExamples\n\njulia> SatOf(RTA_SN)\nSN\n\n\n\n\n\n","title":"AIFloats.SatOf"},{"category":"function","location":"95-reference/10-core.html#AIFloats.SignednessOf","page":"Formats, datums, projections","text":"SignednessOf(format)\n\nThe signedness parameter S, as a Bool: true for signed, false for unsigned.\n\nFor a predicate that also accepts singletons, use is_signed / is_unsigned.\n\nExamples\n\njulia> SignednessOf(Binary(8, 4, SIGNED, FINITE))\ntrue\n\n\n\n\n\n","title":"AIFloats.SignednessOf"},{"category":"function","location":"95-reference/10-core.html#AIFloats.TrailingSignificantBitsOf","page":"Formats, datums, projections","text":"TrailingSignificantBitsOf(format)\n\nThe number of significand bits actually stored, P - 1.\n\nPrecisionOf counts the implicit leading bit; this does not.\n\nExamples\n\njulia> TrailingSignificantBitsOf(Binary(8, 4, SIGNED, FINITE))\n3\n\n\n\n\n\n","title":"AIFloats.TrailingSignificantBitsOf"},{"category":"function","location":"95-reference/10-core.html#AIFloats.bitwidth","page":"Formats, datums, projections","text":"bitwidth(x)\n\nJulia-style spelling of BitwidthOf — the same function.\n\n\n\n\n\n","title":"AIFloats.bitwidth"},{"category":"function","location":"95-reference/10-core.html#AIFloats.codedistance","page":"Formats, datums, projections","text":"codedistance(a::T, b::T) -> Int\n\nDistance between two datums of one format in code-point steps along the total order. Not exported.\n\n\n\n\n\n","title":"AIFloats.codedistance"},{"category":"function","location":"95-reference/10-core.html#AIFloats.codemask","page":"Formats, datums, projections","text":"codemask(format)\n\nThe mask of the format's K code bits in its storage unit (CodeType): 0xff for K = 8, 0x03ff for K = 10.\n\nNot exported; call it as AIFloats.codemask.\n\n\n\n\n\n","title":"AIFloats.codemask"},{"category":"function","location":"95-reference/10-core.html#AIFloats.domain","page":"Formats, datums, projections","text":"domain(x)\n\nJulia-style spelling of DomainOf — the same function.\n\n\n\n\n\n","title":"AIFloats.domain"},{"category":"function","location":"95-reference/10-core.html#AIFloats.formatof","page":"Formats, datums, projections","text":"formatof(x)\n\nJulia-style spelling of BinaryFormatOf — the same function.\n\nformatof(F) === F for a format type, so internal code has one total normalization query, whatever it is handed.\n\nExamples\n\njulia> formatof === BinaryFormatOf\ntrue\n\njulia> formatof(Binary8p4sf(1.5)) === Binary8p4sf\ntrue\n\njulia> formatof(Binary8p4sf) === Binary8p4sf\ntrue\n\n\n\n\n\n","title":"AIFloats.formatof"},{"category":"function","location":"95-reference/10-core.html#AIFloats.orderkeytype","page":"Formats, datums, projections","text":"orderkeytype(format)\n\nThe unsigned type that holds a total-order key for the format's datums.\n\nOne key wider than the code: a format has 2^K code points mapping to keys 1:2^K (key 0 is reserved for NaN, which sorts first), so the key type must not wrap at 2^K — UInt16 for UInt8-coded formats, UInt32 for UInt16-coded ones. Not exported.\n\n\n\n\n\n","title":"AIFloats.orderkeytype"},{"category":"function","location":"95-reference/10-core.html#AIFloats.signedness","page":"Formats, datums, projections","text":"signedness(x)\n\nJulia-style spelling of SignednessOf — the same function.\n\n\n\n\n\n","title":"AIFloats.signedness"},{"category":"function","location":"95-reference/10-core.html#AIFloats.signmask","page":"Formats, datums, projections","text":"signmask(format)\n\nThe sign bit's mask in the storage unit: one(CodeType) << (K - 1).\n\nFor a signed format this isolates the sign; for an unsigned format the same bit position is ordinary magnitude. Not exported; call it as AIFloats.signmask.\n\n\n\n\n\n","title":"AIFloats.signmask"},{"category":"type","location":"95-reference/10-core.html#AIFloats.BinaryValue","page":"Formats, datums, projections","text":"BinaryValue(F) -> Type\n\nThe concrete AbstractFloat datum type of format F: BinaryValue{F,CodeType(F)}.\n\nThis is the spelling to use for an array element type or a type annotation. F itself is the FORMAT — x isa F is false for every datum x, because a format describes a value set rather than belonging to one. Calling a format, F(x), is a convenience that constructs a datum; it is not the ordinary Julia relationship between a type and its instances, so the two spellings answer different questions and both are needed.\n\nExamples\n\njulia> F = Binary(8, 4, SIGNED, FINITE);\n\njulia> T = BinaryValue(F)\nBinaryValue(Binary8p4sf)\n\njulia> F(1.5) isa T\ntrue\n\njulia> F(1.5) isa F\nfalse\n\n\n\n\n\n","title":"AIFloats.BinaryValue"},{"category":"function","location":"95-reference/10-core.html#AIFloats.Class","page":"Formats, datums, projections","text":"Class(x::BinaryValue)\n\nThe datum's FPClass.\n\nExamples\n\njulia> F = Binary(8, 4, SIGNED, EXTENDED);\n\njulia> Class(MaxFiniteOf(F)), Class(fromcode(F, AIFloats.nan_code(F)))\n(ClassPosNormal, ClassNaN)\n\n\n\n\n\n","title":"AIFloats.Class"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.ClassNaN","page":"Formats, datums, projections","text":"FPClass member: the single NaN — first in the draft's total order. Obtain it with Class.\n\n\n\n\n\n","title":"AIFloats.ClassNaN"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.ClassNegInf","page":"Formats, datums, projections","text":"FPClass member: negative infinity (EXTENDED signed formats only). Obtain it with Class.\n\n\n\n\n\n","title":"AIFloats.ClassNegInf"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.ClassNegNormal","page":"Formats, datums, projections","text":"FPClass member: a negative normal datum. Obtain it with Class.\n\n\n\n\n\n","title":"AIFloats.ClassNegNormal"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.ClassNegSubnormal","page":"Formats, datums, projections","text":"FPClass member: a negative subnormal datum (P > 1 only). Obtain it with Class.\n\n\n\n\n\n","title":"AIFloats.ClassNegSubnormal"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.ClassPosInf","page":"Formats, datums, projections","text":"FPClass member: positive infinity (EXTENDED formats only). Obtain it with Class.\n\n\n\n\n\n","title":"AIFloats.ClassPosInf"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.ClassPosNormal","page":"Formats, datums, projections","text":"FPClass member: a positive normal datum. Obtain it with Class.\n\n\n\n\n\n","title":"AIFloats.ClassPosNormal"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.ClassPosSubnormal","page":"Formats, datums, projections","text":"FPClass member: a positive subnormal datum (P > 1 only). Obtain it with Class.\n\n\n\n\n\n","title":"AIFloats.ClassPosSubnormal"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.ClassZero","page":"Formats, datums, projections","text":"FPClass member: the single zero — P3109 has no negative zero. Obtain it with Class.\n\n\n\n\n\n","title":"AIFloats.ClassZero"},{"category":"type","location":"95-reference/10-core.html#AIFloats.CodeCountingSort","page":"Formats, datums, projections","text":"CodeCountingSort\n\nThe default sort algorithm for datum vectors: a counting sort over the 2^K + 1 order keys (NaN first). Falls back to Base's default algorithm for vectors shorter than 2^K and for orderings other than forward/reverse. Not exported.\n\n\n\n\n\n","title":"AIFloats.CodeCountingSort"},{"category":"type","location":"95-reference/10-core.html#AIFloats.FPClass","page":"Formats, datums, projections","text":"FPClass\n\nThe eight-way classification of a datum: ClassNaN, ClassNegInf, ClassNegNormal, ClassNegSubnormal, ClassZero, ClassPosSubnormal, ClassPosNormal, ClassPosInf — in total-order position.\n\n\n\n\n\n","title":"AIFloats.FPClass"},{"category":"function","location":"95-reference/10-core.html#AIFloats.NextGreaterThan","page":"Formats, datums, projections","text":"NextGreaterThan(x::BinaryValue)\n\nThe next datum above x in the total order. The step off the top of the lattice returns NaN; NextGreaterThan(NaN) is the bottom datum — NaN sits below everything in the total order.\n\n\n\n\n\n","title":"AIFloats.NextGreaterThan"},{"category":"function","location":"95-reference/10-core.html#AIFloats.NextLessThan","page":"Formats, datums, projections","text":"NextLessThan(x::BinaryValue)\n\nThe next datum below x in the total order; the step below the bottom datum — and below NaN itself — is NaN, the order's floor.\n\n\n\n\n\n","title":"AIFloats.NextLessThan"},{"category":"function","location":"95-reference/10-core.html#AIFloats.codetable","page":"Formats, datums, projections","text":"codetable(F; by = :code)\n\nEvery (code, value) pair of format F (a Binary, or a datum type/alias), as a vector of named tuples — ordered by code point, or by value (by = :value, total order: NaN first).\n\nExamples\n\njulia> codetable(Binary(3, 1, UNSIGNED, FINITE))\n8-element Vector{@NamedTuple{code::UInt8, value::Float64}}:\n (code = 0x00, value = 0.0)\n (code = 0x01, value = 0.125)\n (code = 0x02, value = 0.25)\n (code = 0x03, value = 0.5)\n (code = 0x04, value = 1.0)\n (code = 0x05, value = 2.0)\n (code = 0x06, value = 4.0)\n (code = 0x07, value = NaN)\n\n\n\n\n\n","title":"AIFloats.codetable"},{"category":"function","location":"95-reference/10-core.html#AIFloats.decode","page":"Formats, datums, projections","text":"decode(x::BinaryValue)\n\nThe value the datum denotes, on the format's datum carrier — Float64 for UInt8-coded formats, Float64/Float128 (or the exact carrier at the widest biases) above.\n\nExact by construction: every datum of every format is representable on its carrier.\n\nExamples\n\njulia> decode(MaxFiniteOf(Binary(8, 4, SIGNED, FINITE)))\n240.0\n\njulia> decode(MinPositiveOf(Binary(8, 4, UNSIGNED, FINITE)))\n3.814697265625e-6\n\n\n\n\n\n","title":"AIFloats.decode"},{"category":"function","location":"95-reference/10-core.html#AIFloats.encode","page":"Formats, datums, projections","text":"encode(F, sign, S, Q)\n\nThe code point of the datum sign · S · 2^Q of format F, from canonical integer form (S ∈ 0:2^P; S == 2^P carries into the next binade).\n\nKernel-internal half of the codec; the inverse of the field split decode performs. Not exported, and not a user entry point — values enter a format only through the projection engine.\n\n\n\n\n\n","title":"AIFloats.encode"},{"category":"function","location":"95-reference/10-core.html#AIFloats.fromcode","page":"Formats, datums, projections","text":"fromcode(F, code) -> BinaryValue\nfromcode(T, code) -> BinaryValue\n\nThe datum of format F (or datum type T) whose code point is code, checked.\n\nThis is the ONLY public way to say \"the datum at this code point\". Ordinary construction means a numeric VALUE: F(3) is the number three projected into F, while fromcode(F, 3) is the datum stored as code point three. Keeping those apart in the spelling is the point — they used to be told apart by the argument's type, which made T(codepoint(y)) a silent reinterpretation.\n\ncode may be any Integer, of any width, independent of CodeType(F): the range is checked first and narrowed only after, so fromcode(F, UInt16(3)) is safe for an eight-bit format rather than silently truncating. Negative values and values above codemask(F) throw ArgumentError.\n\nThe guaranteed round trip is fromcode(typeof(x), codepoint(x)) === x.\n\nExamples\n\njulia> F = Binary(8, 4, SIGNED, EXTENDED);\n\njulia> x = fromcode(F, 0x45)\n1.625\n\njulia> codepoint(x)\n0x45\n\njulia> fromcode(typeof(x), codepoint(x)) === x\ntrue\n\njulia> fromcode(F, 3) === fromcode(BinaryValue(F), UInt16(3))\ntrue\n\n\n\n\n\n","title":"AIFloats.fromcode"},{"category":"function","location":"95-reference/10-core.html#AIFloats.nan_code","page":"Formats, datums, projections","text":"nan_code(F)\n\nThe code point of the format's single NaN: signmask (the would-be −0 slot) for a signed format, the top code for an unsigned one.\n\nNot exported.\n\n\n\n\n\n","title":"AIFloats.nan_code"},{"category":"function","location":"95-reference/10-core.html#AIFloats.neginf_code","page":"Formats, datums, projections","text":"neginf_code(F)\n\nThe code point of −Inf — the top code. Meaningful only for a SIGNED, EXTENDED format. (For an unsigned format the same code is NaN, which is why decode tests NaN first.) Not exported.\n\n\n\n\n\n","title":"AIFloats.neginf_code"},{"category":"function","location":"95-reference/10-core.html#AIFloats.order_key","page":"Formats, datums, projections","text":"order_key(x::BinaryValue)\n\nThe datum's rank in the format's total order, as an unsigned key. NaN gets key 0 — the draft's total order places NaN first, below −Inf; every other datum's key is its order position plus one.\n\nNot exported.\n\n\n\n\n\n","title":"AIFloats.order_key"},{"category":"function","location":"95-reference/10-core.html#AIFloats.posinf_code","page":"Formats, datums, projections","text":"posinf_code(F)\n\nThe code point of +Inf — one below the NaN slot. Meaningful only when the format is EXTENDED. Not exported.\n\n\n\n\n\n","title":"AIFloats.posinf_code"},{"category":"function","location":"95-reference/10-core.html#AIFloats.printcodetable","page":"Formats, datums, projections","text":"printcodetable([io,] F; by = :code)\n\nPrint codetable one row per line: the code point in hex, then the value.\n\n\n\n\n\n","title":"AIFloats.printcodetable"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.VALID_SHOW_STYLES","page":"Formats, datums, projections","text":"VALID_SHOW_STYLES\n\nThe four datum display styles: :value (the default — a datum prints as the number it denotes), :codepoint, :datum ((value ⇆ code)), and :typed (formatname(value ⇆ code)).\n\n\n\n\n\n","title":"AIFloats.VALID_SHOW_STYLES"},{"category":"function","location":"95-reference/10-core.html#AIFloats.get_show_style","page":"Formats, datums, projections","text":"get_show_style()\nget_show_style(io::IO)\n\nThe active datum display style: the stream's :binary_show_style property if set, else the process-wide default.\n\n\n\n\n\n","title":"AIFloats.get_show_style"},{"category":"function","location":"95-reference/10-core.html#AIFloats.set_show_style!","page":"Formats, datums, projections","text":"set_show_style!(style::Symbol)\n\nSet the process-wide datum display style — one of VALID_SHOW_STYLES. Per-stream override: IOContext(io, :binary_show_style => style).\n\nExamples\n\njulia> set_show_style!(:value)\n:value\n\n\n\n\n\n","title":"AIFloats.set_show_style!"},{"category":"function","location":"95-reference/10-core.html#AIFloats.DefaultProjection","page":"Formats, datums, projections","text":"DefaultProjection() -> Projection\n\nThe task's default Projection, consumed by the same-format convenience methods and by the value constructors. RTE_SN unless a with_projection block is in dynamic extent.\n\nCoherent with DefaultRoundingMode and DefaultSaturationMode by construction: both derive from this one value, so they cannot be read torn or set inconsistently.\n\nExamples\n\njulia> DefaultProjection()\nρ(RTE, SN)\n\njulia> with_projection(RTZ_SF) do\n           DefaultProjection()\n       end\nρ(RTZ, SF)\n\njulia> DefaultProjection()          # restored on exit\nρ(RTE, SN)\n\n\n\n\n\n","title":"AIFloats.DefaultProjection"},{"category":"function","location":"95-reference/10-core.html#AIFloats.DefaultRoundingMode","page":"Formats, datums, projections","text":"DefaultRoundingMode() -> RoundingMode\n\nThe rounding half of DefaultProjection, derived from it.\n\n\n\n\n\n","title":"AIFloats.DefaultRoundingMode"},{"category":"function","location":"95-reference/10-core.html#AIFloats.DefaultSaturationMode","page":"Formats, datums, projections","text":"DefaultSaturationMode() -> SaturationMode\n\nThe saturation half of DefaultProjection, derived from it.\n\n\n\n\n\n","title":"AIFloats.DefaultSaturationMode"},{"category":"type","location":"95-reference/10-core.html#AIFloats.Projection","page":"Formats, datums, projections","text":"Projection{R,S}\n\nA rounding mode paired with a saturation mode — together, how a real number is mapped onto a format's representable values.\n\nRounding settles what happens between neighbouring values; saturation settles what happens past the largest. Build one with Projection(r, s), or use one of the 27 preset constants named <rounding>_<saturation>, such as RTE_SF. Read the halves back with RoundOf and SatOf.\n\nA projection is applied to a number by project.\n\nExamples\n\njulia> p = Projection(RTE, SF)\nρ(RTE, SF)\n\njulia> p === RTE_SF\ntrue\n\njulia> RoundOf(p), SatOf(p)\n(RTE, SF)\n\n\n\n\n\n","title":"AIFloats.Projection"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.RSA","page":"Formats, datums, projections","text":"RSA\n\nStochasticA — a StochasticRoundingMode. Rounds away with probability\n\nP = ⌊ν·2^N⌋ / 2^N\n\nwhere ν is the exact leftover fraction. That is ν truncated onto the 2^N grid, so RSA rounds away slightly less often than ν warrants: its bias is never positive and reaches 2^-N in magnitude. It is the cheapest of the three — one shift, floor and compare.\n\nThe report's predicate (§4.7.4) is RoundAway(StochasticA_{N,R}) = ⌊ν·2^N⌋ + R ≥ 2^N, with R the supplied random bits, 0 ≤ R < 2^N.\n\nThe mode carries its random-bit budget N in the type; this constant is the default budget, ρRSA{8}(). Other budgets via AIFloats.ρRSA(N) with 1 <= N <= 60. Query with nrandbits.\n\nwarning: Not exactly unbiased\nAll three variants quantize the rounding probability onto a grid of 2^N steps, so none is exactly unbiased at finite N. A fraction that is not on that grid — ν = 0.8 at N = 3, say — is rounded away with a grid probability, never with probability ν. Choose the variant for the balance §4.7.4 NOTE 2 describes, between accuracy and complexity, and choose N for how fine that grid must be.\n\nnote: Motivation, not contract\nStochastic rounding is used in low-precision training because an update smaller than one unit in the last place is discarded by every deterministic mode but can accumulate under a random one. That is motivation from the wider literature, not a guarantee this package makes.\n\nSee also RSB, RSC.\n\n\n\n\n\n","title":"AIFloats.RSA"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.RSA_SF","page":"Formats, datums, projections","text":"RSA_SF\n\nThe Projection pairing RSA (StochasticA) with SF (SatFinite).\n\nOne of the 27 presets; see RTE_SF for the naming scheme.\n\n\n\n\n\n","title":"AIFloats.RSA_SF"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.RSA_SN","page":"Formats, datums, projections","text":"RSA_SN\n\nThe Projection pairing RSA (StochasticA) with SN (SatNone).\n\nOne of the 27 presets; see RTE_SF for the naming scheme.\n\n\n\n\n\n","title":"AIFloats.RSA_SN"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.RSA_SP","page":"Formats, datums, projections","text":"RSA_SP\n\nThe Projection pairing RSA (StochasticA) with SP (SatPropagate).\n\nOne of the 27 presets; see RTE_SF for the naming scheme.\n\n\n\n\n\n","title":"AIFloats.RSA_SP"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.RSB","page":"Formats, datums, projections","text":"RSB\n\nStochasticB — a StochasticRoundingMode. Rounds away with probability\n\nP = ⌊ν·2^N + ½⌋ / 2^N\n\nthat is, ν rounded to the nearest grid point, ties resolved upward. Centring the quantization this way halves the worst-case bias against RSA's truncation, to 2^-(N+1), and costs no extra random bits.\n\nThe report's predicate (§4.7.4) is RoundAway(StochasticB_{N,R}) = ⌊ν·2^(N+1)⌋ + (2R + 1) ≥ 2^(N+1). Evaluating at doubled resolution with the +1 is what supplies the half-step offset: the same N random bits buy nearest-rounding rather than truncation.\n\nDiffers from RSC only where ν·2^N lands exactly halfway — RSB goes up there, RSC goes to even. Default budget N = 8; see RSA for the budget mechanics and the caveats that apply to all three.\n\n\n\n\n\n","title":"AIFloats.RSB"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.RSB_SF","page":"Formats, datums, projections","text":"RSB_SF\n\nThe Projection pairing RSB (StochasticB) with SF (SatFinite).\n\nOne of the 27 presets; see RTE_SF for the naming scheme.\n\n\n\n\n\n","title":"AIFloats.RSB_SF"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.RSB_SN","page":"Formats, datums, projections","text":"RSB_SN\n\nThe Projection pairing RSB (StochasticB) with SN (SatNone).\n\nOne of the 27 presets; see RTE_SF for the naming scheme.\n\n\n\n\n\n","title":"AIFloats.RSB_SN"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.RSB_SP","page":"Formats, datums, projections","text":"RSB_SP\n\nThe Projection pairing RSB (StochasticB) with SP (SatPropagate).\n\nOne of the 27 presets; see RTE_SF for the naming scheme.\n\n\n\n\n\n","title":"AIFloats.RSB_SP"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.RSC","page":"Formats, datums, projections","text":"RSC\n\nStochasticC — a StochasticRoundingMode. Rounds away with probability\n\nP = RNITE(ν·2^N) / 2^N\n\nthat is, ν rounded to the nearest grid point with ties to even. Worst-case bias matches RSB's 2^-(N+1), and the even tie-break lets tie bias cancel across many roundings instead of always pushing up. It is the most expensive of the three: it needs an actual round-to-nearest-even of the scaled fraction, not just a floor.\n\nThe report's predicate (§4.7.4) is RoundAway(StochasticC_{N,R}) = RNITE(ν·2^N) + R ≥ 2^N, with RNITE the round-to-nearest-ties-even the report defines alongside it.\n\nDiffers from RSB only at an exact halfway ν·2^N. Default budget N = 8; see RSA for the budget mechanics and the caveats that apply to all three.\n\n\n\n\n\n","title":"AIFloats.RSC"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.RSC_SF","page":"Formats, datums, projections","text":"RSC_SF\n\nThe Projection pairing RSC (StochasticC) with SF (SatFinite).\n\nOne of the 27 presets; see RTE_SF for the naming scheme.\n\n\n\n\n\n","title":"AIFloats.RSC_SF"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.RSC_SN","page":"Formats, datums, projections","text":"RSC_SN\n\nThe Projection pairing RSC (StochasticC) with SN (SatNone).\n\nOne of the 27 presets; see RTE_SF for the naming scheme.\n\n\n\n\n\n","title":"AIFloats.RSC_SN"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.RSC_SP","page":"Formats, datums, projections","text":"RSC_SP\n\nThe Projection pairing RSC (StochasticC) with SP (SatPropagate).\n\nOne of the 27 presets; see RTE_SF for the naming scheme.\n\n\n\n\n\n","title":"AIFloats.RSC_SP"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.RTA","page":"Formats, datums, projections","text":"RTA\n\nRoundToAway — to the nearest representable value; ties go away from zero.\n\nA ToNearestRoundingMode.\n\n\n\n\n\n","title":"AIFloats.RTA"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.RTA_SF","page":"Formats, datums, projections","text":"RTA_SF\n\nThe Projection pairing RTA (RoundToAway) with SF (SatFinite).\n\nOne of the 27 presets; see RTE_SF for the naming scheme.\n\n\n\n\n\n","title":"AIFloats.RTA_SF"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.RTA_SN","page":"Formats, datums, projections","text":"RTA_SN\n\nThe Projection pairing RTA (RoundToAway) with SN (SatNone).\n\nOne of the 27 presets; see RTE_SF for the naming scheme.\n\n\n\n\n\n","title":"AIFloats.RTA_SN"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.RTA_SP","page":"Formats, datums, projections","text":"RTA_SP\n\nThe Projection pairing RTA (RoundToAway) with SP (SatPropagate).\n\nOne of the 27 presets; see RTE_SF for the naming scheme.\n\n\n\n\n\n","title":"AIFloats.RTA_SP"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.RTE","page":"Formats, datums, projections","text":"RTE\n\nRoundToEven — to the nearest representable value; ties go to the one with an even last bit.\n\nThe unbiased default, and the same rule as IEEE 754's round-to-nearest-ties-to-even. A ToNearestRoundingMode.\n\n\n\n\n\n","title":"AIFloats.RTE"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.RTE_SF","page":"Formats, datums, projections","text":"RTE_SF\n\nThe Projection pairing RTE with SF.\n\nOne of the 27 presets, named <rounding>_<saturation> over the 9 rounding modes (RTE, RTA, RTP, RTN, RTZ, RTO, RSA, RSB, RSC) and the 3 saturation modes (SF, SP, SN): RTE_SF, RTE_SP, RTE_SN, RTA_SF, … , RSC_SN.\n\nRTE_SF rounds to nearest with ties to even and clamps everything, infinities included, to the finite range. It is not the package default: DefaultProjection is initially RTE_SN, and randn — not ordinary construction — is what defaults to RTE_SF.\n\n\n\n\n\n","title":"AIFloats.RTE_SF"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.RTE_SN","page":"Formats, datums, projections","text":"RTE_SN\n\nThe Projection pairing RTE (RoundToEven) with SN (SatNone).\n\nOne of the 27 presets; see RTE_SF for the naming scheme.\n\n\n\n\n\n","title":"AIFloats.RTE_SN"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.RTE_SP","page":"Formats, datums, projections","text":"RTE_SP\n\nThe Projection pairing RTE (RoundToEven) with SP (SatPropagate).\n\nOne of the 27 presets; see RTE_SF for the naming scheme.\n\n\n\n\n\n","title":"AIFloats.RTE_SP"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.RTN","page":"Formats, datums, projections","text":"RTN\n\nRoundTowardNegative — to the nearest representable value at or below the exact one.\n\nA UnidirectionalRoundingMode.\n\n\n\n\n\n","title":"AIFloats.RTN"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.RTN_SF","page":"Formats, datums, projections","text":"RTN_SF\n\nThe Projection pairing RTN (RoundTowardNegative) with SF (SatFinite).\n\nOne of the 27 presets; see RTE_SF for the naming scheme.\n\n\n\n\n\n","title":"AIFloats.RTN_SF"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.RTN_SN","page":"Formats, datums, projections","text":"RTN_SN\n\nThe Projection pairing RTN (RoundTowardNegative) with SN (SatNone).\n\nOne of the 27 presets; see RTE_SF for the naming scheme.\n\n\n\n\n\n","title":"AIFloats.RTN_SN"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.RTN_SP","page":"Formats, datums, projections","text":"RTN_SP\n\nThe Projection pairing RTN (RoundTowardNegative) with SP (SatPropagate).\n\nOne of the 27 presets; see RTE_SF for the naming scheme.\n\n\n\n\n\n","title":"AIFloats.RTN_SP"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.RTO","page":"Formats, datums, projections","text":"RTO\n\nRoundToOdd — an inexact result takes the odd neighbour; an exact result is left alone. The report's predicate (§4.7.4) is RoundAway(ToOdd) = ν > 0 and CodeIsEven.\n\nThe one ParityRoundingMode.\n\nnote: Motivation, not contract\nRound-to-odd's practical use is avoiding double rounding: round a wide intermediate to odd, narrow it, and the second rounding lands where a single direct rounding would have. That is a property of the composition, not a guarantee this package makes about error magnitude or hardware cost on any particular device.\n\n\n\n\n\n","title":"AIFloats.RTO"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.RTO_SF","page":"Formats, datums, projections","text":"RTO_SF\n\nThe Projection pairing RTO (RoundToOdd) with SF (SatFinite).\n\nOne of the 27 presets; see RTE_SF for the naming scheme.\n\n\n\n\n\n","title":"AIFloats.RTO_SF"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.RTO_SN","page":"Formats, datums, projections","text":"RTO_SN\n\nThe Projection pairing RTO (RoundToOdd) with SN (SatNone).\n\nOne of the 27 presets; see RTE_SF for the naming scheme.\n\n\n\n\n\n","title":"AIFloats.RTO_SN"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.RTO_SP","page":"Formats, datums, projections","text":"RTO_SP\n\nThe Projection pairing RTO (RoundToOdd) with SP (SatPropagate).\n\nOne of the 27 presets; see RTE_SF for the naming scheme.\n\n\n\n\n\n","title":"AIFloats.RTO_SP"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.RTP","page":"Formats, datums, projections","text":"RTP\n\nRoundTowardPositive — to the nearest representable value at or above the exact one.\n\nA UnidirectionalRoundingMode.\n\n\n\n\n\n","title":"AIFloats.RTP"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.RTP_SF","page":"Formats, datums, projections","text":"RTP_SF\n\nThe Projection pairing RTP (RoundTowardPositive) with SF (SatFinite).\n\nOne of the 27 presets; see RTE_SF for the naming scheme.\n\n\n\n\n\n","title":"AIFloats.RTP_SF"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.RTP_SN","page":"Formats, datums, projections","text":"RTP_SN\n\nThe Projection pairing RTP (RoundTowardPositive) with SN (SatNone).\n\nOne of the 27 presets; see RTE_SF for the naming scheme.\n\n\n\n\n\n","title":"AIFloats.RTP_SN"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.RTP_SP","page":"Formats, datums, projections","text":"RTP_SP\n\nThe Projection pairing RTP (RoundTowardPositive) with SP (SatPropagate).\n\nOne of the 27 presets; see RTE_SF for the naming scheme.\n\n\n\n\n\n","title":"AIFloats.RTP_SP"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.RTZ","page":"Formats, datums, projections","text":"RTZ\n\nRoundTowardZero — truncation; magnitude never increases.\n\nA UnidirectionalRoundingMode.\n\n\n\n\n\n","title":"AIFloats.RTZ"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.RTZ_SF","page":"Formats, datums, projections","text":"RTZ_SF\n\nThe Projection pairing RTZ (RoundTowardZero) with SF (SatFinite).\n\nOne of the 27 presets; see RTE_SF for the naming scheme.\n\n\n\n\n\n","title":"AIFloats.RTZ_SF"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.RTZ_SN","page":"Formats, datums, projections","text":"RTZ_SN\n\nThe Projection pairing RTZ (RoundTowardZero) with SN (SatNone).\n\nOne of the 27 presets; see RTE_SF for the naming scheme.\n\n\n\n\n\n","title":"AIFloats.RTZ_SN"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.RTZ_SP","page":"Formats, datums, projections","text":"RTZ_SP\n\nThe Projection pairing RTZ (RoundTowardZero) with SP (SatPropagate).\n\nOne of the 27 presets; see RTE_SF for the naming scheme.\n\n\n\n\n\n","title":"AIFloats.RTZ_SP"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.SF","page":"Formats, datums, projections","text":"SF\n\nSatFinite — a saturating AIFloats.SaturationMode.\n\nA SaturatingSaturationMode: project clamps everything past the largest finite value — a genuine infinity too — to that value. See AIFloats.saturate.\n\n\n\n\n\n","title":"AIFloats.SF"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.SN","page":"Formats, datums, projections","text":"SN\n\nSatNone — the non-saturating AIFloats.SaturationMode.\n\nThe one NonsaturatingSaturationMode: past the largest finite value, project follows the draft's direction/signedness/domain rows — a directed rounding pointing back into range gives the extremal finite, an extended domain spends its infinity, otherwise NaN. See AIFloats.saturate.\n\n\n\n\n\n","title":"AIFloats.SN"},{"category":"constant","location":"95-reference/10-core.html#AIFloats.SP","page":"Formats, datums, projections","text":"SP\n\nSatPropagate — a saturating AIFloats.SaturationMode.\n\nA SaturatingSaturationMode: project keeps a representable infinity and clamps the rest to the largest finite value. See AIFloats.saturate.\n\n\n\n\n\n","title":"AIFloats.SP"},{"category":"function","location":"95-reference/10-core.html#AIFloats.isstochastic","page":"Formats, datums, projections","text":"isstochastic(x)\n\nWhether a rounding mode (or the rounding half of a Projection) is stochastic.\n\nA compile-time constant: stochastic projections are never tabulable and consume random bits; pure ones never touch RNG state.\n\nExamples\n\njulia> isstochastic(RSA), isstochastic(RTE)\n(true, false)\n\njulia> isstochastic(RSB_SF), isstochastic(RTE_SF)\n(true, false)\n\n\n\n\n\n","title":"AIFloats.isstochastic"},{"category":"function","location":"95-reference/10-core.html#AIFloats.nrandbits","page":"Formats, datums, projections","text":"nrandbits(x)\n\nThe random-bit budget N of a stochastic rounding mode (or of the rounding half of a Projection); 0 for a deterministic mode.\n\nExamples\n\njulia> nrandbits(RSA), nrandbits(RTE)\n(8, 0)\n\njulia> nrandbits(AIFloats.ρRSA(12))\n12\n\n\n\n\n\n","title":"AIFloats.nrandbits"},{"category":"function","location":"95-reference/10-core.html#AIFloats.project","page":"Formats, datums, projections","text":"project(F, ρ, X; R = 0, sticky = 0) -> BinaryValue\n\nMap the value X (any float carrier — Float64, Float128, BigFloat) onto format F under projection ρ: round to F's precision, saturate, encode. The single write path into a code point.\n\nR is the pre-drawn random integer in [0, 2^N) for a stochastic ρ (0 otherwise). Projection is monotone in the value at fixed R — the property the interval oracle relies on.\nsticky ∈ {-1, 0, +1} declares the true value to be X + sticky·ε for an infinitesimal ε > 0, letting an enclosure endpoint stand in for an irrational value without losing direction information.\n\nExamples\n\njulia> F = Binary(8, 4, SIGNED, EXTENDED);\n\njulia> decode(project(F, RTE_SF, 1.6))\n1.625\n\njulia> decode(project(F, RTZ_SF, 1.6))\n1.5\n\njulia> decode(project(F, RTE_SF, 1.0e9))   # saturates at the max finite datum\n224.0\n\n\n\n\n\n","title":"AIFloats.project"},{"category":"function","location":"95-reference/10-core.html#AIFloats.project_interval","page":"Formats, datums, projections","text":"project_interval(F, ρ, f; R = 0) -> BinaryValue\n\nProject a lazily-refined enclosure onto F: f(prec) must return directed MPFR endpoints (d, u) with the true value in [d, u], tightening as prec grows. Sound for every rounding mode, including stochastic, at fixed R. Not exported.\n\n\n\n\n\n","title":"AIFloats.project_interval"},{"category":"function","location":"95-reference/10-core.html#AIFloats.round_to_precision","page":"Formats, datums, projections","text":"round_to_precision(P, B, μ, X, R, sticky) -> Rounded\n\nInterim Report §4.7.4 ωRoundToPrecision: round the closed extended real X to precision P at exponent bias B under rounding mode μ, returning the canonical (kind, sign, S, Q) decomposition rather than a code point.\n\nR supplies the random bits for a stochastic μ (0 <= R < 2^N) and is ignored otherwise. sticky is +1, 0, or -1: it says that the true value lies a shade above, exactly at, or a shade below X, so each §4.7.4 RoundAway predicate is evaluated on the exact fraction even when the carrier cannot hold it. Callers that have an exact X pass 0.\n\nThis is the rounding half of project; saturate is the other half. Ordinary code wants project, which composes them and encodes the result. Methods exist for each carrier (Float64, Float128, BigFloat, AIFloats.Dyadic); no Quadmath arithmetic function is used on the Float128 path, which is decoded exactly instead.\n\nNot exported; call it as AIFloats.round_to_precision.\n\n\n\n\n\n","title":"AIFloats.round_to_precision"},{"category":"function","location":"95-reference/10-core.html#AIFloats.saturate","page":"Formats, datums, projections","text":"saturate(F, ρ, r::Rounded) -> code point\n\nInterim Report §4.7.5 ωSaturate, followed by the encode: map the already rounded value r onto a code point of format F under ρ's saturation mode.\n\nThe three modes differ only outside F's finite range, and they differ from each other only on a genuine infinity:\n\nMode Out-of-range finite ±Inf reaching saturation\nSF clamps to the extremal finite clamps to the extremal finite\nSP clamps to the extremal finite kept when F can represent it, else clamped\nSN extremal finite when the rounding direction points back into range, else the infinity, else NaN the infinity when F is EXTENDED and signed as needed, else NaN\n\nUnder SN, an unsigned extended format has no -Inf, so a negative overflow is NaN; a FINITE format has no infinity at all, so every out-of-range result is NaN. The rounding mode is passed in because §4.7.5 needs it to resolve those directed rows — saturation itself never rounds.\n\nThis is the saturation half of project; round_to_precision is the other half.\n\nNot exported; call it as AIFloats.saturate.\n\n\n\n\n\n","title":"AIFloats.saturate"},{"category":"function","location":"95-reference/10-core.html#AIFloats.with_projection","page":"Formats, datums, projections","text":"with_projection(f, ρ::Projection)\nwith_projection(f, μ::RoundingMode, σ::SaturationMode)\n\nRun f() with ρ as the task's default projection, restoring the previous value on return and on exception. Blocks nest, and a task started inside one inherits the binding.\n\nThis replaces the former DefaultProjection! setters. The change is not cosmetic: a projection changes numeric results, so a process-wide setter makes two concurrently running tasks silently fight over what arithmetic means.\n\nThe convenience seam it feeds costs one dynamic dispatch for a non-RTE_SN projection. Where that matters, pass the projection explicitly — Add(F, ρ, x, y) never reads this value at all.\n\nExamples\n\njulia> F = Binary8p4se;\n\njulia> with_projection(RTZ_SN) do\n           F(1.35)\n       end\n1.25\n\njulia> F(1.35)                      # the unscoped default rounds to nearest\n1.375\n\n\n\n\n\n","title":"AIFloats.with_projection"},{"category":"section","location":"40-projections.html#projections","page":"Projections","text":"A format holds finitely many values, so most reals are not among them. A projection is the choice of how a real is mapped onto one that is: a rounding mode for values that fall between neighbours, and a saturation mode for values past the largest.\n\nUse a projection with Convert, a registered operation, or project. Projection is the package's single code-point write path, so the selected rounding and saturation behavior is applied exactly once.\n\nnote: Which names are the report's\nAIFloats spells some modes differently from the Interim Report. The report says NearestTiesToEven, NearestTiesToAway, and ToOdd; AIFloats displays RoundToEven, RoundToAway, and RoundToOdd. Those are package long names, not report terminology. The mapping is in Report names and package names below, and every semantic claim on this page is checked against Interim Report §4.2, §4.7.4, and §4.7.5.","title":"Projections"},{"category":"section","location":"40-projections.html#rounding-modes","page":"Projections","text":"Nine modes, each a singleton with a short name and a long one:\n\nConstant Long name Family Behavior\nRTE RoundToEven to nearest nearest; ties to the even last bit\nRTA RoundToAway to nearest nearest; ties away from zero\nRTP RoundTowardPositive unidirectional toward +Inf\nRTN RoundTowardNegative unidirectional toward −Inf\nRTZ RoundTowardZero unidirectional truncate\nRTO RoundToOdd parity inexact results get a 1 last bit\nRSA StochasticA stochastic randomized\nRSB StochasticB stochastic randomized\nRSC StochasticC stochastic randomized\n\nThe families are types, so a group can be dispatched on or tested as a group:\n\njulia> RTE isa AIFloats.ToNearestRoundingMode\ntrue\n\njulia> RTZ isa AIFloats.DeterministicRoundingMode\ntrue\n\njulia> RSA isa AIFloats.StochasticRoundingMode\ntrue\n\nAIFloats.DeterministicRoundingMode covers the to-nearest, unidirectional, and parity families — everything except the stochastic three.","title":"Rounding modes"},{"category":"section","location":"40-projections.html#What-each-mode-actually-decides","page":"Projections","text":"Interim Report §4.7.4 defines every mode by one predicate, RoundAway(µ), on the fraction ν left over after truncating the exact significand. AIFloats implements exactly these:\n\nMode RoundAway is true when\nRTZ never\nRTP ν > 0 and the value is positive\nRTN ν > 0 and the value is negative\nRTA ν ≥ 0.5\nRTE ν > 0.5, or ν = 0.5 and the truncated code is odd\nRTO ν > 0 and the truncated code is even\nRSA{N} ⌊ν·2^N⌋ + R ≥ 2^N\nRSB{N} ⌊ν·2^(N+1)⌋ + (2R + 1) ≥ 2^(N+1)\nRSC{N} RNITE(ν·2^N) + R ≥ 2^N\n\n\"Even\" here is the report's CodeIsEven, which for P = 1 formats — where there is no significand bit to be even — falls back to the parity of the biased exponent.","title":"What each mode actually decides"},{"category":"section","location":"40-projections.html#How-the-three-stochastic-variants-differ","page":"Projections","text":"All three consume exactly N random bits and differ only in how the rounding probability is quantized onto the grid of 2^N steps:\n\nMode P(round away) Quantization of ν Bias\nRSA{N} ⌊ν·2^N⌋ / 2^N truncated one-signed, up to 2^-N\nRSB{N} ⌊ν·2^N + ½⌋ / 2^N nearest, ties up centred, 2^-(N+1)\nRSC{N} RNITE(ν·2^N) / 2^N nearest, ties to even centred, 2^-(N+1)\n\nRSB and RSC can disagree only where ν·2^N lands exactly halfway. Cost runs opposite to accuracy — that is the \"balance between accuracy and complexity\" of §4.7.4 NOTE 2.\n\nwarning: None is exactly unbiased at finite `N`\nStochastic rounding is unbiased when P(away) = ν, which at a finite budget holds only for the ν that land on the grid. ν = 0.8 at N = 3 is rounded away with probability ¾ by all three. AIFloats claims no bias figure beyond the bounds above.\n\nAlgorithms derives these probabilities from the §4.7.4 predicates, works through where the variants diverge, and gives guidance on choosing between them and on choosing N.\n\nnote: Motivation, not contract\nRound-to-odd's practical use is avoiding double rounding: round a wide intermediate to odd, narrow it, and the second rounding lands where a single direct rounding would have — a property of the composition, not of the mode alone. Stochastic rounding is used in low-precision training because a weight update smaller than one unit in the last place is discarded by every deterministic mode but can accumulate under a random one. Both statements are motivation from the wider literature, not guarantees this package makes, and neither is a claim about hardware cost or error magnitude on any particular device.","title":"How the three stochastic variants differ"},{"category":"section","location":"40-projections.html#saturation-modes","page":"Projections","text":"Three modes, covering what happens to a value too large for the format:\n\nConstant Long name Family\nSF SatFinite saturating\nSP SatPropagate saturating\nSN SatNone non-saturating\n\njulia> SF isa AIFloats.SaturatingSaturationMode\ntrue\n\njulia> SN isa AIFloats.NonsaturatingSaturationMode\ntrue","title":"Saturation modes"},{"category":"section","location":"40-projections.html#What-they-do,-exactly","page":"Projections","text":"Interim Report §4.7.5. The three modes agree on everything inside the finite range and differ outside it — and they differ from each other only on a genuine infinity:\n\n out-of-range finite ±Inf reaching saturation\nSF clamps to the extremal finite clamps to the extremal finite\nSP clamps to the extremal finite kept when the format can represent it, else clamped\nSN the extremal finite when the rounding direction points back into range; otherwise the infinity if the format is EXTENDED, else NaN the infinity if the format is EXTENDED and signed as needed, else NaN\n\nTwo consequences worth stating outright, because they are easy to get wrong:\n\nUnder SN, an unsigned EXTENDED format has no -Inf, so a negative overflow is NaN, not -Inf.\nUnder SN, a FINITE format has no infinity at all, so every remaining out-of-range result is NaN.\n\nSF and SP are indistinguishable on a finite input, however large. To see the difference, project an actual infinity:\n\njulia> F = Binary(8, 3, SIGNED, EXTENDED);\n\njulia> decode(Convert(F, RTE_SF, Inf)), decode(Convert(F, RTE_SP, Inf))\n(49152.0, Inf)\n\njulia> decode(Convert(F, RTE_SF, 1.0e100)), decode(Convert(F, RTE_SP, 1.0e100))\n(49152.0, 49152.0)\n\nSaturation never rounds. It receives the rounding mode only because §4.7.5's SatNone rows need it to decide whether a directed rounding should deliver a finite.","title":"What they do, exactly"},{"category":"section","location":"40-projections.html#Short-and-long-names","page":"Projections","text":"Every mode answers to both. Lowercase string gives the short name, capital String the long one:\n\njulia> string(RTE), String(RTE)\n(\"RTE\", \"RoundToEven\")\n\njulia> string(SF), String(SF)\n(\"SF\", \"SatFinite\")\n\nDisplaying a mode uses the short name:\n\njulia> RTE\nRTE","title":"Short and long names"},{"category":"section","location":"40-projections.html#Pairing-them","page":"Projections","text":"A Projection is one rounding mode and one saturation mode together:\n\njulia> p = Projection(RTA, SN)\nρ(RTA, SN)\n\njulia> RoundOf(p), SatOf(p)\n(RTA, SN)\n\nAll 27 pairings exist as constants named <rounding>_<saturation>, so building one is usually unnecessary:\n\njulia> p === RTA_SN\ntrue\n\nThe full set runs RTE_SF, RTE_SP, RTE_SN, RTA_SF, … , RSC_SN.","title":"Pairing them"},{"category":"section","location":"40-projections.html#Defaults,-and-how-far-each-one-reaches","page":"Projections","text":"The Interim Report defines no default projection. §4.5 requires the listed operation specializations at (NearestTiesToEven, SatNone); that is a conformance requirement, not a package-wide default. AIFloats' default happens to be the same pair, as a package choice:\n\njulia> DefaultProjection()\nρ(RTE, SN)\n\nWhat Default Scope\nconstruction, convenience arithmetic, Base operators RTE_SN task-local — DefaultProjection, bound by with_projection\nrand RTZ_SN per call; keeps draws provably < 1\nrandn RTE_SF per call; clamps the tails, signed formats only\nOp(F, ρ, …), Convert(F, ρ, x), project none — ρ is explicit per call\n\nThe task-local default is bound for a dynamic extent and restored on return and on exception; it nests, and a task started inside inherits it. There is deliberately no setter: a process-wide one would let two concurrent tasks fight over what arithmetic means.\n\njulia> with_projection(RTZ_SF) do\n           DefaultProjection()\n       end\nρ(RTZ, SF)\n\njulia> DefaultProjection()          # restored on exit\nρ(RTE, SN)\n\nLibrary code should still prefer an explicit projection. Add(F, ρ, x, y) never reads the default at all.","title":"Defaults, and how far each one reaches"},{"category":"section","location":"40-projections.html#report-names","page":"Projections","text":"Interim Report §4.2 AIFloats short AIFloats long\nNearestTiesToEven RTE RoundToEven\nNearestTiesToAway RTA RoundToAway\nTowardPositive RTP RoundTowardPositive\nTowardNegative RTN RoundTowardNegative\nTowardZero RTZ RoundTowardZero\nToOdd RTO RoundToOdd\nStochastic[A, B, C] RSA, RSB, RSC StochasticA/B/C\nSatFinite SF SatFinite\nSatPropagate SP SatPropagate\nSatNone SN SatNone\n\nThe first, second, and sixth rows are the ones where AIFloats' long name is not the report's word. Everything else matches.","title":"Report names and package names"},{"category":"section","location":"40-projections.html#Stochastic-projections-and-randomness","page":"Projections","text":"A stochastic projection needs N random bits per rounding decision, where N is carried in the mode's type:\n\njulia> isstochastic(RSA_SN), nrandbits(RSA_SN)\n(true, 8)\n\njulia> isstochastic(RTE_SN)\nfalse\n\nThe contract:\n\nN is in 1:60. The report bounds the random bits R by 0 ≤ R < 2^N (§4.7.4 Details) and does not bound N; the ceiling of 60 is this package's limit.\nAn explicit R must be in 0:(2^N - 1). Out of range is an ArgumentError.\nWhen both R and rng are supplied, R wins — it names the draw outright.\nWith neither, a stochastic projection resolves Random.default_rng().\nA pure projection touches no RNG state at all, whatever rng or R you pass.\nArray calls take an rng and consume one sequential stream in eachindex order, so a seeded call is reproducible and never depends on the scheduler. There is no per-element R.\n\njulia> F = Binary(8, 3, SIGNED, EXTENDED); T = BinaryValue(F);\n\njulia> x, y = T(1.0), T(0.125);\n\njulia> Add(F, RSA_SN, x, y; R = 3) === Add(F, RSA_SN, x, y; R = 3)\ntrue\n\njulia> Add(F, RSA_SN, x, y; R = 1 << 60)\nERROR: ArgumentError: explicit R=1152921504606846976 outside 0:255 for N=8 random bits\n[...]","title":"Stochastic projections and randomness"},{"category":"section","location":"40-projections.html#How-projections-display","page":"Projections","text":"A Projection prints as ρ(rounding, saturation), and like the modes it has a short form and a long one. The short form is what the REPL shows:\n\njulia> RTE_SF\nρ(RTE, SF)\n\njulia> repr(RTE_SF)\n\"ρ(RoundToEven, SatFinite)\"","title":"How projections display"},{"category":"section","location":"45-operations.html#operations","page":"Operations","text":"The Interim Report defines operations as mappings from operand datums to result datums, parameterized by the operand formats, the result format, and a projection (§4.1). AIFloats implements that register directly: one generated family, one shape, no hand-written per-operation variants.\n\nEvery name on this page comes from the registry, so an operation cannot exist without a documented signature and cannot be documented with the wrong arity.\n\njulia> length(AIFloats.operations())\n52\n\njulia> AIFloats.operationinfo(:Add)\n(name = :Add, arity = 2, group = :A, factors = 1)\n\nfactors is the carrier-width driver: the largest number of datum factors in any monomial of the exact result. It is not the arity — FAA has three operands and one factor.","title":"Operations"},{"category":"section","location":"45-operations.html#The-four-signatures","page":"Operations","text":"Take any register name — Add, Exp, FMA, MinimumMagnitude, … — and it has these shapes:\n\njulia> F = Binary8p4se; G = Binary8p3se; x = F(1.5); y = F(0.25);\n\njulia> Add(F, RTE_SN, x, y)          # 1. the draft form: result format, ρ, operands\n1.75\n\njulia> Add(x, y)                     # 2. same-format convenience, task default ρ\n1.75\n\njulia> Exp(RTZ_SF, x)                # 3. projection-first (unary only)\n4.0\n\njulia> vmap(:Add, F, RTE_SN, [x, y], [y, x])   # 4. elementwise over arrays\n2-element Vector{BinaryValue(Binary8p4se)}:\n 1.75\n 1.75\n\nForm Result format Projection Notes\nOp(fr, ρ, xs...) fr, explicit ρ, explicit the draft's shape; operands may be of different formats\nOp(xs...) the shared operand format DefaultProjection (task) resolves once operands must share a format\nOp(ρ, x) the operand's format ρ, explicit unary operations only\nvmap(:Op, fr, ρ, As...) fr, explicit ρ, resolved once per call see vmap! to write into an existing array, which takes the result format from dest instead\n\nfr accepts a Binary format type, a datum type, or an alias — all three name the same format:\n\njulia> Add(Binary8p4se, RTE_SN, x, y) === Add(BinaryValue(Binary8p4se), RTE_SN, x, y)\ntrue","title":"The four signatures"},{"category":"section","location":"45-operations.html#Mixed-operand-formats","page":"Operations","text":"The explicit form accepts operands of any formats. Each is decoded onto a carrier wide enough for the exact result, and the single result is projected once into fr:\n\njulia> Add(F, RTE_SN, F(1.5), G(0.25))\n1.75\n\nThere is no automatic promotion of different formats: Binary8p4se(1) + Binary8p3se(1) raises a MethodError. Working with mixed formats requires an explicit Convert or an explicit Op(fr, ρ, …) call. The computational system does not presume to know how you want to resolve operations with values of mixed formats.","title":"Mixed operand formats"},{"category":"section","location":"45-operations.html#How-the-result-is-computed","page":"Operations","text":"The registry records which route each operation takes to a correctly rounded answer. Every route projects exactly once, and each is pinned equal to a rigorous reference by the test suite — the group affects speed, never the result.\n\nKind Operations Route\nSign manipulation Abs, CopySign low-level bit operations and comparison\nRing arithmetic Negate, Add, Subtract, Multiply, FAA, FMA error-free computation\nSelection based {Minimum, Maximum}[Magnitude]{Number, Finite}, Clamp exact order-based selection from a constrained set\nEnclosure Sqrt, Divide, Exp, Log, the trigonometric and hyperbolic families, and their π-scaled and inverse forms, … the correctly rounded interval-enclosure ladder\nProjection Convert a projection with no arithmetic of its own\n\nThe first three kinds are all exact, and differ in what they do with the operands: sign manipulation rewrites one bit, ring arithmetic computes a result the operands do not contain, and a selection chooses one that they do. (Negate is a sign-bit flip too, and is listed with ring arithmetic because that is the role it plays there — the additive inverse.)\n\nThe selection row is a schema — […] optional, {…} a choice — naming the shape of the extremum family rather than enumerating it. Not every combination the pattern admits exists (there is no MinimumMagnitudeFinite), and the family also holds the bare Minimum and Maximum. AIFloats.operations() is the complete and authoritative list; this table is a map of the routes, not a census.\n\nnote: These kinds are finer than the registry's groups\nAIFloats.operationinfo reports :B for Enclosure and :conv for Projection, but it does not draw the other lines above. The Sign manipulation row alone spans two groups: Abs is recorded :A and CopySign is :C. Clamp is :A beside the ring operations, while the extremum family is :C.That is not an inconsistency in either scheme. The registry groups by evaluation route — how an exact answer is reached — and Abs and Clamp reach one the same way the ring operations do. The Kind column groups by what the operation does with its operands, which is the more useful reading here and a finer cut than the registry needs to make.\n\nAlgorithms explains how the enclosure route proves a result is correctly rounded rather than merely computing it carefully, and why no fixed working precision could do the same.\n\nwarning: Float128 is a carrier, not an oracle\nQuadmath.Float128 is used as a value carrier. libquadmath's elementary functions are not assumed to be correctly rounded, and no Quadmath transcendental result is ever accepted on its own. A fast result is accepted only when a proof or a two-sided enclosure check confirms it; otherwise the operation escalates to the rigorous MPFR ladder. The pure-Julia AIFloats.fma128 and AIFloats.faa128 carry their own documented guarantees. AIFloats.FAST_ARITH and AIFloats.FAST_ENCLOSURE switch the fast stages off for differential testing.","title":"How the result is computed"},{"category":"section","location":"45-operations.html#Randomness","page":"Operations","text":"rng and R are consulted only under a stochastic projection. A pure projection touches no RNG state, whatever you pass. The full contract — the 1:60 bound on N, the 0:(2^N - 1) bound on R, R taking precedence over rng, and the sequential eachindex stream for arrays — is on the Projections page.","title":"Randomness"},{"category":"section","location":"45-operations.html#Julia's-operators-are-veneers","page":"Operations","text":"Same-format datums support Julia's ordinary spelling. Each veneer is exactly one register call under the task's default projection — not a second, looser semantics:\n\njulia> x + y === Add(x, y)\ntrue\n\njulia> exp(y) === Exp(y)\ntrue\n\nabs, sqrt, exp, log, the trigonometric and hyperbolic families, hypot, copysign, max/min, fma/muladd, and clamp all map this way. Comparison, isless/sort in the draft's NaN-first total order, and the AbstractFloat contract (zero, eps, floatmin, frexp, nextfloat, …) are on the same footing.","title":"Julia's operators are veneers"},{"category":"section","location":"45-operations.html#Refusals-are-explicit","page":"Operations","text":"Where the draft defines no answer, AIFloats throws an ArgumentError that says why — never a bare MethodError, and never a silently plausible number:\n\njulia> rem(x, y)\nERROR: ArgumentError: rem is not defined for Binary8p4se: the draft defines no remainder; the exact result is generally not a datum, so any answer would round outside `project`. Compute on `decode(x)` and `Convert` back if that is what you want.\n[...]\n\njulia> F(1//3)\nERROR: ArgumentError: cannot exactly project a Rational into Binary8p4se; convert explicitly, e.g. Binary8p4se(Float64(x)), and own the double rounding\n[...]\n\nAn out-of-domain value is different from a refused operation: it has a defined answer. Under an unsigned format, a negative input is out of domain and the projection delivers NaN rather than throwing.\n\njulia> Binary8p4uf(-1.0)\nNaN","title":"Refusals are explicit"},{"category":"section","location":"45-operations.html#Blocked-and-scaled-forms","page":"Operations","text":"Every register operation also has a Block* form (lanewise over shared-scale Blocks, with an explicit result scale) and a Scaled* form (§5.8, over scale/value pairs). Both are generated from the same registry rows, so their arities follow the scalar operation's. See Advanced examples.","title":"Blocked and scaled forms"},{"category":"section","location":"45-operations.html#Discovering-the-register","page":"Operations","text":"julia> [(o.name, o.arity, o.group) for o in first(AIFloats.operations(), 3)]\n3-element Vector{Tuple{Symbol, Int64, Symbol}}:\n (:Abs, 1, :A)\n (:Add, 2, :A)\n (:ArcCos, 1, :B)\n\njulia> AIFloats.operationinfo(:FMA).arity\n3\n\nAIFloats.operations() and AIFloats.operationinfo are the supported way to enumerate the register; the underlying OP_REGISTRY is private and its shape is not part of the interface.","title":"Discovering the register"},{"category":"section","location":"90-contributing.html#contributing","page":"Contributing","text":"First of all, thanks for the interest!\n\nWe welcome all kinds of contribution, including, but not limited to code, documentation, examples, configuration, issue creating, etc.\n\nBe polite and respectful, and follow the code of conduct.","title":"Contributing guidelines"},{"category":"section","location":"90-contributing.html#Bug-reports-and-discussions","page":"Contributing","text":"If you think you found a bug, feel free to open an issue. Focused suggestions and requests can also be opened as issues. Before opening a pull request, start an issue or a discussion on the topic, please.","title":"Bug reports and discussions"},{"category":"section","location":"90-contributing.html#Working-on-an-issue","page":"Contributing","text":"If you found an issue that interests you, comment on that issue what your plans are. If the solution to the issue is clear, you can immediately create a pull request (see below). Otherwise, say what your proposed solution is and wait for a discussion around it.\n\ntip: Tip\nFeel free to ping us after a few days if there are no responses.\n\nIf your solution involves code (or something that requires running the package locally), check the developer documentation. Otherwise, you can use the GitHub interface directly to create your pull request.","title":"Working on an issue"},{"category":"section","location":"98-fma128.html#fma128","page":"FMA","text":"warning: Unstable — not part of the public interface\nImplementation detail. Names and behavior may change in any release without a deprecation. The supported surface is the Reference.\n\nAIFloats.fma128 is a pure-Julia fused multiply–add for Quadmath.Float128, vendored from SmallFloats.jl (provenance header in src/carriers/fma128.jl).\n\nIt exists because Float128 is a carrier, not an oracle: libquadmath's arithmetic is never used to decide a rounding, so the exactness proofs that let Group A operations skip the enclosure ladder need a fused operation carrying a guarantee of its own. A computed product p is proved exact by fma128(x, y, -p) == 0, which holds only if the fused operation is genuinely fused — with a rounded multiply followed by a subtraction, the residual is not the residual. See Algorithms for where those proofs sit in the evaluation path.","title":"FMA — fused multiply–add"},{"category":"method","location":"98-fma128.html#AIFloats.Float128FMA.fma128-Tuple{Quadmath.Float128, Quadmath.Float128, Quadmath.Float128}","page":"FMA","text":"fma128(x::Float128, y::Float128, z::Float128) -> Float128\n\nCorrectly rounded (round-to-nearest, ties-to-even) x*y + z with a single rounding, computed in pure Julia integer arithmetic; bit-for-bit compatible with libquadmath's fmaq, including signed zeros, subnormals (gradual underflow), infinities, NaN payload propagation, and invalid-operation NaN generation.\n\n\n\n\n\n","title":"AIFloats.Float128FMA.fma128"},{"category":"method","location":"98-fma128.html#AIFloats.Float128FMA.mul256-Tuple{UInt128, UInt128}","page":"FMA","text":"Exact 128x128 -> 256-bit product, returned as (hi, lo).\n\n\n\n\n\n","title":"AIFloats.Float128FMA.mul256"},{"category":"method","location":"98-fma128.html#AIFloats.Float128FMA.shl256-Tuple{UInt128, UInt128, Int64}","page":"FMA","text":"Left shift by 0 <= s < 256 (callers never shift set bits past bit 255).\n\n\n\n\n\n","title":"AIFloats.Float128FMA.shl256"},{"category":"method","location":"98-fma128.html#AIFloats.Float128FMA.shr256jam-Tuple{UInt128, UInt128, Int64}","page":"FMA","text":"Right shift by s >= 0 with \"jamming\": if any shifted-out bit is nonzero, the result's least-significant bit is set (a sticky bit in the lowest position).\n\n\n\n\n\n","title":"AIFloats.Float128FMA.shr256jam"},{"category":"method","location":"98-fma128.html#AIFloats.Float128FMA.sub256-NTuple{4, UInt128}","page":"FMA","text":"a - b for 256-bit values, assuming a >= b.\n\n\n\n\n\n","title":"AIFloats.Float128FMA.sub256"},{"category":"section","location":"20-concepts.html#concepts","page":"Concepts","text":"Machine learning runs into a wall that general-purpose arithmetic does not: moving weights and activations costs more than computing with them. The response has been to shrink the numbers — 8 bits, 6, 4, sometimes fewer. At those widths the design choices IEEE 754 made for 32- and 64-bit arithmetic stop paying for themselves. A second NaN, a signed zero, or an exponent range reserved for specials costs a noticeable fraction of everything the format can say.\n\nIEEE P3109, the draft Standard for Arithmetic Formats for Machine Learning, is the response. A P3109 format is completely determined by four parameters, and AIFloats.jl models exactly those four.","title":"Concepts"},{"category":"section","location":"20-concepts.html#The-four-parameters","page":"Concepts","text":"A Binary specifier is written Binary{K,P,S,D}.\n\n Name Meaning\nK bitwidth total bits the format occupies\nP precision significand bits, counting the implicit leading bit\nS signedness SIGNED or UNSIGNED\nD domain FINITE or EXTENDED\n\nNothing else is needed. There is no separate flag for subnormals, no exponent-bits field, no bias parameter — each follows from these four.","title":"The four parameters"},{"category":"section","location":"20-concepts.html#The-bit-budget","page":"Concepts","text":"Every bit is either sign, exponent, or stored significand:\n\nK  =  S  +  exponent bits  +  (P - 1)\n\nP - 1 rather than P because the leading significand bit is implicit — it is not stored, so precision always exceeds the stored significand by one. That is what AIFloats.TrailingSignificantBitsOf returns:\n\njulia> B = Binary(8, 4, SIGNED, FINITE);\n\njulia> PrecisionOf(B), AIFloats.TrailingSignificantBitsOf(B)\n(4, 3)\n\nRearranged, the exponent gets whatever is left:\n\nexponent bits  =  (K - S) - (P - 1)\n\nThe - (P - 1) is the stored significand, not P, which is why the rearrangement keeps a +1 that is easy to drop. So for the format above: (8 − 1) − 3 = 4 exponent bits, alongside 1 sign bit and 3 stored significand bits — and 1 + 4 + 3 = 8, the whole budget. ExponentBitwidthOf is the same arithmetic:\n\njulia> ExponentBitwidthOf(B), AIFloats.TrailingSignificantBitsOf(B), SignednessOf(B)\n(4, 3, true)\n\njulia> ExponentBitwidthOf(B) + AIFloats.TrailingSignificantBitsOf(B) + SignednessOf(B)\n8\n\nThe report derives the exponent bias from the same budget: B = 2^(K-P-1) for a signed format and 2^(K-P) for an unsigned one (ExponentBiasOf). Both are 2^(E-1) in the exponent width E above — a useful check that the two derivations agree.","title":"The bit budget"},{"category":"section","location":"20-concepts.html#Dropping-the-sign-buys-range","page":"Concepts","text":"An unsigned format spends no bit on sign, so the same K and P leave it one more exponent bit — twice the dynamic range, for giving up negative values. That is a real trade for quantities known to be non-negative, such as post-ReLU activations or attention weights.\n\nFormat K P sign exponent stored significand bias\nBinary(8, 4, SIGNED, …) 8 4 1 4 3 8\nBinary(8, 4, UNSIGNED, …) 8 4 0 5 3 16\n\njulia> U = Binary(8, 4, UNSIGNED, FINITE);\n\njulia> ExponentBitwidthOf(B), ExponentBitwidthOf(U)\n(4, 5)\n\njulia> ExponentBiasOf(B), ExponentBiasOf(U)\n(8, 16)","title":"Dropping the sign buys range"},{"category":"section","location":"20-concepts.html#Where-the-validity-rules-come-from","page":"Concepts","text":"Binary rejects combinations that do not describe a format. The rules read directly off the budget:\n\nP > 0 — a format needs at least one significand bit.\nP <= K - S — after the sign bit, at least one bit must be left for the exponent. This is the - S term: signed formats can reach P = K - 1, unsigned formats P = K.\nK > 2 — below three bits nothing survives the other two rules.\n\njulia> Binary(8, 7, SIGNED, FINITE)     # 1 sign + 6 stored + 1 exponent\nBinary{8, 7, ±, ⏥}\n\njulia> Binary(8, 8, UNSIGNED, FINITE)   # no sign, so P may reach K\nBinary{8, 8, +, ⏥}\n\njulia> Binary(8, 8, SIGNED, FINITE)     # would need 9 bits\nERROR: ArgumentError: Invalid format: K=8, P=8, S=SIGNED, D=FINITE\n[...]","title":"Where the validity rules come from"},{"category":"section","location":"20-concepts.html#Signedness","page":"Concepts","text":"SIGNED formats represent negative values; UNSIGNED formats represent magnitudes only.\n\nP3109 formats have a single zero — there is no negative zero. The code point that would encode −0 in an IEEE 754 layout is put to other use.","title":"Signedness"},{"category":"section","location":"20-concepts.html#Domain","page":"Concepts","text":"FINITE and EXTENDED name which values exist beyond the reals:\n\nDomain Value set Displayed\nFINITE reals ∪ {NaN} ⏥\nEXTENDED reals ∪ {NaN, ±Inf} ∞\n\nwarning: \\\"Extended\\\" means infinities\nIt does not mean extra bits, extra exponent range, or extended precision. An extended format is one whose value domain has been extended with the infinities — nothing more. Choosing EXTENDED spends representable values on ±Inf; choosing FINITE keeps them for finite numbers.\n\nEither way there is exactly one NaN. Where IEEE 754 has millions of NaN payloads, P3109 has one, and spends the difference on numbers.","title":"Domain"},{"category":"section","location":"20-concepts.html#Subnormals-are-not-a-parameter","page":"Concepts","text":"Subnormals follow from P alone:\n\nP == 1 — no subnormals.\nP > 1 — gradual underflow, with 2^(P-1) - 1 subnormal magnitudes.\n\nSo there is no has_subnormals argument to set. Zero together with the subnormal magnitudes is called the prenormal region, and it holds 2^(P-1) values.","title":"Subnormals are not a parameter"},{"category":"section","location":"20-concepts.html#Naming","page":"Concepts","text":"P3109 names a format Binary⟨K⟩p⟨P⟩⟨u|s⟩⟨f|e⟩ — bitwidth, p, precision, then u/s for unsigned/signed and f/e for finite/extended. Each name maps one-to-one onto a Binary:\n\nP3109 name AIFloats.jl\nBinary8p4sf Binary(8, 4, SIGNED, FINITE)\nBinary8p4se Binary(8, 4, SIGNED, EXTENDED)\nBinary8p4uf Binary(8, 4, UNSIGNED, FINITE)\nBinary4p2ue Binary(4, 2, UNSIGNED, EXTENDED)\n\nEach generated alias names a format, not a datum and not a datum type. The capital Binary follows Julia's convention for names that denote types, and Binary8p4se is exactly the type Binary(8, 4, SIGNED, EXTENDED) returns:\n\njulia> Binary8p4se === Binary(8, 4, SIGNED, EXTENDED)\ntrue\n\njulia> BinaryValue(Binary8p4se)          # the datum TYPE is a separate spelling\nBinaryValue(Binary8p4se)\n\nBinary formats sets out the full F / T / x model. The separate IEEE 754 aliases binary16, binary32, binary64, and binary128 retain their standard lowercase spellings.","title":"Naming"},{"category":"section","location":"20-concepts.html#Compared-with-IEEE-754","page":"Concepts","text":" IEEE 754 P3109\nSmallest standard width 16 bits a handful of bits (this package: K > 2)\nZeros +0 and −0 one zero\nNaNs many payloads exactly one\nInfinities always present only when EXTENDED\nSubnormals always when P > 1\n\nThe IEEE 754 binary formats are available under their standard names — binary16, binary32, binary64, binary128 — along with bfloat16, so they can be referred to the same way.","title":"Compared with IEEE 754"},{"category":"section","location":"10-getting-started.html#getting-started","page":"Getting started","text":"","title":"Getting started"},{"category":"section","location":"10-getting-started.html#Install","page":"Getting started","text":"AIFloats.jl is not yet in the General registry, so install it from its repository:\n\njulia> using Pkg; Pkg.add(url = \"https://github.com/JeffreySarnoff/AIFloats.jl\")\n\njulia> using AIFloats\n\nTo work against a local checkout instead:\n\njulia> using Pkg; Pkg.develop(path = \"/path/to/AIFloats.jl\")\n\nOnce the package is registered, Pkg.add(\"AIFloats\") will be the short form.","title":"Install"},{"category":"section","location":"10-getting-started.html#Describe-a-format","page":"Getting started","text":"A format needs four things: a bitwidth, a precision, a signedness, and a domain.\n\njulia> B = Binary(8, 4, SIGNED, FINITE)\nBinary{8, 4, ±, ⏥}\n\nThat reads as: 8 bits wide, 4 bits of significand, negatives representable, no infinities. The glyphs in the display are a shorthand — ± for signed, ⏥ for finite. Ask for the spelled-out form with string:\n\njulia> string(B)\n\"Binary{8, 4, SIGNED, FINITE}\"\n\nBinary returns a type, not a value:\n\njulia> B isa Type\ntrue\n\nThat is deliberate: a format is a compile-time description, and the four parameters are all there is to it. There is no instance to make — B() is an error, on purpose. When you need a value to store in an array or to dispatch on, you want a datum, and its type is BinaryValue(B):\n\njulia> T = BinaryValue(B)\nBinaryValue(Binary8p4sf)\n\njulia> B(1.5) isa T\ntrue\n\nThose three spellings are the whole model, and the rest of the documentation uses these letters for them:\n\nLetter What it is How you get it\nF the format Binary(K, P, S, D), or an alias like Binary8p4se\nT the datum type BinaryValue(F)\nx a datum F(1.5), T(1.5), fromcode(F, c)\n\nA datum is not an instance of its format — B(1.5) isa B is false, because a format describes a value set rather than belonging to one.","title":"Describe a format"},{"category":"section","location":"10-getting-started.html#Values-and-code-points-are-distinct-views","page":"Getting started","text":"Every constructor argument is a number, and that includes every Unsigned. To name a raw code point, use fromcode — a different question with a different spelling:\n\njulia> F = Binary8p4se;\n\njulia> fromcode(F, 0x45)          # the datum stored at code point 0x45\n1.625\n\njulia> F(0x45)                    # the number 0x45 is 69, projected into F\n72.0\n\nThey used to be told apart by the argument's type, which silently made F(codepoint(y)) a reinterpretation. Now the two never collide.","title":"Values and code points are distinct views"},{"category":"section","location":"10-getting-started.html#Read-the-fields-back","page":"Getting started","text":"julia> BitwidthOf(B), PrecisionOf(B)\n(8, 4)\n\njulia> SignednessOf(B), DomainOf(B)\n(true, false)\n\nSignednessOf and DomainOf return raw Bools. The predicates are usually easier to read:\n\njulia> is_signed(B), is_unsigned(B)\n(true, false)\n\njulia> is_finite(B), is_extended(B)\n(true, false)\n\nEach capitalized query has a lower-case Julia-style spelling, and the pair is the same function object — not a wrapper, not a compatibility shim:\n\njulia> bitwidth === BitwidthOf, signedness === SignednessOf\n(true, true)\n\njulia> bitwidth(B), domain(B), codetype(B)\n(8, false, UInt8)\n\nformatinfo answers all of them at once, and folds to a literal when the format is known at compile time:\n\njulia> info = formatinfo(B);\n\njulia> info.bitwidth, info.precision, info.exponentbits, info.exponentbias\n(8, 4, 4, 8)","title":"Read the fields back"},{"category":"section","location":"10-getting-started.html#Pick-storage-types","page":"Getting started","text":"Two helpers answer \"what do I keep this in?\":\n\njulia> CodeType(B)\nUInt8\n\njulia> ValueType(B)\nFloat32\n\nCodeType is wide enough for any bit pattern of the format; ValueType is wide enough to hold any of its values exactly. Both widen as the format does:\n\njulia> CodeType(16), ValueType(16)\n(UInt16, Quadmath.Float128)","title":"Pick storage types"},{"category":"section","location":"10-getting-started.html#Say-how-to-round","page":"Getting started","text":"Rounding and saturation are chosen together, as a Projection:\n\njulia> p = Projection(RTE, SF)\nρ(RTE, SF)\n\njulia> RoundOf(p), SatOf(p)\n(RTE, SF)\n\nAll 27 pairings already exist as constants, so you rarely build one:\n\njulia> p === RTE_SF\ntrue\n\nThe task's default is RTE_SN — round to nearest ties-to-even, no saturation:\n\njulia> DefaultProjection()\nρ(RTE, SN)\n\nThat default is task-local, bound for a dynamic extent by with_projection and never by a setter. Two other defaults are deliberately different: rand draws under RTZ_SN and randn under RTE_SF. See Projections for what each mode does.","title":"Say how to round"},{"category":"section","location":"10-getting-started.html#Invalid-formats-are-rejected","page":"Getting started","text":"Not every set of four numbers describes a real format. The constructor checks:\n\njulia> Binary(8, 8, SIGNED, FINITE)\nERROR: ArgumentError: Invalid format: K=8, P=8, S=SIGNED, D=FINITE\n[...]\n\nEight significand bits plus a sign bit needs nine bits, leaving nothing for the exponent. The Concepts page explains the bit budget these rules come from.","title":"Invalid formats are rejected"},{"category":"section","location":"97-internals.html#dyadic-numbers","page":"Dyadic Numbers","text":"warning: Unstable — not part of the public interface\nEverything on this page is implementation detail. Names, types, and behavior may change in any release without a deprecation, and nothing here is covered by the package's contract. The supported surface is the Reference; use decode, the register operations, and project rather than depending on a particular carrier.\n\nAIFloats.Dyadic is the exact carrier: an exact dyadic rational S · 2^Q in an Int128 significand, together with the three non-finite rows. Every datum of every P3109 format is exactly S · 2^Q with |S| < 2^16, so arithmetic over datums is closed and exact here and allocation-free — MPFR buys nothing for it. Transcendental fallbacks still escalate to MPFR through an exact BigFloat image.\n\nIt is the carrier of the widest rung, above Float64 and Float128. Which rung a format uses follows from its exponent span rather than its storage width; see Algorithms for where the carriers sit in the evaluation path.\n\nDyadic <: Real, deliberately not <: AbstractFloat. Methods elsewhere written ::AbstractFloat mean \"a float carrier\", and Dyadic implements about ten operations rather than the full AbstractFloat obligation. AIFloats.promotecarrier targets BigFloat, never this type.\n\nThe exact fixed-point accumulator behind the block reductions is this type, and its alignment band — DYADIC_ALIGN_MAX, 94 bits — is what makes the sticky protocol sound: a tail discarded past that band lies below every rounding threshold in play, so recording its direction loses nothing.\n\nVendored from SmallFloats.jl, with the provenance headers in src/carriers/dyadic.jl, and verified against golden digests captured from the original (test/support/dyadic_golden.sha256).","title":"Dyadic Numbers"},{"category":"module","location":"97-internals.html#AIFloats.DyadicNumbers","page":"Dyadic Numbers","text":"DyadicNumbers\n\nThe rung-3 exact carrier: an exact dyadic rational S · 2^Q plus the three non-finite rows, with Dyadic as its type.\n\nEvery datum of every P3109 format is exactly S · 2^Q with |S| < 2^16, so arithmetic on datums is closed and exact here, allocation-free — MPFR buys nothing for it. Transcendental fallbacks still escalate to MPFR through an exact BigFloat image.\n\nDyadic <: Real, deliberately not <: AbstractFloat: methods elsewhere written ::AbstractFloat mean \"a float carrier\", and Dyadic implements about ten operations rather than the full AbstractFloat obligation. AIFloats.promotecarrier targets BigFloat, never this.\n\nImplementation detail, and unstable: it is documented on the Dyadic Numbers page, not in the public reference. Vendored from SmallFloats.jl (provenance header in src/carriers/dyadic.jl) and verified against golden digests in test/support/dyadic_golden.sha256.\n\nNot exported; reach it as AIFloats.DyadicNumbers and its type as AIFloats.Dyadic.\n\n\n\n\n\n","title":"AIFloats.DyadicNumbers"},{"category":"type","location":"97-internals.html#AIFloats.DyadicNumbers.Dyadic","page":"Dyadic Numbers","text":"Dyadic(S::Int128, Q::Int64)          -> the exact value S · 2^Q\nDyadic(kind::UInt8)                  -> one of the three non-finite rows\n\nAn exact dyadic rational, the rung-3 evaluation carrier. S is signed and deliberately not normalized (the only consumer, round_to_precision, realigns anyway), so == compares values, not fields. kind is the LAST field: trailing, the tag lands in Q's slack and the struct is 32 bytes.\n\n\n\n\n\n","title":"AIFloats.DyadicNumbers.Dyadic"},{"category":"method","location":"97-internals.html#AIFloats.DyadicNumbers.add_dy-Tuple{AIFloats.DyadicNumbers.Dyadic, AIFloats.DyadicNumbers.Dyadic}","page":"Dyadic Numbers","text":"add_dy(x, y) -> Dyadic\n\nExact sum when the operands align within DYADIC_ALIGN_MAX; otherwise the caller is past the sticky threshold and add_sticky_dy applies (this throws). The kind carries the IEEE ∞/NaN algebra.\n\n\n\n\n\n","title":"AIFloats.DyadicNumbers.add_dy"},{"category":"method","location":"97-internals.html#AIFloats.DyadicNumbers.add_dy_checked-Tuple{AIFloats.DyadicNumbers.Dyadic, AIFloats.DyadicNumbers.Dyadic}","page":"Dyadic Numbers","text":"add_dy_checked(x, y) -> Dyadic\n\nadd_dy with its width precondition enforced (a separate function, not @boundscheck: that elides only under @inbounds, and no engine call site is one).\n\n\n\n\n\n","title":"AIFloats.DyadicNumbers.add_dy_checked"},{"category":"method","location":"97-internals.html#AIFloats.DyadicNumbers.add_sticky_dy-Tuple{AIFloats.DyadicNumbers.Dyadic, AIFloats.DyadicNumbers.Dyadic}","page":"Dyadic Numbers","text":"add_sticky_dy(x, y) -> (Dyadic, Int)\n\nThe sum with a sticky sign in {-1, 0, +1} describing a tail below the returned value's last bit: the exact band when the operands align, the sign-only band otherwise.\n\n\n\n\n\n","title":"AIFloats.DyadicNumbers.add_sticky_dy"},{"category":"method","location":"97-internals.html#AIFloats.DyadicNumbers.dyadic_to_rational-Union{Tuple{T}, Tuple{Type{T}, AIFloats.DyadicNumbers.Dyadic}} where T<:Integer","page":"Dyadic Numbers","text":"dyadic_to_rational([T=BigInt,] x::Dyadic) -> Rational{T}\n\nThe exact value as a rational. ±Inf map to ±1//0 (as Base does); NaN throws. A narrow T is checked, never wrapped. The reduction is a shift.\n\n\n\n\n\n","title":"AIFloats.DyadicNumbers.dyadic_to_rational"},{"category":"method","location":"97-internals.html#AIFloats.DyadicNumbers.exponent_dy-Tuple{AIFloats.DyadicNumbers.Dyadic}","page":"Dyadic Numbers","text":"⌊log₂|x|⌋; undefined for zero and the non-finite rows (throws).\n\n\n\n\n\n","title":"AIFloats.DyadicNumbers.exponent_dy"},{"category":"method","location":"97-internals.html#AIFloats.DyadicNumbers.isdyadic-Tuple{Rational}","page":"Dyadic Numbers","text":"Whether q is exactly a Dyadic: its denominator is a power of two (±1//0 count).\n\n\n\n\n\n","title":"AIFloats.DyadicNumbers.isdyadic"},{"category":"method","location":"97-internals.html#AIFloats.DyadicNumbers.mul_dy-Tuple{AIFloats.DyadicNumbers.Dyadic, AIFloats.DyadicNumbers.Dyadic}","page":"Dyadic Numbers","text":"mul_dy(x, y) -> Dyadic\n\nExact product, unchecked: precondition nbits(x.S) + nbits(y.S) ≤ 96, which every operand the engine forms satisfies (a datum carries ≤ 16 bits). mul_dy_checked enforces it.\n\n\n\n\n\n","title":"AIFloats.DyadicNumbers.mul_dy"},{"category":"method","location":"97-internals.html#AIFloats.DyadicNumbers.mul_dy_checked-Tuple{AIFloats.DyadicNumbers.Dyadic, AIFloats.DyadicNumbers.Dyadic}","page":"Dyadic Numbers","text":"mul_dy_checked(x, y) -> Dyadic\n\nmul_dy with its width precondition enforced: throws ArgumentError rather than wrapping when the significand product would leave Int128.\n\n\n\n\n\n","title":"AIFloats.DyadicNumbers.mul_dy_checked"},{"category":"method","location":"97-internals.html#AIFloats.DyadicNumbers.rational_to_dyadic-Tuple{Rational}","page":"Dyadic Numbers","text":"The exact Dyadic for a dyadic rational; refuses (InexactError) rather than rounds.\n\n\n\n\n\n","title":"AIFloats.DyadicNumbers.rational_to_dyadic"},{"category":"method","location":"97-internals.html#Base.Math.frexp-Tuple{AIFloats.DyadicNumbers.Dyadic}","page":"Dyadic Numbers","text":"(f, e) with x == f · 2^e, f ∈ [½, 1), exactly.\n\n\n\n\n\n","title":"Base.Math.frexp"},{"category":"method","location":"97-internals.html#Base.Math.ldexp-Tuple{AIFloats.DyadicNumbers.Dyadic, Integer}","page":"Dyadic Numbers","text":"x · 2^n, exactly and unconditionally: an exponent-field add, no range to leave.\n\n\n\n\n\n","title":"Base.Math.ldexp"},{"category":"method","location":"97-internals.html#Base.Math.significand-Tuple{AIFloats.DyadicNumbers.Dyadic}","page":"Dyadic Numbers","text":"significand(x) ∈ [1, 2), exactly (an exponent-field move).\n\n\n\n\n\n","title":"Base.Math.significand"},{"category":"method","location":"97-internals.html#Base.decompose-Tuple{AIFloats.DyadicNumbers.Dyadic}","page":"Dyadic Numbers","text":"(S, Q, 1); den == 0 signals non-finite, as Base's contract requires.\n\n\n\n\n\n","title":"Base.decompose"},{"category":"method","location":"97-internals.html#Base.round-Tuple{AIFloats.DyadicNumbers.Dyadic}","page":"Dyadic Numbers","text":"Round half to even, matching Base.round(::AbstractFloat).\n\n\n\n\n\n","title":"Base.round"},{"category":"section","location":"index.html#AIFloats","page":"AIFloats","text":"AIFloats.jl describes the small binary floating-point formats used in machine learning — the 8-bit, 6-bit, 4-bit, and smaller formats that trade precision for memory bandwidth.\n\nIt follows IEEE P3109, the draft standard for arithmetic formats for machine learning, in which a format is completely determined by four things: how many bits it occupies, how many of those carry the significand, whether it represents negative values, and whether it includes the infinities. Those four make a Binary format specifier.\n\nnote: The normative source, and what it is not\nEvery semantic claim in this documentation conforms to: the IEEE Working Group P3109 Interim Report on Arithmetic Formats for Machine Learning, version 4.0.3 (1 September 2026), available here: P3109/Public.That report is an unapproved draft; its cover states it must not be used for conformance or compliance purposes. conformance reports what this package implements, in the shape the draft's §4.6 describes — not IEEE approval, not a certification, and not a compliance determination.AIFloats uses some language and spelling that differs from the report. In all cases this is done to glean advantage from working with Julia, in accord with Julia best practices. In all cases, this is noted in the docstring, or in the documentation. Often the Julia function name does not shadow the report's name, and both are available.\n\njulia> using AIFloats\n\njulia> B = Binary(8, 4, SIGNED, FINITE)   # 8 bits wide, 4 bits of precision\nBinary{8, 4, ±, ⏥}\n\njulia> BitwidthOf(B), PrecisionOf(B)\n(8, 4)\n\njulia> is_signed(B), is_extended(B)\n(true, false)\n\njulia> CodeType(B), ValueType(B)          # how to store a code, and a value\n(UInt8, Float32)\n\nnote: Scope\nAIFloats.jl models formats and their values, including encoding, decoding, correctly rounded scalar and array operations, shared-scale blocks, and packed storage. See Implementation status for the supported surface and deliberate limits.","title":"AIFloats"},{"category":"section","location":"index.html#Installation","page":"AIFloats","text":"AIFloats.jl is not yet in the General registry, so install it from its repository:\n\njulia> using Pkg; Pkg.add(url = \"https://github.com/JeffreySarnoff/AIFloats.jl\")\n\njulia> using AIFloats\n\nTo work against a local checkout instead:\n\njulia> using Pkg; Pkg.develop(path = \"/path/to/AIFloats.jl\")\n\nOnce the package is registered, Pkg.add(\"AIFloats\") will be the short form.","title":"Installation"},{"category":"section","location":"index.html#Where-to-go-next","page":"AIFloats","text":"The documentation is in four parts, and the menu on the left follows the same shape.\n\nLearn the model. Read in order the first time; each page assumes the one before it.\n\nPage What it covers\nGetting started Build a format and ask it questions, step by step\nConcepts What K, P, S and D mean, and why the rules are what they are\nBinary formats The F / T / x model, aliases, queries, validity, display\nProjections Rounding and saturation modes, the projections that pair them, and the defaults\nOperations The register: signatures, result formats, refusals, and how each result is reached\n\nWorked examples. Every block runs on its own in a fresh session.\n\nPage What it covers\nBasic examples Construct datums, calculate, inspect, convert\nIntermediate examples Explicit projections, saturation, the stochastic contract, arrays\nAdvanced examples Packed storage, blocks, reductions, block conversion\nTechnical examples Table policy, cache snapshots, diagnostics, conformance\n\nLook something up. Generated, and complete by construction.\n\nPage What it covers\nReference Every exported and public name, in four listings\nFormats, datums, projections Binary, the axes and queries, BinaryValue and the codec, display, projections\nOperations, kernels, storage The register operations, vmap/vmap!, the table policy surface, PackedVector\nBlocks and scaled operations Block, BlockVector, and the generated Block* and Scaled* families\nConformance conformance and the ϰ registry, the IEEE 754 aliases, carrier-level types and switches\nBenchmark results Generated from a run of the suite, with the commit and machine it describes\n\nHow it works, and how to work on it. Mechanism rather than interface — none of it is a stable contract.\n\nPage What it covers\nAlgorithms Stochastic rounding, interval enclosure, and the sticky protocol\nImplementation What is implemented, the deliberate limits, and what costs what\nDyadic Numbers The exact carrier behind the block reductions\nFMA · FAA The pure-Julia fused operations for Float128\nContributing · Developing Filing an issue; running the tests, the benchmarks and the docs build","title":"Where to go next"},{"category":"section","location":"index.html#Contributors","page":"AIFloats","text":"This work would not have been possible without the ongoing contributions from many members of the IEEE SA Working Group P3109.\n\n<!-- ALL-CONTRIBUTORS-LIST:START - Do not remove or modify this section -->\n<!-- prettier-ignore-start -->\n<!-- markdownlint-disable -->\n\n<!-- markdownlint-restore -->\n<!-- prettier-ignore-end -->\n\n<!-- ALL-CONTRIBUTORS-LIST:END -->","title":"Contributors"}]
}

*.jl.*.cov
*.jl.cov
*.jl.mem
*.rej
.DS_Store
.benchmarkci
Manifest.toml
benchmark/*.json
coverage
docs/build/
env
node_modules

# Documentation pages GENERATED by docs/make.jl — never hand-edited, and
# rebuilt by every docs build (including CI). 60-benchmarks.md comes from a
# live benchmark run; the reference listing pages are built from the public
# surface (the 95-reference.md landing page is hand-written and tracked).
docs/src/60-benchmarks.md
docs/src/95-reference/
